# デザインブック採用分の実装計画（2026-09-15 凍結）

**起案: 統括（Fable）/ 実装: 実装部隊（Opus, jisso）/ 本人裁定: 2026-09-14〜15**

con moto の部門規程「アプリのデザイン」（32項目）を Ficus に仮に当て、本人が採用を裁定した
10件を実装する。**con moto のフォルダには一切書き込まない**（Ficus は本社の管轄外。読むだけ）。
モック（本人が見て裁定した姿）: https://claude.ai/code/artifact/4bffd2c4-8390-4276-98fa-a5ec2d7a6672

## 0. 実装部隊が着手前に必ず読むもの

1. `design/DESIGN-SYSTEM.md` §6.0（正典が勝つ）・§1.11（動きの時間の表）・§2（角丸）・§3（余白）
2. `scripts/pitch-test.mjs` の冒頭の `codeOf()` ── **綴りを数える検査はコメントを剥がしてから**。
   コメントの中の綴りを数えて穴を空けた事故が3度ある
3. `docs/superpowers/plans/2026-09-10-community-plan-5-moderation.md` §0（作業の作法）

### 検証ゲート（3つ全部。1つでも落ちたら未完成）

```bash
rtk proxy npx vitest run
rtk proxy node scripts/pitch-test.mjs
rtk proxy npx vite build
```

- `pitch-test` はテキスト検査で構文解析をしない。**ビルドが落ちていても全緑になりうる**（実証済み）。
  だから3つ回す
- 検査を足したり向け直したりしたら、**壊す変異で落ちることを確かめる**（変異試験）。
  変異は複製で行い、必ず元に戻す。`git stash` は素で使わない（共有スタック）
- 本人の実機確認が要るものは「未確認」と報告に書く。Chrome の実測を実機の証拠にしない

### 触ってはいけないもの

- `C:\Users\tonbi\.claude\sessions\con moto\` 配下（読むのは可。書き込み・実行結果の保存は不可）
- `firestore.rules`・`src/community/*Repo.js`（サーバー側の形を変える理由が無い）
- `src/community/LoadingRing.jsx`・`loadProgress.js`（決着ずみ）
- **不採用の項目**: F2（必須/任意の表記）・R1/4-1 の押下の手応え（`:active`）・D7（`?? 0`）
- コミュニティの「参加してプロフィールを作る」ボタン（本番に実アカウントが出来る）。検証は
  使い捨ての probe ページで部品だけ描き、終わったら消す

### コミット

- 便ごとに1コミット。本文は「本人裁定 → 何を変えたか → なぜその形か → 検査の増減と変異試験」。
- 末尾: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- `git push` はしない（配信は統括が本人の承認を取ってから行う）

---

## 便の分け方（順に。並列にしない）

| 便 | 中身 | 危険度 |
|---|---|---|
| **A** | 文言と補助文: F1 / F7 / F3・F4 / R7 / T5 / T2・T3 | 低 |
| **B** | 通知の帯と削除の作り替え: 帯の部品 / T6・T8・R6 / T7→R8 / T7 アカウント削除 | 中 |
| **C** | トークンと正典: 4-1 動きの時間 / 2-1 文字サイズ / 2-2 角丸・余白 | 高（正典と検査が動く） |

各便の終わりに3ゲート + 変異試験 + 報告。統括が受け入れてから次の便へ。

---

## 便 A ── 文言と補助文

### A-1. F1 ラベルを外へ（2箇所）

**目安の名前** `src/App.jsx:9424-9430`
- `placeholder="目安の名前"` を外し、input の直上にラベルを置く。ラベルの見た目はコミュニティの
  `Field` のラベルと同じ（`--fs-xs` / `--c-ink-2` / 600 / `letter-spacing .02em`）。
  文言: **「目安の名前」**
- `aria-label` は残す（読み上げ用）

**開封後日数の最小・最大** `src/App.jsx:14869-14882`（分析タブ › 絞り込み › 軸「開封後日数」）
- `placeholder="最小"` `"最大"` を外し、各 input の直上に小さいラベル（`--fs-xs` / `--c-ink-3`）
  「最小」「最大」を置く。「日目」の語はそのまま。モックの「参考 — 直すならこう」の形

合格: `placeholder="目安の名前"` `placeholder="最小"` `placeholder="最大"` が 0 件。
既存の検査でこれらの placeholder を錨にしているものがあれば**向け直す**（緩めない）。

### A-2. F7 ボタンの語（2つだけ）

- `src/App.jsx:8542` 「取り直し」→ **「取り直す」**
- `src/community/CommunityTab.jsx:1015` `{busy ? "保存中…" : "作成"}` → **「プロフィールを作る」**
  （編集時の分岐があれば「保存」側は触らない。初回作成の語だけ）
- **「登録」「保存」「完了」「追加」は変えない**（本人裁定）

### A-3. F3・F4 ニックネームの欄

`src/community/CommunityTab.jsx:921-928` と `src/community/profile.js:117-125`（`validateNickname`）

- 補助文（`Field` の `note`）は **「ニックネームは他の利用者に公開されます」だけ**にし、右端に
  **「n / 20」**（`tabular-nums`、`--c-ink-3`）を同じ行に出す。n は `[...value].length`
  （`profile.js:120` と同じ数え方＝コードポイント）
- 入力のたびに `validateNickname(nickname)` を呼び、エラーがあれば**その欄の直下**に赤文字
  （`--fs-xs` / `--c-danger`）で出す。文言は `validateNickname` が返すもの（「使えない文字が
  含まれています」など）。**空欄のときは出さない**（まだ何も入れていない人に赤を見せない）
- エラーがあるあいだ input に `box-shadow: inset 0 0 0 1px var(--c-danger)`
- **下の `error`（1012）は残す**。ただしニックネームのエラーは下に重ねて出さない
  （`submit` で `buildProfileDoc` が返したエラーがニックネーム由来なら、欄の側だけに出す）。
  楽器種別・属性など他の欄のエラーは従来どおり下
- 検査: `validateNickname` を欄が呼んでいること / 補助文が「他の利用者に公開されます」と
  「/ 20」だけであること（「記号」「絵文字」「20文字まで」を補助文に**書かない**）

### A-4. R7 押せない理由

- **リード枠の select** `src/App.jsx:7827-7832`（`disabled={isRecording || !selectedBoxGroup}`）:
  箱が未選択のとき、枠の直下に「箱を選ぶとリードを選べます」（`--fs-xs` / `--c-ink-3`）。
  録音中の disabled には**出さない**（環が録音中だと言っている）
- **目安の名前の「保存」** `src/App.jsx:9442-9446`（`disabled={!name.trim()}`）:
  名前が空のとき input の直下に「名前を入れると保存できます」。入れたら消える
- ボタンの語は変えない（キャンセル / 保存 のまま）

### A-5. T5 マイクの案内

`src/App.jsx:3748` `setErrorMsg("マイクにアクセスできませんでした")` と描画 `4150-4165`

- 文言を3行の構成に:
  1. 見出し（太字・`--c-danger` のまま）: **「マイクの使用が許可されていません」**
  2. 本文（`--fs-xs` / `--c-ink-3`）: **「設定 › Ficus › マイク を「許可」にすると計測できます」**
     （本人裁定: アプリとしてリリースする前提の経路。Safari の経路は書かない）
  3. 操作: **「もう一度試す」**（B型ピル `.ctl-plain .ctl-pill`）→ `startListening` を再実行
- `errorMsg` は文字列 state なので、マイク拒否だけ**構造を持つ**必要がある。
  `ERROR_MEASURE_ONLY` の仕組みを壊さない形で、マイク拒否用の定数キーを1つ足し、描画側で
  そのキーのときだけ3行構成にする（他のエラーは従来どおり1行）
- 検査: 「マイクにアクセスできませんでした」が 0 件 / 新しい3行の綴りがある

### A-6. T2・T3 コミュニティの0件

`src/community/screens.jsx` の `Empty`（`418-423` 順位 / `618` 機材 / `932` 目安）と
`FilterRow`（165）、`directory.js` の `isFiltered` / `EMPTY_FILTER` / `ANY`

- 絞り込み中（`isFiltered(filter)` または画面ごとの `isFilteredBy`）の0件では:
  1. 文言に**条件名を入れる**: 「{ジャンル} × {属性}で、{期間}に練習した人はまだいません」の形。
     条件が1つならその1つだけ。楽器は「絞り込みに数えない」画面（932 のコメント）ではその
     規則に従う
  2. 文の下に **「条件を外す」**（B型ピル）。押すと filter を `EMPTY_FILTER`（楽器の既定は
     その画面の規則どおり `saxTypes[0]`）に戻す
- 絞り込み中でない0件は現行の文言のまま、ボタンも出さない
- 3画面で同じ部品にする（`Empty` に `onClear` と条件の文を渡す形）。写しを3つ作らない
- 検査: 3画面とも `onClear` を渡していること / 絞り込み無しのときに「条件を外す」が出ないこと

---

## 便 B ── 通知の帯と削除の作り替え

### B-0. 帯の部品（1つ）

新しい共有部品 **`ActionNotice`**（`src/App.jsx` 内。App の根で1つだけ描く）。

- 置き場: `position: fixed`、下部ナビの直上（`bottom: calc(var(--nav-h) + var(--sp-3) + safe-area)`）、
  左右は `--page-side-pad`。**画面上端には出さない**（R6: 操作の隣。録音ボタンも FAB も下にある）
- 見た目: `.card` の面（`--c-surface` / `--r-md` / 影は既存の `0 8px 24px rgba(15,23,42,.18)`）、
  文字 `--fs-sm`、左にアイコン or ✓（`--c-good`）、右端に操作（`--c-accent` / 700）
- 中身は state `{ text, action?: { label, onPress }, expiresAt }` を1つ持つ。新しい通知が来たら
  前のを置き換える（積まない）
- **時間**: 既定 5000ms。残り秒数を出す通知（削除）は `Math.ceil(残り/1000)` を1秒ごとに描き直す。
  時間の値は index.css ではなく JS が持つ（見た目の曲線ではなく「待つ長さ」なので §1.11 の対象外。
  `loadProgress.js` の τ と同じ理由をコメントに書く）
- 出現・消失の動き: `--d-base`（便 C で作る。便 B の時点では `220ms` を **1箇所の定数**で持ち、
  便 C でトークンへ差し替える）。`prefers-reduced-motion` では動かさない
- `role="status"` は付けない（数字が減るたび読み上げる）。`aria-live="polite"` を**帯の文だけ**に
  付け、秒数の数字は `aria-hidden`
- 他のタブへ移っても時間は止めない（迷わせない）。帯は App の根にあるので移っても見える

### B-1. T6・T8・R6 保存の合図

- `registerPendingSession`（`src/App.jsx:3189`）の直後に帯:
  **「✓ 12:34 の計測を保存しました」** + 操作 **「開く」**（その計測の詳細を開く。既存の
  `onOpenSession` 相当）。時刻は `recordedAt` を `HH:mm`
- 取り込み（動画/音声の解析）の**完了**通知（`14653-14656` 付近の進捗表示は残す）: 解析が終わった
  合図を同じ帯で「解析が完了しました」+「目安に設定」or「開く」。上端の完了通知があれば帯へ寄せる。
  **進捗バー自体は動かさない**（それは操作の場所の中にある）
- 検査: `registerPendingSession` が帯を呼ぶこと / 文言に時刻の書式があること

### B-2. T7→R8 リード・計測の削除

`src/App.jsx` の `window.confirm` 4件: `10040`（箱）/ `10046`（枚）/ `10710`（この箱）/
`14562`（計測）。**アカウント削除（CommunityTab）は対象外**（B-3）。

- confirm を出さない。押した瞬間に一覧から消し、帯:
  **「{対象名}を削除しました」** + 操作 **「元に戻す {n}」**（n は残り秒、5 → 0）
  対象名の例: 「Select Jazz #3S の2箱（6枚）」「V16 #3 の3枚」「計測 4件」。銘柄が複数なら
  「2箱（6枚）」のように数で
- **戻し方**（既存の作りで足りる。新しい永続化は作らない）:
  - リード: `reeds` は `usePersistedState`（`3002`）なので、消した個体を退避しておき、
    「元に戻す」で `setReeds(prev => [...prev, ...退避])`。`deleteReeds`（`10009`）が外した
    セッションの `reedId` / `linkedAt` も退避して `updateSessions` で戻す
  - 計測: `deleteSessions`（`2240`）は `idbDeleteSessions` を即時に呼ぶ。退避したセッションを
    `addSession` 相当（`setSessionsState` + `idbPutSessions`）で戻す
- 5秒で帯が消えたら退避を捨てる（＝本当に消える）。帯が出ているあいだに別の削除が来たら、
  前の退避は**その時点で捨てる**（積まない。モックの裁定どおり「意味がない」を作らない）
- 選択モード（`exitMode` / `exitSelectionMode`）は従来どおり抜ける
- 検査: `window.confirm(` が `App.jsx` で 0 件 / 帯の文言に「元に戻す」がある / 退避を捨てる
  経路がある（5秒後・次の削除）。**変異**: 「元に戻す」が退避を戻さない変異で落ちること

### B-3. T7 アカウント削除のシート

`src/community/CommunityTab.jsx:1102-1116`（`window.confirm`）→ 既存の `BottomSheet`
（`App.jsx:13493` `{ ariaLabel, onClose, children }`）で1枚。

- 見出し: **「アカウントを削除しますか」**
- 行（`k / v` の2列。ラベル `--c-ink-3`）:
  - **消えるもの**: 「ニックネーム「{nickname}」・プロフィール」
  - **外から見えなくなるもの**: 「公開している目安 {n}件」（n = `Object.keys(myIdeals ?? {}).length`。
    0 のときはこの行を出さない）
    ── 本人指摘のとおり、目安の本体はこの端末にあり、サーバーの公開コピーだけが消える
    （`accountRepo.js:72` `unpublishAllIdeals`）。「消える」と書かない
  - **残るもの**: 「この端末の計測・リード・目安の記録」
  - 「戻せるか」の行は**置かない**（本人裁定）
- 区切りの下に補助文: **「外部公開を停止したい場合は、削除せずに非公開にできます」**
- ボタン2つ（縦）: **「非公開にする」**（B型。押すと `onTogglePublic(false)` してシートを閉じる。
  すでに非公開ならこのボタンを出さない）/ **「アカウントを削除する」**（塗りの危険色
  `--c-danger`。既存の「アカウントを削除」と同じ作法）
- `ProfileView` に `nickname` は `profile` から取れる。`myIdeals` は `JoinedView`（138）が
  持っているので prop で渡す（`ProfileView` の呼び出し `244`）
- 失敗時の `DELETE_ERROR` は従来どおりシートの中に出す
- 検査: `window.confirm(` が `CommunityTab.jsx` で 0 件 / 「外部公開を停止したい場合は、
  削除せずに非公開にできます」の綴り / 「戻せ」の語が無い

---

## 便 C ── トークンと正典（**正典を書き換える便。**§6.0 の逆向き。B10 案アの前例）

### C-1. 4-1 動きの時間を3段に

- `src/index.css` の `:root` に **`--d-fast: 120ms; --d-base: 200ms; --d-slow: 320ms;`**
- `design/DESIGN-SYSTEM.md` §1.11 の表を書き換える（**検証33 がこの表を読む**ので表が先）:

| 動き | 旧 | 新 |
|---|---|---|
| 中身が消える | 140 | `--d-fast` 120 |
| 中身が現れる | 220 | `--d-base` 200 |
| 枠を閉じる | 240 | `--d-base` 200 |
| 枠を開く | 320 | `--d-slow` 320 |
| シートの暗幕・シート | 360 | `--d-slow` 320 |

  遅延（60ms）と曲線（`cubic-bezier(0.32, 0.72, 0, 1)`）は変えない
- index.css の該当宣言（`.day-panel*` の transition / `.sheet-scrim` `.sheet-card` の animation
  `687, 690`）を **トークン参照**にする。検証33 が「値の綴り」で突き合わせているなら、
  表と検査の両方を「トークン名 → 値」で読める形に**向け直す**（緩めない）
- 便 B の帯の 220ms 定数を `--d-base` に差し替える
- 順位の光（3.4s / 3.6s）と呼吸（1.9s / 2.6s）は**触らない**（操作への反応ではなくループ。
  DESIGN-SYSTEM に「4-1 の『動きの時間』はこれを含まない」と1行書く）
- 変異: 表の 320 を 360 に戻す → 検証33 が落ちること

### C-2. 2-1 文字サイズ（利用者の画面に出る 9 / 10 / 11 / 12.5 → 12）

**触るもの**（すべて `--fs-xs` = 12px へ）:
- `9`: `src/App.jsx:14256` ▾（MyDataSection）
- `10`: `src/App.jsx:8598` PlainSelect のラベル（caption）
- `11`: `src/App.jsx:14767`（AnalysisLabView）
- `12.5`: `1873`（ReedStrengthPills）/ `8397, 8401, 8431, 8487, 8504`（MeasureView のテンポ・拍子
  シートのピルと行）/ `11459`（ReedCompareTab）
- `10.5`: メモのラベル（`pitch-test.mjs:14491` が錨。App 側の該当行を探す）

**触らないもの**: `MetroDiagPanel`（`6918-7231`。開発用の診断パネル。利用者に出ない）の
9.5 / 10 / 10.5 / 11 / 11.5 / 12.5。`hint` `key` `val` `tname` `tnum` `thead` `skey` `sval`
はこのパネルの持ち物

**正典の書き換え**（`design/north-star-measure.html`）:
- `.chev` 10px → 12px（40）/ `.selpill` 12.5px → 12px（89）/ `.ckrow` 12.5px → 12px（93）/
  `.mrow .l` 10px → 12px（156）/ `.dlabel` `.starrow .l` `.mrow .rl` 等の 11px → 12px
  （101, 105, 137, 152 と本文中の 11px）。**9px（669）も 12px**
- `design/canvas/*.dc.html` に同じ値の写しがあれば同時に直す（`seed-canvas.mjs --extract` の
  作法は `design/canvas/` の README / 既存コミットを見る）

**検査の向け直し**（`scripts/pitch-test.mjs`）: `12666`（.selpill 12.5px）/ `12800`（.ckrow）/
`9525` / `14491`（10.5）/ `16131`（9px ▾）ほか、`12.5` `10.5` `9` を綴りで見ている箇所を
**新しい値で**突き合わせる。正典から値を読んでいる検査は正典を直せば追随する

**DESIGN-SYSTEM**: §4（タイポグラフィ）と統一の棚卸し 0-1 の「正典が実寸を指定する12箇所」の
例外を**廃止**し、「利用者の画面の最小は `--fs-xs` = 12px。診断パネルだけが例外」と書く

合格: コメントを除いた `App.jsx` で `fontSize:\s*(9|9\.5|10|10\.5|11|11\.5|12\.5)\b` が
**`MetroDiagPanel` の外に 0 件**（検査にする。診断パネルの行範囲は関数名で切る）

### C-3. 2-2 角丸・余白の段

**角丸（5段 → 3段）** `src/index.css:26-31`
- 新しい正: **`--r-1: 8px; --r-2: 12px; --r-full: 999px;`**
- 旧名は**移行の別名**として残し、値を新しい正へ寄せる:
  `--r-xs: var(--r-1)`（4 → 8。入力欄・PlainSelect が丸くなる）/ `--r-sm: var(--r-1)` /
  `--r-md: var(--r-2)` / `--r-lg: var(--r-2)`（16 → 12。カードが角ばる）/ `--r-pill: var(--r-full)`
- 正典 `north-star-measure.html` の対応する radius も同じ値へ。シートの「上だけ 28px」
  （`pitch-test.mjs:4442` の裁定）は**別の部品**なので触らない
- 検査（`7831` `8597` `16462` `6435` `7830` `16133`）は**名前**で突き合わせているので通るが、
  「B型は入力欄のスケール `--r-xs`」等の説明文は新しい段の意味に書き直す

**余白（7段 → 5段）**
- `--sp-6: var(--sp-5)`（24 → 20）/ `--sp-8: var(--sp-5)`（32 → 20）。App での使用は
  `--sp-6` 1件・`--sp-8` 0件、index.css 各1件。見た目の差を報告に書く（どの画面のどこが 4px 詰まるか）

**行間（新設）**: `--lh-tight: 1.3; --lh-base: 1.6; --lh-loose: 1.75;`。
既存の `lineHeight: 1.6` / `1.7` / `1.35` の直書きは**この便では置き換えない**（範囲が広い。
トークンを置くことまで）。DESIGN-SYSTEM §4 に3段を書く

**DESIGN-SYSTEM**: §2 角丸の表を3段に、§3 余白を5段に書き換える。旧名は「別名（移行中）」と明記

変異: `--r-lg` を 16px に戻す → 正典との突き合わせが落ちること（落ちる検査が無ければ足す）

---

## 報告様式（便ごと）

1. 変更したファイルの一覧（行番号つき）
2. 施工内容（項目記号で。F1 / F7 / …）
3. 3ゲートの結果（数字）と変異試験の結果（何を壊して何件落ちたか）
4. 見た目が変わった箇所の列挙（2-2 は「どこが何px 変わったか」まで）
5. 未確認（実機でしか分からないもの）
6. 懸念（無ければ「なし」）

# 引き継ぎメモ（2026-08-28 時点 / D-18 まで）

**次に入る人はここを最初に読む。** この文書は「いま何が宙に浮いているか」だけを書く。
恒久的な知識は下記へ:

- **フローの正典**: `design/LOOP.md`（仕様凍結 → 波及の洗い出し → 実装 → 4つの検証ゲート → 記録 → デプロイ → 実機確認）
- **値の唯一の答え**: `design/DESIGN-SYSTEM.md`
- **直前の周の凍結仕様**: `design/D10-SPEC.md`（**実装は済んだ**。その前は `design/D9-SPEC.md`）
  D-11 以降は仕様書を起こさず、BACKLOG の完了記録に凍結値を書いている
- **残作業と、踏んだ罠の日誌**: `design/BACKLOG.md`（冒頭の「罠の目録」15件は毎周読む）

---

## セッション開始時の手順

1. `cat design/HANDOFF.md` → `design/LOOP.md` → BACKLOG の「罠の目録」「決着済みの前提」「現役の起票」
2. `npm install`（新しいワークツリーには `node_modules` が無い）
3. 基準値を取る: `npx vite build` と `node scripts/pitch-test.mjs`
   → **この周の PASS 数の下限**（直近の実測は **PASS 6897 / FAIL 0**）
4. `src/App.jsx` は約 13,400 行。**全文を読まない。** `grep -n` で絞ってから開く
5. **起票の記述を「まだ壊れている」の根拠にしない。** 着手前にコードで確かめる
   （D-3a は「3つとも呼び手ゼロ」と書いてあったが、2つは生きていた。N-5b は既に直っていた）

---

## いまの状態

- ブランチ `claude/design-handoff-continuation-4bec1d`
- 本番 = `main`。**D-9 〜 D-13 はデプロイ済み**（2026/08/27）。**D-14 〜 D-17a はデプロイ済み。D-18 は未デプロイ**
- 本番: https://wind-tone-lab.vercel.app

### 通したゲート（`LOOP.md` の4つ）

| | D-9 | D-10 | D-11〜13 | **D-14〜17a** |
|---|---|---|---|---|
| ① build | 成功 | 成功 | 成功 | 成功 |
| ② pitch-test | 6590/0 | 6693/0 | 6742/0 | **6877/0** |
| ③ 375×812 実測 | 済 | 済 | 済 | 済 |
| ④ 審査役 | 後追い→不合格→D-12で修正 | 4回（3回不合格） | 1回不合格→D-13 | **2回（1回不合格）→合格** |

**D-9 は審査を通さずに本番へ出て退行を出した。ゲート④を飛ばさないこと。**

### この周（D-14〜D-17a）の中身 ── 本人の実機フィードバック第7陣（14項目）

| 周 | 内容 |
|---|---|
| **D-14** | カレンダーの記録が無い日でも閉じる / すべてのセッションは先頭から / 「選択」→ゴミ箱 / `±0` 削除 / 「（その日）」削除 / シートの出現 / PIVOT の「編集」削除 |
| **D-15** | チューナー（数値と環を同じ値へ・平滑を時間ベースへ）/ メトロノーム（毎フレーム 170KB の確保を 0 へ・`<stop>` 60個の静的化・`memo`）/ 累計カードを濃紺へ / シートの作法を5枚へ |
| **D-16 / D-16a** | 折れ線を「線 + 面」へ（案4）。点を撤去し、**孤立した1音は `<path d="M x,y L x,y">` で救う** |
| **D-17 / D-17a** | 恒真検査の撤去 / **ハングの経路を塞ぐ** / 数表の訂正 / **環を速く（τ 102.55→60ms、抑制 1200→1110ms）** |

---

## 次にやること

### 1. 本人の実機確認（**上から重い**）

**2026/08/28 の本人確認の結果**: チューナー **OK** / メトロノーム **直っていない** /
折れ線の面 **却下**（実データはギザギザで面が凹凸を全部拾う）。

**メトロノームは推測で3周当てて効かなかった。** D-18 で計測タブに**診断の計器**を置いた。
**本人が `#metro-diag` を開いて30秒動かし、スクリーンショットを1枚**くれれば原因が確定する。
それまで**推測で4周目に入らないこと。**

| | 見どころ | 出どころ |
|---|---|---|
| 1 | **チューナーの環が速くなったと感じるか。** 逆に**ガタつかないか** ── τ=60 は 5Hz±10¢ のビブラートを **30.0% → 46.3%** 通す。**戻るとしたら「速さ」ではなくこちら** | D-17a |
| 2 | **メトロノームの振り子と拍が滑らかになったか。** 原因は絞り切れておらず、軽くする手当てだけ当てた | D-15 §2 |
| 3 | **折れ線の面（不透明度 0.28）が見えるか / 濃すぎないか。孤立した1音（2.4px の丸）が見えるか** | D-16 |
| 4 | 累計カードの濃紺が浮いていないか | D-15 §3 |
| 5 | シート5枚の出方が揃っているか | D-14 §6 / D-15 §4 |
| 6 | `.day-panel-inner` の `transform` で中身がちらつかないか（**F-106 と同じ領域**） | D-11 |
| 7 | 日付入力が画面から出ていないか | F-119 |

**τ を戻すときの注意**: **τ と抑制は対で動く**。`DESIGN-SYSTEM.md` §1.11 の2行を戻せば実装も検査も追随するが、**τ を上げるなら抑制も上げる**（窓 = 抑制 ∈ `(800+2.0149τ, 1000+2.0149τ]`、かつ「走り直しの境目 990ms」を保つ台地を掃引で取り直す）。片方だけ動かすと検査が落ちる。

### 2. 本人の裁定待ち

- **カードの大きさ・余白・色**（本人「カードの大きさとか、色も今一つなのかも」）。**案4 が実機に乗ってから**モックを出す、と統括が決めた
- **D-9b 式の既定** / **D-9a `--c-quiet` が読み手ゼロ** / **D-5a・N-9a PIVOT の条件チップが 20px**

### 3. 起票（現役）

`D-15a`（5/8・7/8 の振り子が1フレーム最大 **16.68°**＝上限の 1.93倍 跳ぶ。**♩≒55 から**超える）/
`D-17b`〜`D-17e`（`nsdf.fill` の到達条件 / 裸の `new Function` 2箇所 / 陳腐化コメント / `detectPitchMPM` が NaN を返し得る）/
`D-14a`〜`D-14d` / `D-13a` / `D-12a`（本人裁定「このまま」で決着）/ `D-12b` / `D-11a`

---

## モック（Claude Design キャンバス）

- 公開先: https://claude.ai/code/artifact/9cee0670-3053-4e82-aa82-6f08a9a1134b
- 作業ファイル: `design/canvas/`（`*.dc.html` / `canvas.json` / 生成器 `generate.mjs`）
- `Main.dc.html` / `Windows.dc.html` / `S1.dc.html` は**本人がキャンバス上で直接いじったものが元**。
  生成器で上書きする前に、必ず `seed-canvas.mjs --extract` で**本人の手直しを読み戻して** `generate.mjs` へ畳むこと
- 実装で使った実測値は完了記録（BACKLOG）の表に入っている

---

## 環境まわりの注意

1. **`node_modules` はワークツリーに付いてこない。** まず `npm install`
2. **検証ゲートは他の重い処理と並走させない**（`速度(60fps耐性)` の2件が負荷で落ちる。F-94）
3. **Browser pane はスクリーンショットが撮れないことがある**（ペインが非表示だと compositing が止まる）。
   判定は `getBoundingClientRect()` / `getComputedStyle()` の実測で行い、`resize_window` で **375×812** にしてから測る。
   **ペインが非表示だと `requestAnimationFrame` も発火しない**（走査ループが固まる）。待つなら `setTimeout`
4. **測る前に `fetch('/src/App.jsx')` で served コードに自分の変更が入っているか確かめる**（HMR 失敗で古いコードを測る事故）。
   **Vite の変換は空白を潰す**ので、正規表現は `minWidth:X` のように**空白なし**で書くこと
5. **変異試験は複製ツリーで単独逐次。** 実ツリーには当てない
6. **`git` のブランチ操作が auto mode の分類器に拒否されることがある。**
   その場合は本人に実行してもらう。**PowerShell では `&&` が使えない**ので、コマンドは1行ずつ渡すこと
7. IndexedDB はキー単位で復元。`clear()` / `deleteDatabase` 禁止

---

## このプロジェクトで繰り返し踏んでいる罠

**「構造上失敗し得ないアサーション」**。`design/LOOP.md` が名指しで禁じている。
D-9 では20件の変異のうち1件が生存した（「中央線を引かない」── ドメインの計算と目盛の文字だけを見ていて、
**線そのもの**を見ていなかった）。

**検査を書いたら必ず複製ツリーで変異試験を行い、壊すと実際に落ちることを確認してから報告する。**
あわせて **「KILL されては困る変異」**（宣言順の入れ替え・変数名の変更・無関係な属性の追加）も
必ず当てること。D-10 では**検査が正しい修正を落とす形**になっていたのを審査役が2回見つけた。

### D-14 → D-17 で新しく踏んだもの

**1. 「軽くする」変更が、壊れ方を「値が違う」から「固まる」へ変えることがある。**
毎フレームの確保をやめて配列を使い回したら、ゼロ埋めを1つ落としただけで `NaN` が入り、
山探索の `while` が**永久に回る**ようになった（`NaN <= 0` も `NaN > 0` も false）。
確保していた頃は構造的に起こり得なかった。**検査ハーネスは 90秒経っても集計行を1行も出さない。**
不変条件を1つ消すときは、**それが何を防いでいたか**を数えること。

**2. 本人の言葉を「症状」に切り詰めない。**
本人は「**円が遅い**」と原因まで言っていたのに、統括は「数値と円が食い違っている」とだけ読み、
**数値の側を遅らせて**「直った」と報告した。**環の速さは1msも変えていなかった。**

**3. 同じ数字を3人が別々に間違えた。**
5/8 の振り子の跳びを、診断役 24.75° / 実装役 8.10° / 正解 16.68°。原因は
**X/8 拍子がテンポ数値を付点四分として扱う**（`(60/tempo)/3`）ことの見落とし。
**式を導出した人だけが正解した。** 数値の突き合わせは、式の前提まで遡ること。

**4. 「たまたま合う式」は緑のまま通る。**
走り直しの境目を `ceil((抑制−尾)/10)*10` で見る検査は、採用した組では偶然一致していた。
**正当な別解が誤って赤になる**ことで初めて露見した。**期待値は式ではなく実測（掃引）から取る。**

### D-9 → D-12 で新しく踏んだもの

**0.「従来のまま」を主張するなら、従来を測る。**（罠16に昇格）
D-9 の完了記録に「他3画面は水平線4本＝**従来のまま**」と書いたが、**従来（D-8）は3本**だった。
**変更後だけを測って「変わっていない」と断定した**のが原因で、退行が本番まで出た。
比較対象を測っていない「不変」の主張は、主張ではなく願望。

**0b. 検査は、失敗ではなく「落ちない」ことがある。**
`new Function` で組んだ関数の**呼び出し**を try/catch で包まないと、壊れたときに
**ハーネスごと落ちて PASS/FAIL の集計行すら出ない**。ビルドは素通りするので、
**壊れているのに緑にも赤にもならない**。例外は偽値でも真値でも返さず、
`{ ok, v, err }` で返して `.ok` を条件に入れる（`scripts/pitch-test.mjs:442` の `runFn`）。

**0c. 規範文書に「禁止されている」と書く前に、何が禁止か確かめる。**
D-11 は `will-change` を断る根拠として「`contain` も `transform` も禁止」と書いたが、
**app-wide の禁止は `will-change` だけ**だった。誤った引用を規範に置くと、
次に触る人がそれを根拠にする。

### D-10 で新しく踏んだもの

**1. 幅が足りない行で「配分」をいじっても、損失は消えず移動する。**
軸の行を1行 30px に畳もうとして3周かかった。ラッパを挟む → はみ出す。`flex` を直す → 短い値から消える。
`min-width` で床を作る → **無条件の床なので、余っている組にまで不足を作る**（30組中14組が悪化）。
最後は**器の形を変えて**（等幅グリッド）解決した。**入らないものは入らない。**

**2. 規範に「測った2組から『どの組でも』へ一般化する」書き方をしてはいけない。**
`DESIGN-SYSTEM.md` §5.2 は**同じ節で同じ型の失敗を2回**している。
規範には**測った範囲・測り方・「この記述が言えないこと」**まで書く。

**3. 作法どうしが正面からぶつかることがある。**（D-9）
片方を破るのではなく、**不変条件を検査へ移して**両立させる。

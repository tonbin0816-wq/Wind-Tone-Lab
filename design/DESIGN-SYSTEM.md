# Ficus デザインシステム

**ループが参照する唯一の答え。** `BACKLOG.md` の各項目を実装するとき、値はすべてここから引く。
ここに無い値を新しく発明してはいけない。必要なら先にこの文書を更新する。

方向性の根拠は `design/DESIGN.md`（育てる × 静けさ / 二面サーフェス設計）。
現状の実数は `design/BACKLOG.md` の「検証済みの実数」。

作成: 2026-07-31 / 状態: **承認待ち**

---

## 0. 設計の前提

このシステムは**新しい見た目を作るためのものではない**。既に最も多く使われている値を正とし、
判別できない差異を消して、実装が毎回発明していた値を段階に畳むためのもの。

したがって各トークンの値は「今いちばん多く使われている実測値」を採用している。
移行しても**見た目はほとんど変わらない**。変わるのは、次に誰かが書くときに迷わなくなること。

---

## 1. 色

### 1.1 文字

| トークン | 値 | 用途 | 現状の出現 |
|---|---|---|---|
| `--c-ink` | `#121F32` | 最濃。見出し・主要数値 | 38 |
| `--c-ink-2` | `#435266` | 副次テキスト・本文 | 50 |
| `--c-ink-3` | `#8D95A1` | ミュート。ラベル・キャプション | 64 |
| `--c-ink-4` | `#A6AEBA` | 最薄。**装飾専用** | 7 |

> `--c-ink-4` は白背景でコントラスト比 **約2.2:1**、`--c-ink-3` は **約3.0:1**。
> どちらもWCAG 4.5:1 未満なので、**読ませたい文章には使わない**。軸目盛や区切り記号まで。

### 1.2 面

| トークン | 値 | 用途 |
|---|---|---|
| `--c-bg` | `#FFFFFF` | ページ背景。**地は白** |
| `--c-surface` | `#FFFFFF` | 面。沈めた面の上に置く白い要素（タイル等） |
| `--c-sunk` | `#F6F7F9` | 沈めた面。**データタブのパネル**（群の境界） |
| `--c-sunken` | `#EEF1F4` | もう1段沈めた面。**B型＝枠線を持たない操作対象の地**（§6.7）・サブタブの溝 |

> **2段ある理由。** `--c-sunk` は「群の境界」（見分けられれば足りる）、`--c-sunken` は
> 「ここは触れる」（操作できると分からせる）。後者のほうが強い信号が要る。
> 白地との差は **1.0719:1 / ΔL\* 2.79** と **1.1337:1 / ΔL\* 5.00**。

> **地が白なので、カードは「地より明るい面」では区別できない。** まとめ方は §6.6 の
> 「面の作法」がタブごとに決める。`--c-surface` は地と同値だが、**沈めた面の上に置く白**
> という別の役割で残している（値が同じでも役割が違うトークンは分ける。`--c-on-accent` と同じ考え）。

> `--c-sunk` は旧来の地の値をそのまま流用している。白地との差は **1.0719:1 / ΔL\* 2.79**。
> これは旧来の「白カード vs 地 `#F6F7F9`」とまったく同じ差で、**データタブの群の境界は
> 旧来と同じ強さ**。一度 `#F7F9FB`（2.17）にして弱めたことがあるので、
> `scripts/pitch-test.mjs` が **1.0719:1 を下限として**縛っている。**値ではなく差で縛る。**

### 1.3 罫線

| トークン | 値 | 用途 |
|---|---|---|
| `--c-line` | `#E9ECF0` | ヘアライン。区切り・タイルの枠 |
| `--c-rule` | `#E1E6EC` | **罫の作法で群の境界を担う線** |
| `--c-line-strong` | `#C3CAD3` | **A型（状態を持つもの）の枠**（§6.7） |

> **`--c-line-strong` は「入力欄の枠」ではなくなった。** 入力欄は §6.7 の B型（枠なし・地だけ）。
> この線は**状態を持つものにだけ**使う。

> **かつて「ヘアライン3値は判別できないので2値に畳む」と書いていた。それは地が `#F6F7F9`
> だった前提。** 地を白にすると罫が群の境界を担う唯一の信号になるので、ヘアラインより
> 1段強い `--c-rule` が要る。白地とのコントラストは **1.2551:1 / ΔL\* 8.92** で、
> 旧来いちばん強かった信号（カード枠 `#E9ECF0` vs 地 `#F6F7F9` = ΔL\* 3.93）の **2.3倍**。
> ただし **WCAG 1.4.11 の 3:1 には届かない。** 1m先で足りなければ、体系内で上げられる
> 次の段は `--c-line-strong`（1.6523:1 / ΔL\* 18.96）。**新しい値を発明しないこと。**

### 1.4 アクセント（紺）

| トークン | 値 | 用途 | 吸収する色 |
|---|---|---|---|
| `--c-accent` | `#174585` | プライマリ。**押せる／選ばれている物にだけ使う** | — |
| `--c-accent-mid` | `#7FA0CE` | 系列色の第2段 | — |
| `--c-accent-dim` | `#9DB3CC` | 弱いアクセント（オクターブ数字など） | `#9DB3D6` |
| `--c-accent-line` | `#B9C9E4` | 紺系の罫線・系列色の第3段 | `#D9E1EC` |
| `--c-accent-tint` | `#EAEFF5` | 選択状態の背景 | `#F3F6FA` |

> **`--c-accent` はアクション専用に戻す。** 現状 `MetricCard` の静的な補助行「目安: -18.2 dB」など、
> 押せない要素にも使われていて「押せる／選ばれている」の意味が薄まっている。
> 静的な補助テキストは `--c-ink-3` に落とす。
> **唯一の例外（§6.0 で規定）**: データタブのヒーロー1枚だけは、押せない面だが
> `--c-accent` の塗りを許す（「1画面に山は1つ」の山として。第2の例外を作らない）。

### 1.5 機能色（意味を持つ。装飾に使わない）

| トークン | 値 | ティント | 意味 |
|---|---|---|---|
| `--c-good` | `#16A34A` | `--c-good-bg` `#E8F6ED` | 合っている（0¢） |
| `--c-warn` | `#D97706` | `--c-warn-bg` `#FDF0E1` | やや外れ（13¢） |
| `--c-bad` | `#DC2626` | `--c-bad-bg` `#FEF2F2` | 大きく外れ（30¢以上） |

**厳守事項**
- この3色は `pitchBarColorRGB()` の補間端点であり、**音程の正誤という機能的意味**を持つ
- **系列識別色に流用しない**（現状 `PIVOT_LINE_COLORS` と `REED_COMPARE_COLORS` が違反）
- **★評価に使わない**（現状 `StarRating` が `#D97706`＝警告色を肯定的評価に使っている）
- 第2の機能色系を作らない（現状データタブのヒーローが Tailwind パステルで二重化している）

### 1.6 その他

| トークン | 値 | 用途 |
|---|---|---|
| `--c-disabled` | `#B7BFC9` | 無音時の音名など、値が無い状態 |
| `--c-danger` | `#DC2626` | 停止・削除・録音中。**破壊的/中断の操作** |
| `--c-on-accent` | `#FFFFFF` | `--c-accent` などの濃い面の上に乗る文字・アイコン |

> `--c-on-accent` は `--c-surface`（白いカードの面）と値が同じだが**役割が違う**。
> 面の色と、面の上に乗る文字の色を同じトークンで兼ねると、
> 片方だけ変えたいときに必ず破綻する。塗りは `--c-surface`、文字は `--c-on-accent`。

> `--c-danger` は `--c-bad` と**同じ値だが別の意味**。`--c-bad` は「音程が大きく外れている」
> という計測結果を指す機能色で、STOPボタンや削除ボタンに使うと意味が混ざる。
> 値が同じでも、参照するトークンで意味を分ける。将来どちらかの色を変えるときに効いてくる。

### 1.7 系列色（グラフの識別色）

**機能色とグレーは系列に使わない。** 紺の明度段階＋線種で分ける。

| 順 | 色 | 線種 |
|---|---|---|
| 1 | `--c-accent` `#174585` | 実線 |
| 2 | `--c-accent-mid` `#7FA0CE` | 実線 |
| 3 | `--c-accent-line` `#B9C9E4` | 実線（太め） |
| 4 | `--c-accent` `#174585` | 破線 |
| 5 | `--c-accent-mid` `#7FA0CE` | 破線 |
| 6 | `--c-accent-line` `#B9C9E4` | 破線（太め） |

> 理想値の破線は `--c-ink-3` `#8D95A1` を予約する。系列がこの色を取らないこと。
> 7系列以上が必要になったら、色を足すのではなく表示を絞る。

### 1.8 線の太さ

グラフの線幅。薄い色ほど太くしないと同じ強さに見えないため、色と対で決める。

| 用途 | 太さ |
|---|---|
| 軸・グリッド・罫線 | `1px` |
| 系列（`--c-accent` / `--c-accent-mid`） | `2px` |
| 系列（`--c-accent-line`）＝ §1.7 の「実線（太め）」 | `3px` |

**破線のパターン**: `stroke-dasharray="4 3"`。§1.7 の破線系列とゼロ基準線で共通。

### 1.9 グラフの寸法要件

グラフは**コンテナ実測値に追従**させる。固定幅にしない。

| 要件 | 値 |
|---|---|
| SVG の実寸 : `viewBox` 幅 | **1 : 1**（`preserveAspectRatio` による全体縮小は禁止） |
| 項目ラベル欄の上限 | コンテナ幅の **42%** |
| 目盛ラベルの左右余白 | **`--sp-2`（8px）以上** |
| 目盛ラベルの上下余白 | **`--sp-1`（4px）以上**（ディセンダと書体差の逃げ） |

> **縮小禁止の理由**: 全体を縮めれば固定幅でも収まるが、12px の文字が実効 9.6px になり
> §4.1 のサイズスケールが無意味になる。文字は常に真のpxサイズで描く。

> **余白が `--sp-2` の理由**: `getComputedTextLength()`（送り幅）と実際のインク幅は一致せず、
> 和文グリフはインクが送り幅を **最大1.6px程度**超える（実測値）。この超過量は書体とグリフ次第で
> 上限が保証されないため、`--sp-1`(4px) では「検証したら足りていた」に留まる。
> 余白は `--sp-2`(8px) を取るか、`getBoundingClientRect()` の実インク幅で検算して決める。

> SVG のプレゼンテーション属性には `var()` が書けない。数値を使う場合は
> ファイル内に1つ定数を置いてそこから引き、マジックナンバーを散らさないこと。
> 色は `style` 属性なら `var()` が解決される。

---

## 2. 角丸

現状 **19値・105箇所**。5段に畳む。

| トークン | 値 | 用途 | 吸収する値 |
|---|---|---|---|
| `--r-xs` | `4px` | 入力欄・select・小さなタグ | 1, 2, 3, 5 |
| `--r-sm` | `8px` | カード内の小ブロック | 6, 9, 10 |
| `--r-md` | `12px` | カード内のブロック | 11, 12 |
| `--r-lg` | `16px` | カード | 14, 20 |
| `--r-pill` | `999px` | ピル・丸ボタン | （既に一貫。22箇所） |

円は `50%` のまま（7箇所。既に一貫）。

> 「白背景 + `1px solid` + 角丸」という**同一のカードが22箇所**あるのに、角丸は 6 / 14 / 16 / 11 と4種類あった。
> この3点セットの直書きは **20箇所 → 0** になり、`.card` クラスに集約済み。

> **カードの角丸は「作法」が決める（§6.6）。** 罫の作法では **0**（箱にしない）、
> 沈める作法では `--r-md`。**個々のカードが角丸を直書きしてはいけない。**
> 直書きするとインライン style が CSS より強いので、その要素だけ作法が効かなくなる。

---

## 3. 余白

`gap` は既に 4/6/8/10/12 で87%が収まっており、**唯一まともに揃っている軸**。これを全体の基準にする。

| トークン | 値 |
|---|---|
| `--sp-1` | `4px` |
| `--sp-2` | `8px` |
| `--sp-3` | `12px` |
| `--sp-4` | `16px` |
| `--sp-5` | `20px` |
| `--sp-6` | `24px` |
| `--sp-8` | `32px` |

**カードの内側余白は `--sp-4`（16px）に統一。**
現状は `"16px 18px"` `"14px 16px"` `"17px"` `"14px"` `"12px 14px"` の5種。

### レイアウト定数

| トークン | 値 | 用途 |
|---|---|---|
| `--nav-h` | `47px` | 下部ナビの高さ（`borderTop 1px + 46px`） |
| `--page-pad` | `14px` | ページ左右余白 |

**ページ下端の余白は必ず `calc(var(--nav-h) + env(safe-area-inset-bottom))`。**
現状は `72px` 固定が2箇所に複製されており、iPhone 12〜15 の PWA で**9pxがナビに隠れる**。

**画面いっぱいの高さは `100vh` ではなく `100dvh`。**（`100vh` を先に書いてフォールバックにする）
iOS Safari の `100vh` は**アドレスバーを含む高さ**を返すため、可視領域より大きい `min-height` が
敷かれ、コンテンツが収まっていても常にアドレスバーぶんだけ縦スクロールできてしまう。
**JSから可視高を測るときも `window.visualViewport.height` を優先**する
（無ければ `innerHeight`）。`visualViewport` の `resize` も購読して測り直すこと。

---

## 4. タイポグラフィ

### 4.1 サイズ

既存の7段を正とする。**`--fs-2xl` は現在0回使用だが廃止しない**（28pxの受け皿が無いと26/24が復活する）。

| トークン | 値 | 用途 | 吸収する値 |
|---|---|---|---|
| `--fs-xs` | `12px` | 補助・キャプション・軸目盛 | 9, 9.5, 10, 11 |
| `--fs-sm` | `13px` | 副次テキスト | — |
| `--fs-md` | `15px` | 本文 | 14 |
| `--fs-lg` | `18px` | 小見出し | 17 |
| `--fs-xl` | `22px` | 見出し | 24 |
| `--fs-2xl` | `28px` | 大見出し | 26 |
| `--fs-hero` | `46px` | 数値ヒーロー | 36, 42 |

**グラフSVG内の 9 / 9.5 / 10 / 11px は `--fs-xs`（12px）に引き上げる。**
DESIGN.md §4.1 が「9〜10.5pxの細かい刻みを廃止する」と明示している。
入りきらない場合はグラフ幅を可変化して解決する（サイズを下げない）。

### 4.2 演奏中サーフェス専用スケール（音名の組み方）

7段とは別系統。**確定済み（2026-07-31）。**

| 定数 | 値 | 対象 |
|---|---|---|
| `NOTE_FS_RATIO` | **`0.3576`** | 音名の文字サイズ = **環の直径 × この比** |
| `NOTE_SCALE_X` | **`1.30`** | 音名の横幅。`transform: scaleX()` で明示指定 |
| `NOTE_ACC_RATIO` | `0.34` | 臨時記号 ♯/♭ = 音名サイズ × 0.34 |
| `NOTE_OCT_RATIO` | `0.31` | オクターブ数字 = 音名サイズ × 0.31 |

**音名は環の直径に比例させる（固定pxにしない）。**
環が縮んだときに音名だけ据え置くと、音名と環内周の余白（クリアランス）が
55px/辺 → 14px/辺 のように4倍近く変わり、同一サーフェス内で見え方が別物になる。
比で持てば、環がどの大きさでもクリアランスの比率が一定に保たれる。

| 環の直径 | 音名サイズ | 使う場面 |
|---|---|---|
| `RING_D_FULL` = `330px` | 118px | **常にこれ**（素のチューナー / メトロノームを開いている間とも） |

> **環はメトロノームの開閉で大きさを変えない（2026-07-31 確定）。** 以前は開くと 330→250 に
> 縮めていたが、演奏中サーフェスの主役が開閉のたびに別の大きさになるのは読み取りを妨げる。
> 縮小で捻出していた縦スペースは、**メトロノームを開いている間だけ「これまでの音」
> （`PitchDeviationLine`・quiet時94px）を隠す**ことで確保する。メトロノーム使用中は
> リズムに集中していて、ピッチの履歴は合間に読むものだから。
> 比の仕組み（`NOTE_FS_RATIO × 環の直径`）は残す。環が固定なので結果は118px固定になる。

**クリアランス要件**: 最も幅の広い音名（`G♯3` 等、臨時記号＋オクターブ付き）でも、
`scaleX` 適用後の視覚幅の左右端が環の内周に対し **音名サイズの10%以上**の余白を持つこと。
実測で確認する。

**横幅を明示する理由**: Instrument Serif の既定の送り幅は **0.457em**（一般的なserifの64%）。
細さ自体は1m先から読む演奏中サーフェスに合っているが、書体既定のままでは
「幅を選んでいない」見え方になる。`scaleX(1.30)` を掛けて **0.594em** にする。

**scaleX のレイアウト補正**: `transform` は要素のレイアウト幅を変えないため、変形後のグリフが
左右に `(1.30-1)/2 = 15%` はみ出す。送り幅 0.457em の15%分を左右marginで補う
（`NOTE_SCALE_PAD_EM`）。これを入れないと臨時記号と重なる。

**臨時記号の扱い（確定）**
- **文字として組む**（SVGで描かない）
- **`scaleX` は掛けない**。本体だけに掛ける
- 音名本体と近いサイズにしない。**付属物として明確に小さく**する
- 既知の制約: `♯`(U+266F) `♭`(U+266D) は Instrument Serif に**収録されていない**ため
  フォールバック書体で描画される。この見え方を採用する判断済み

**実測（375px幅・利用可能幅347px）**

| 表示 | 音名の視覚幅 | 臨時記号 | 重なり | 総幅 |
|---|---|---|---|---|
| `A4` | 70.1px | — | — | 87.5px |
| `B♭4` | 73.3px | 40.1px | 0.4px | 130.5px |
| `F♯5` | 62.6px | 40.1px | −0.9px | 120.6px |

**演奏中サーフェスでは `--fs-xs`（12px）を使わない。** 最小は `--fs-md`（15px）。
1m先・チラ見・周辺視野で12pxは読めない。読めない文字を置くくらいなら、置かない。

### 4.3 書体の使い分け

| 変数 | 書体 | 使ってよい対象 |
|---|---|---|
| `--font-jp` | OS標準和文（iPhoneはヒラギノ） | 和文すべて |
| `--font-num` | Space Grotesk | 数値・英字（セント値・Hz・dB・テンポ・日付） |
| `--font-serif` | Instrument Serif | **音名とリード番号のみ。それ以外に使わない** |

### 4.4 字間

| クラス | 値 | 対象 |
|---|---|---|
| `.sans` | `letter-spacing: 0.02em` | 和文全般（**現在は空の宣言。中身を入れる**） |
| `.num-tight` | `letter-spacing: -0.02em` | 24px以上の数値 |

> `.sans` は現在 `font-family: var(--font-jp)` だけで、`body` と同一宣言＝**141箇所すべてが no-op**。
> 一方 `.jp-label`（+0.02em）は**使用0回**。「静けさ・和の間」を最も直接担う字間設計が丸ごと未適用。
> `.sans` の中身を字間に差し替えれば、**141箇所を一切触らずに index.css の1行**で全和文に効く。
> 全画面に効くので375pxでの目視検証を必ず行うこと。

---

## 5. タップ領域

**最小 44×44pt。例外なし。**

現状 `<button>` 53個中 **44個が44pt未満**。最頻タップの下部ナビすら実高32px。

実装方針: **見た目の大きさは変えない。当たり判定だけ広げる。**
`minHeight: 44` + 透明パディング、または疑似要素で拡張する。

> DESIGN.md §2 では「合間サーフェス＝手は自由」だが、**手が空いた瞬間に押す**操作なので、
> 通常アプリより**むしろ大きくすべき**。指の太さはサーフェスによって変わらない。

### 5.1 例外その1 ─ カレンダーのマス（2026/08/23 本人裁定・D-6）

**My Data のカレンダーのマスだけ 38pt**（`CALENDAR_CELL_H`）。他はすべて 44pt のまま。

本人が `design/mydata-size-band-proposals.html` の4案を**実寸で見比べて**選んだ結果。
なぜこの1箇所だけ例外になったのかを記録として残す:

- **「見た目は小さく当たり判定だけ広げる」手が、縦方向には使えない。**
  指標タブ・子タブでは行の `gap` を 0 にして左右へ padding を入れることで、
  見た目の間隔を保ったまま当たり判定を 44pt にしている。しかしカレンダーの行は
  **上下で隣接している**ので、縦に広げると隣の行と当たり判定を食い合う。
  つまり**行の高さ＝タップの高さ**で、逃げ道がない。
- 横は 45.9pt あるので実寸は **38 × 45.9**。指の当たりは横に余裕がある。
- 見返りは 6行 × 6px = **36px**。同じ周でマトリクスの窓を 28 → 40px に上げているので、
  ページ全体では +36px に収まっている。

**この例外を広げないこと。他の場所で 44 を割ってよい根拠にはならない。**
`scripts/pitch-test.mjs` の 27.8 が「38 であること」と「断り書きが残っていること」を
両方固定してあるので、黙って他所へ波及させることはできない。

### 5.2 例外その2 ─ My Data の「式」の行（2026/08/25 本人裁定・D-9 §3）

**My Data の式の行（`[ 8/24 ] × [ my平均 ]`）のチップだけ高さ 20px**
（`MY_DATA_EXPR_CHIP_H`）。行そのものは 35px（`MY_DATA_EXPR_ROW_H`）。

本人の言葉:

> min高さが44に設定されていましたが、この行で44は余白が大きすぎるのでここは最小44の必要はありません

なぜこの行だけ例外になったのかを記録として残す:

- この行は**画面のいちばん上に3行目として積まれる**（子タブ44 → 指標タブ44 → 式の行）。
  44pt を3つ積むと、データが始まる前に 132px を使う。本人は D-7 / D-8 と
  **2周続けて**「この上部の縦幅が過剰」と名指ししている。
- **「見た目は小さく当たり判定だけ広げる」手はここでは使える。**
  縦は行の高さいっぱい（35px）、横はチップの幅まで当たり判定を広げてある
  （5.1 のカレンダーと違い、上下に隣接する行が無いので食い合わない）。
  つまり**見た目 20px / 当たり判定 35px** で、44 を割るのは当たり判定のほう。
- 同じ行に同居する見せ方の切替（`ViewToggle`）も**行と同じ 35px**にしてある。
  ここだけ 44 のままだと、行が 44 に戻って例外の意味が消える。

**この例外を広げないこと。** `scripts/pitch-test.mjs` の 27.4c / 27.9 が
「チップは 20px」「行は 35px」「切替も行と同じ高さ」を固定してあるので、
黙って他所へ波及させることはできない。

---

## 6. 二面サーフェスの設計言語

### 6.0 北極星の設計言語（2026/08/11 本人確定。目標状態）

**正典**: `design/north-star-measure.html`（確定版モック）と `design/north-star-coverage.md`。
移行は `design/BACKLOG.md` の N-1〜N-6。

> ## ⚠️ 見た目については、モックが唯一の正典（2026/08/12 本人確定）
>
> **本人指示**: 「色塗りやテキストサイズなどデザインについてはモックを正とする。
> 今のデザインに不満があってモックを作って一新しようとしているので、
> **既存のデザインにかかるルールは気にする必要がない**」
>
> したがって、モックと本書の既存規定が食い違う場合は **無条件でモックが勝つ**。
> 該当するのは少なくとも次の規定で、**これらはモックに対する制約として機能しない**:
> - §6.7 の A型/B型（枠線を持てるのは状態を持つ物だけ）→ モックの塗りピルが優先
> - §6.1 の「演奏中サーフェスで12px禁止」→ モックの 12px が優先
> - §4.1 のフォントスケール7段 → モックの 76px / 20px 等が優先
> - §2 の角丸スケール・§3 の余白スケール → モックの 28px / 34px / 30px 等が優先
> - §1.4 の「`--c-accent` はアクション専用」→ モックの塗り面が優先
>
> **「DESIGN-SYSTEM に無い値を発明しない」は、モックに書かれている値には適用しない。**
> モックの値はそれ自体が本人の決定であって、発明ではない。
> 移行が済んだ画面については、**モックの値を本書へ逆輸入して**規定を書き換えること
> （古い規定を残したまま実装だけ変えると、次の担当が「規定違反」と誤読する）。
>
> ただし**機能に関わる規定は引き続き有効**: §1.5 の機能色の意味（緑=合っている等）、
> §6.5 の OKLCH 補間と環の色、§5 のタップ領域 44px、計測アルゴリズム。
> 本人指示は「デザイン（色・サイズ・形）」についてのもので、意味と操作性は対象外。
> 機能とモックが衝突したときは**機能を残す**（2026/08/12 本人確定）。

**3原則**
1. **説明を消して形に語らせる** — 枠・囲い・ラベル・常設ガイドは「これはこういう物です」
   という説明。データだけを残し、意味は大きさ・位置・余白・濃さで伝える
2. **1画面に山は1つ** — 各画面で一番見たい物を1つ決めて強くし、残りを脇役に落とす。
   計測=音名 / リード=箱とタイル / データ=青いヒーロー
3. **今に関係ない物は出ていない** — 拍のガイドは鳴っている間だけ。演奏が始まると周辺は淡くなる。
   使用頻度の低い操作は「…」メニューへ

**囲いの序列**（まとまりを作る手段。上ほど優先）
1. **余白**で分ける
2. **揃え**（グリッド）で分ける
3. **罫線1本**で分ける
4. **面（カード）**で分ける — **1画面に1枚まで**。現在その1枚は
   データタブの青いヒーロー（`--c-accent` の面）だけ。グレーの面は使わない

**部品の統一**
- 選択・フィルタ・切替は**同じ形のピル**。ふるまいの差は ▾ の有無で示す
- 下部タブは**アイコンのみ**（実装済み: `App.jsx` の `BottomNav`。ラベルは aria-label）。
  アイコン形状は **`BottomNav` の実装を正典**とし、モックの `#ic-*` は実装と同形に揃える
  （リード=肩の丸いドーム状の先端・直線のヴァンプ境界・平らな根元。実物写真準拠）
- 主要ボタンは塗り（`--c-accent` 地に白文字）、フィルタピルは輪郭のみ。形言語を分ける

**表記**（実装は N-2）
- 「ピッチ誤差」→**「平均差分」** / 「ばらつき」→**「標準偏差」**
- 「基準」「理想（値）」→**「目安」**（**「基準ピッチ 442Hz」は別概念なので対象外**）
- 日付は **yyyy/mm/dd**（時刻付きは yyyy/mm/dd hh:mm）
- サックス種別は英語表記（Alto / Tenor / Soprano / Baritone）

**§6.1 への予告改訂**（実装は N-4。それまで現行規定が有効）
- 環の**枠（トラックの常設円）・緑の帯（これまでの音の帯）・12時の刻み**は撤去。
  ズレの帯そのものが軌道を示す
- これまでの音は折れ線（枠・面・目盛なし。0¢基準線1本+間引いた音名ラベル）
- メトロノームの開始/停止は**画面タップ**（操作要素の上を除く）。
  テンポの −/＋ は文字サイズ据え置きで**反応領域だけ 72×48**
- 録音は1つのボタンのトグル。アップロードはデータタブへ移設

### 6.1 演奏中サーフェス

**該当**: 環チューナー（ピッチと拍の両方）、これまでの音、音名

| 規則 | 内容 |
|---|---|
| 最小フォント | `--fs-md`（15px）。**12px禁止** |
| 主役 | 音名。サイズは §4.2 の `NOTE_FS_RATIO × 環の直径` |
| 目盛の数字 | **置かない**（-50/+50、±25¢などは全廃） |
| 状態の伝え方 | **色と動きで返す**。文字で説明しない |
| 色 | `pitchBarColorRGB()` の連続補間（**OKLCH**）。閾値のベタ判定を混ぜない |
| 到達の合図 | **12時から両サイドへ走り、6時で出会う**（§6.5）。`ficus-breathe` は環では使わない |
| 判定閾値 | `RING_IN_TUNE_CENTS`（=**2**。F-47 で 1 から変更）を**唯一の定数**とする。その手前の光だけを出す帯は `RING_GLOW_NEAR_CENTS`（=4） |
| 減速設定 | `prefers-reduced-motion` では**装飾の動きを止める**。機能の動き（錘の移動、拍の点の切り替え、ピッチマーカーの追従）は残す。止めるのは拍の演出＝ふくらみだけ |

> **同一サーフェスに2つの設計言語を併存させない。**

#### 環の二役（ピッチと拍）

**環そのもの（トラック r=136 / 線幅14）はピッチ専用**。拍は環の**内側**に描く。
**この分離は例外なく成立していること。**

| 役割 | 位置 | 色 |
|---|---|---|
| ピッチ | 環のトラック上・12時 ± `RING_SWEEP_DEG`（110°） | 機能色（`--c-good/warn/bad` の連続補間） |
| 拍（振り子） | 環の内側・**上半円** | `--c-accent`（紺） |
| 拍（拍の点） | 環の内側・**下半円** | 現在 `--c-accent` / それ以外 `--c-accent-line` |

#### メトロノームの見せ方（E案: 振り子＋拍の点）確定 2026-07-31

**上半円＝振り子が「予測」、下半円＝点が「今が何拍目か」を担う。**
錘が端に近づくのが見えるから身体が次の拍を準備できる（振り子の価値は予測にある）。
機械式メトロノームの錘が支点の上に来るのは複合振り子の機構上の都合だが、
結果としてそれが視覚的な常識になっているので上半円に置く。

値はすべて `viewBox 300×300` の座標。実寸は `× 環の直径/300`（330px環では 1.1 倍）。

| 対象 | 定数 | 値 |
|---|---|---|
| 錘の軌道半径 | `RING_PEND_R` | **94**（要件「6 CSS px」を満たす最大の整数値） |
| 振れ角（12時=0°・時計回り） | `RING_PEND_SWING_DEG` | **±55°**（`deg = 55·cos(π×通算拍)`。拍の瞬間が両端） |
| 軌道の弧 | `RING_PEND_ARC_SW` | 太さ2 / `--c-line` |
| 錘の半径 | `RING_PEND_BOB_R` (+`_GROW`) | 14 + `e`×4 |
| 錘の外側の輪 | `RING_PEND_HALO_GAP` / `_SW` / `_OPACITY` | 半径+7 / 太さ2 / 不透明度 `e`×0.45 |
| 拍の点の半径（配置） | `RING_BEAT_DOT_ORBIT_R` | 112 |
| 拍の点の広がり | `RING_BEAT_DOT_SPREAD_DEG` | 6時 ± 60°（左から右へ数える） |
| 現在以外の点 | `RING_BEAT_DOT_R` | 4 |
| 現在の点（小節頭 / それ以外） | `RING_BEAT_DOT_HEAD_R` / `_CUR_R` | 8 + `e`×3 / 6.5 + `e`×2 |
| 演出量 `e` | `RING_BEAT_EMPH_*` | `max(0, 1 − 拍内位相×2.2) × 係数`（小節頭 1 / それ以外 0.55） |

- **演出は毎拍・両端で出す。** 実際のメトロノームは両端で鳴る。小節頭だけを強調すると、
  偶数拍子では `cos(π×通算拍)` が必ず同じ端に来るため**片側でしか光らない**。
- **点は動かない。灯る場所が変わるだけ。** 点の数は1小節の拍数（X/8 は主拍の数）。
- 「一拍目にアクセントをつける」がOFFのときは小節頭の"差"を出さない（係数は全拍 0.55）。
  鳴っていないものを見せない。ただし拍そのものは毎拍鳴るので演出自体は毎拍出す。

**マーカーの最小距離（実寸）**: ピッチマーカーと拍の要素（錘・点）は、
**環の実寸において最低 6 CSS px 離れること。**
演出が最大のとき、かつピッチが同じ角度に来たとき（最悪ケース）で判定する。
拍の要素は環のトラックの帯（内縁 r=129）にも同じ 6 CSS px 以上入り込まないこと。

> 距離の検証は**必ず CSS ピクセルで行う**。SVG の `viewBox` 単位は実寸ではない
> （`viewBox 300` に対し実寸は環の直径なので、330px環では `× 330/300 = 1.1` 倍になる）。
> 過去に viewBox 単位を px と誤記した記録が3つ食い違ったことがある。

> **錘の軌道半径は「帯の内縁」から決める。**
> かつては先端に丸い点（`RING_PITCH_DOT_R`=10）があり、トラック内縁より 3 内側まで
> 出ていたのでそこが基準だった。**点は廃止した**（§6.5）ので、基準は帯の内縁
> `RING_R − RING_SW/2 = 129`。錘の輪は演出最大時に 錘14+4 + 7 + 線幅の半分1 = 半径+26 まで
> 広がる。実測クリアランスは **6.605 CSS px**（要件 6）。
> 【注意】以前ここを「トラック内縁 129 から 129−14−7=108」と計算して **−8.8 CSS px（＝重なる）**
> を出した記録があるが、それは錘の軌道半径 `RING_PEND_R` を 108 に置いた場合の話。
> 現行は 94。**数字だけを引き写さないこと。**

> **「到達（`inTune`）」で全周を使ってよいのは、走って閉じるときだけ。**
> 常時 `inTune` で全周をピッチ色にベタ塗りすると、下弧＝拍の領域までピッチ色に染まり、
> 上記の役割分離が**最も見せたい瞬間に崩れる**（過去にこれで1度差し戻している）。
> 現行は「±2¢ に入った瞬間に 12時から走って 6時で閉じる」という**出来事**として全周を使う。
> 本人が明示的に許可した例外で、走り切ったあとは帯の色が 12時で最も濃く 6時へ向かって
> 淡くなるため、拍の領域は最も淡い側に来る。**常時ベタ塗りに戻さないこと。**

### 6.1.5 レイアウトの安定（全画面に適用する規則）

> **何かを開いても、閉じても、値が変わっても、既にあった要素は1pxも動かない。**

本人の指摘（2026-08-01）:
> メトロノームにすると円やボタンなどの上下位置がズレる
> メトロノームなしの時の詳細を表示するとメータの上下位置がブレる
> **とにかくどのページでも追加表示で元のある表示がぶれないようにして**

**原因になる書き方（禁止）**

| 禁止 | なぜ |
|---|---|
| 高さが変わるコンテナに `justify-content: center` | 中身が増減するたび**既存要素が動く** |
| 状態で `justify-content` を切り替える | 切り替えた瞬間に全部動く |
| 出現・消滅する要素の**場所を確保しない** | 出た瞬間に下が押し下げられる |

**正しい構造**

```
[ 上端に固定 ]  設定行など、常にあるもの
[ 固定の間隔 ]  --sp スケールの固定値。状態で変えない
[ 主役 ]        環・メーターなど。★ここは絶対に動かさない
[ 可変の中間 ]  flex:1。状態ごとに中身が入れ替わる（履歴グラフ / 操作UI / 空）
[ 下端に固定 ]  録音ボタンなど、常にあるアクション
[ 追加表示 ]    詳細カードなど。ここから下だけがスクロールする
```

**主役は上から固定の距離**に置き、**アクションは下端に固定**する。
増減するものは**その間**に入れ、余りは中間が吸収する。こうすると
「何を開いても主役とアクションは動かない」が構造で保証される。

**値の出し入れも同じ**: 音が入っていないときに文字を消す場合も、
**領域は確保したまま文字だけ消す**（`—` を出す必要はないが、箱は残す）。
文字の有無で高さが変われば、それも「ぶれ」になる。

**検証**: 状態を切り替えて、主役とアクションの `getBoundingClientRect().top` が
**1px も変わらない**ことを実測する。目視で判断しない。

#### 過渡的な告知は「流れから外す」

録音中・解析中・確認ダイアログのような**過渡的な告知**は、`position: fixed` で
**レイアウトの流れから外す**。流れの中に置くと、上端に置けば主役が動き、
中間に置けば余りを食い潰してアクションが動く（どちらも実測で確認済み）。

**座標はルートの `padding` と同じ式を使う**。
```
top:   calc(16px + env(safe-area-inset-top))
left:  calc(14px + env(safe-area-inset-left))
right: calc(14px + env(safe-area-inset-right))
```
別管理にすると、セーフエリアのある端末でコンテンツ列とずれる。

**容器は `pointer-events: none`、カードだけ `auto`。**
全幅の透明な層を敷くと、告知が出ていない領域まで下の操作を殺す。

> **計測タブでモーダルを垂直中央に置くことはできない。**
> 環は `top 147 / bottom 477`、ビューポート中央は `812/2 = 406` で、
> **環が中心点を跨いでいる**。中央に置いた要素は高さが何pxでも必ず中心点を含むため、
> **カードを小さくしても環と重なる**。下寄せにすること
> （下余白は `calc(var(--page-bottom-gap) + var(--sp-4))`）。

**「押しても何も起きない」を作らない。**
告知が下の操作を覆う場合、覆われた側は**無反応ではなく `disabled` にする**。
無反応は「アプリが固まった」と解釈される最悪のフィードバック。

### 6.2 合間サーフェス

**該当**: 詳細分析、リード管理、データ、設定

| 規則 | 内容 |
|---|---|
| フォント | 7段スケール全部使ってよい |
| 情報密度 | 上げてよい |
| ただし | **密度を上げるほど階層をサイズか位置で表現すること** |

> 「情報密度を上げてよい」は「整理しなくてよい」ではない。
> 現状、箱→個体の2階層で**子のほうが5px大きく専用書体**という反転が起きている。
> 主役級の数値が説明文の末尾に句読点区切りで埋もれている箇所もある。

### 6.3 横スワイプの作法

**この作法はアプリ内で1つだけ**。「指に追従する」以外の横スワイプを作らない。閾値だけを見て指を離すまで何も動かないスワイプは、動いていないのか効いていないのか奏者に区別がつかない。

| 定数 | 値 | 意味 |
|---|---|---|
| しきい値 | 動かす面の幅の **20%**（幅が測れなければ 60px） | 「動かす面」は viewport の `clientWidth`。画面幅ではない。375px 端末の詳細画面では 347 × 0.2 = **69.4px** |
| 軸ロック距離 | **6px** | 縦横どちらのジェスチャーかを決め始める移動量 |
| `SWIPE_VERTICAL_BIAS` | **1.5** | **縦と断定するには縦成分が横成分の 1.5 倍以上必要**。境界は `atan(1.5) = 56.31°` |
| 行き先の無い向きの抵抗 | **0.35** | 動くが遷移しない |
| 戻すときの動き | `transform 0.32s cubic-bezier(.22,.61,.36,1)` / 後始末は **320ms** | この2つは必ず同じ値。片方だけ変えない |

#### 軸を「勝つまで決めない」

横確定 = `|dx| >= 6 && |dx| > |dy|` ／ 縦確定 = `|dy| >= 6 && |dy| >= |dx| * 1.5` ／ **それ以外は未確定のまま観察を続ける**。

親指のスワイプは根元を支点に弧を描くので、最初の6pxは縦成分が勝ちがちで、**画面下ほど弧が急になる**。一度きりの `|dx| > |dy|` で決めると、横スワイプが一度の揺れで縦に固定される。CDP 実測で、この規則の導入前は**水平0°の純粋な横ドラッグですら20本中1本も通らなかった**（20px 進んだ時点で `pointercancel`）。導入後は 20/20。

1.5 が実際に救うのは **42〜44° の帯**（丸めのゆらぎで一瞬 `|dy| >= |dx|` になるところを断定せずに拾い直す）。46〜70° の弧は 1.0 でも 1.5 でも成立しない。45〜56.31° の直線ドラッグは未確定のまま終わる（この設計の帰結。BACKLOG F-20）。

#### ブラウザにスクロールを横取りさせない

**非パッシブの `touchmove` を張り、横と確定している間だけ `preventDefault()` を呼ぶ。** Pointer Events だけではブラウザのスクロール開始を止められず、斜めに引くと `pointercancel` が飛んでジェスチャーごと死ぬ。

**`touch-action` は使わない。** 祖先の指定が子孫より優先されるため、`pan-y` を敷くと中にある横スクロール表が動かせなくなる。

**未確定のうちは `preventDefault` してはいけない。** iOS は最初の `touchmove` で `preventDefault` されるとそのジェスチャー全体をスクロールしなくなるので、縦スクロールが死ぬ。

#### 動かす面の構造

`overflow: hidden` の viewport が track を包み、**動かすのは track だけ**。包まないとドラッグ量ぶんページが横に伸び、ページ全体が横スクロールできてしまう（実測 `scrollWidth 375 → 422`）。

**静止時に `transform` を残さない。** identity（`translateX(0px)`）でも、`transform` がある要素は `position: fixed` の子孫の包含ブロックになり、モーダルの暗幕が画面全体を覆えなくなる。同じ理由で **`will-change: transform` も使わない**。

**ジェスチャーの終わり方は3つ**: `up`（行き先判定あり）／`cancel`（戻すだけ）／**別の `down` による中断**。中断の終端は**対象判定より前**に置く。「対象外かどうか」は新しく始めるかの判断であって、前のを終わらせるかの判断ではない。これを取り違えると、2本目の指が触れた瞬間に進行中のドラッグが後始末なしに捨てられ、**画面がずれたまま無期限に固着する**（実測で最大200px）。

---

### 6.4 主観評価の刻みと入力

| 項目 | 範囲 | 刻み | 表示 |
|---|---|---|---|
| 総評 | 1.0〜5.0（41段） | **0.1** | 常に小数第1位（`3.7` / `3.0`） |
| 厚さ | 1〜5 | 1 | 整数 |
| バランス | 1〜5 | 1 | 整数 |

- **3つは横一列**に並べ、行のどこをタップしても**1つのダイアログで3つ同時にダイヤルが出る**（生年月日ピッカーと同じ作法）。行の高さは `--tap-min` 以上。
- ダイヤルは**上が5、下が1**。窓は **5行**（44px × 5 = 220px）。中央合わせなので、5段すべてが見えるのは選択が中央のときだけ。
- **記録するのは1つでも変わったときだけ1件。** 何も変えずに閉じたら0件。
- **比較する前に必ず丸める。** 0.1 刻みは2進小数で正確に表せないので、生の `===` で「変更あり」を判定すると**開いただけで記録される**。丸めは「刻みに乗せる」→「小数第1位に落とす」の二段（`Math.round(n/0.1)*0.1` → `Math.round(r*10)/10`）。二段目は必須（外すとダイヤル41段のうち15点が `1.2000000000000002` のような値で保存され、位置が引けなくなる）。
- 評価の推移グラフの縦軸は **1〜5 のまま**。総評だけが小数を取る。

### 6.5 チューナーの環の配色と到達

#### 機能色（意味を持つ。装飾都合で変えない）

| セント差 | 色 | |
|---|---|---|
| 0¢ | `#16A34A` | 緑 |
| **8¢** | `#D97706` | 橙。実用域が ±10¢ なので、その中で緑→橙を使い切れる位置に置く |
| 30¢以上 | `#DC2626` | 赤 |

**補間は必ず OKLCH。** sRGB で直線補間すると中間で彩度が落ちて濁る。緑→橙の中間（4¢）で
sRGB は `rgb(120,141,40)` の濁ったカーキ、**OKLCH は `rgb(160,145,0)` の澄んだ金**。
色そのものは1つも変えていないのに見え方が別物になる。**ここが「安っぽい」の主因だった。**

#### 帯（ズレの表示）

| 値 | |
|---|---|
| `RING_RAMP_REF = 62` | 先端から数えて色を作り込む弧長（viewBox単位 ≒ 11.9¢） |
| `RING_RAMP_STOPS = 30` | ストップ数 |
| 明度差 | `0.105e − 0.062(1−e)` |
| 彩度倍率 | `1.12 − 0.30e` |
| 色相ずれ | `−9e + 7(1−e)` 度 |

`e = smoothstep(clamp(s / RING_RAMP_REF, 0, 1))`、`s` は**先端からの弧長**。

- **帯の長さで正規化してはいけない。** ±24¢ の帯は弧長125px、±8¢ は42px。同じ工程を押し込むと
  3分の1に圧縮されて**断層として見える**。実用域は ±10¢ なので、そこで成立しないと意味がない。
- **3要素すべて単調。明るさの山（芯）を置かない。** 単調なら断層は原理的に出ない。
  実画素で確認済み: ±8¢ の帯の隣接画素の最大差 **3/255**、RGB 各成分が単調。
- **不透明度は常に 1。** `stop-opacity` を1つも書かない。根元が透けると下地が出る。
- **ストップ位置は弧を弦へ射影して決める。** グラデーションの軸は弦なので、等間隔に置くと
  色の位置がずれる。検査は「各ストップの点と弦上の offset 位置の差が**弦に垂直**」を内積で見る
  （等間隔を 1% 混ぜただけで落ちる）。
- **`stroke-linecap` は `butt`。** 丸端は終点から `RING_SW/2 = 7` 外へ張り出し、
  (a) 0¢ の基準線を越えてはみ出す (b) 張り出した半円が最終色でベタ塗りされ
  **「別の丸いものが付いている」ように見える**。
- **先端に点を置かない。** 先端はグラデーションの終わりであって、付属物ではない。

#### 到達（±2¢）と、その手前の光（±4¢）　※F-47 で更新

**判定線は `RING_IN_TUNE_CENTS = 2`**（本人指示 2026-08-08。以前は 1）。

| 値 | |
|---|---|
| `RING_IN_TUNE_CENTS = 2` | これ以内なら「合った」。走りが出る |
| `RING_GLOW_NEAR_CENTS = 4` | ここまでは走らせず**光だけ**を出す |
| `RING_RUN_MS = 640` | 12時から6時へ走り切るまで |
| イージング | ease-out cubic `p => 1 − (1−p)³` |
| `RING_BREATH_MS = 2600` | 呼吸の周期 |
| `RING_BREATH_RISE = 0.50` | 周期のうち上りに使う割合 |
| `RING_GLOW_AMP = 0.90` | 光の最大の強さ |
| `RING_RUN_REARM_MS = 1200` | 外れた状態がこれだけ続くまで走り直さない |

**段の切り分け**（`ringGlowOpacity` に閉じ込める。rAF 側に条件分岐を置かない）:

| ズレ | 走り | 光 |
|---|---|---|
| ±2 以内 | 出る | 出る（呼吸つき） |
| ±2〜±4 | **出さない** | 出る。±4 に近づくほど連続的に 0 へ |
| ±4 超 | 出さない | **0** |

- 走りは**左右同時・完全対称**（実測の角度差 5.68e-14）。終端は **±180.0000°**。
- 光は**環の外側だけ**。内側は完全に白のまま。**音名の可読性と静けさのため、これは要件。**
- 光の色は `tune(base, dL +0.16, 彩度 0.62倍)`。**「光源の色」ではなく「照らされた面の色」**にする。
- 呼吸は正弦波ではなく **smoothstep を上り下りに分けたもの**。
- 実効の不透明度 = `AMP × 走りの線形進捗 × (0.34 + 0.66 × 呼吸)`。走りに合わせて光が立ち上がる。
- **減速設定では走らず点灯状態から始め、呼吸も止める。ただし光は消さない。** 到達は伝える。

> **判定線の色はベタ判定しない。** `pitchBarColorRGB` は連続補間なので、±2 ちょうどで閉じる円は
> 色相 76°の黄緑 `rgb(113,156,0)`（0¢ は 142°の緑 `rgb(22,163,74)`）。
> 本人指示は「±2以内で**緑円**判定」だが、閾値で色を切り替える実装にはしていない
> （§1.5 の機能色を閾値で階段状にすると意味体系が壊れる）。**「これで緑と感じるか」は実機の判断。**

##### 外周の光の作り方（F-47）

環の縁から間接照明のように漏れる光。**実装の要点は `src/App.jsx` の `ringGlowAlphaAt` 直上の
解説を読むこと。** ここには設計上の約束だけを書く。

- **光の最大は環の外縁に置く。** 立ち上がりを外縁より外まで続けると、環と光の間の数pxが
  淡いまま残り**細い明るい線**が見える（試作で本人が指摘した不具合）。
- **終端で値も傾きも0にする。** `exp(-decay·x)·(1-x)³` は x=1 で値も微分も 0。
  ここが折れると「グラデーションの終わり」が縁として見える。
- **`mix-blend-mode` を使わない。** 試作の初版は粒をマスクの中で `multiply` で重ねており、
  その指定が効かない環境で**画面全体が緑になった**（本人報告）。マスクの入れ子（=掛け算）だけで作る。
  減衰マスクを常に最外に置けば、どんな事情があっても環の外側へ漏れない。
- **毎フレーム変えるのは明るさだけ。** ストップも粒も静的に1度だけ作る。
- 光は viewBox の外まで届くので `overflow:visible` が要る（r=150 で peak の 42.6% が残っており、
  切ると四角い縁が出る）。同時に `pointerEvents:"none"` が**必須**（無いと光の矩形が
  下の操作の当たり判定を奪う）。**この2つは対で、片方だけでは成立しない。**

> **粒（`feTurbulence`）は入っているが、画面には出ていない。** 本人が「粒の表現が要るかも」と
> 要望した経緯で機構は残してあるが、実測では寄与が **0.972/255**（本人選定の試作も 0.983/255 で
> 事実上同じ）。**`RING_GLOW_GRAIN_LO` / `RING_GLOW_RAMP_POW` を動かしても点かない**
> （grainLo 0.30→0.00 で 0.972→0.972）。効かせるには `falloff × ramp` の掛け算の構造を変える必要がある。
> **「外へ行くほど粒に分解して散る」と書かないこと。**

#### 再走行の抑制

判定線の上でピッチが揺れると走りが何度も起きる。**一度走ったら、外れた状態が
`RING_RUN_REARM_MS` 続くまで走り直さない。** 抑制中に戻った場合は走りをやり直さず
点灯状態（進捗1）から始める。

> **1200 の根拠を取り違えないこと。** 実コンポーネントを 60fps で駆動して `inTune` が偽である
> 連続時間を測ると、判定線の上で揺らした場合は **233〜650ms**（3¢/1.5Hz で 233〜267ms、
> 2¢/0.5Hz で 633〜650ms、1.2¢/3Hz では EMA が吸収して一度も外れない）。
> 900ms を超えるのは **+15¢ に約650ms 保持してから戻す**ような場合で、これは「揺れ」ではない。
> 1200 を選んだのは、生の外れが 800ms までは走らず 1000ms 以上で走る（実効しきい値は生で
> 約930ms）という掃引の結果、**「本当に外して戻った」ときは走る**という性質を保てるため。

### 6.6 面の作法（地が白になったので、まとめ方をタブごとに決める）

地を白にすると、**カードを「地より明るい面」として浮かせられなくなる**。旧来は
地 `#F6F7F9` / カード `#FFFFFF` の2段で、その差（ΔL\* 2.79）が全画面のまとまりを支えていた。
白にすると差が消えるので、**まとまりを別の手段で作り直す**必要がある。

| タブ | 作法 | クラス |
|---|---|---|
| 計測・リード・**データ** | **罫** | `.surf-rule` |

### 2026/08/23 本人裁定: 作法は「罫」の1つだけになった（D-7）

以前はデータタブだけ「沈める（`.surf-sunk`）」で、理由は
「ピボット表など密度が高く、群の境界を罫だけでは示せない」だった。**この判断は取り消した。**

> **本人指示**: 「mydata と カレンダーがそれぞれ薄いグレーのカードになっているのを変更。
> これは整理されていないからカード化する必要に陥っているだけで、整理できていればカード化は
> 不要なはず。**背景色を多用すると画面全体が重く見えるのでやめて。**
> 計測タブもリードタブも背景色使われてないでしょ」

つまり「密度が高いから箱が要る」ではなく、**箱が要るのは整理できていない証拠**という読み。
実装は `.surf-sunk` を名乗る要素が**データタブの根に1つ**あるだけだったので、
そこを `.surf-rule` に変えるだけで My Data / カレンダー / PIVOT / セッション詳細の
4画面が一斉に白地＋罫へ移った。使い手がゼロになった `.surf-sunk .card` /
`.surf-sunk .tile` は**定義ごと削除**した（読み手ゼロの規則を残さない）。

**移すときに壊れたもの（記録）**: 罫の作法では `.card` の左右 `padding` が **0** になる。
カレンダーの grid はカードの `padding` を左右へ食い破って端まで伸びていたので、
そのままだと地の外へ 16px ずつはみ出して**横スクロールが出た**。食い破りは外した
（地の端＝カードの端になったので、そもそも食い破る必要が無い）。

#### 罫（`.surf-rule`）

| | |
|---|---|
| `.card` | 塗り無し・枠無し・**上辺に `--c-rule` の罫1本**・角丸 0・左右 padding 0 |
| `.tile` | 塗り無し・枠無し・**左辺に `--c-rule` の罫1本**・角丸 0 |

タイルの罫は**行の先頭には出さない**（列の左端に罫が出ると囲みに見える）。

> **`:nth-child` で行頭を判定してはいけない。** `TappableMetricCard` は開くと
> `gridColumn: 1/-1` で全幅になり、以降のタイルの行内位置がずれる。実測で、2枚目を
> 全幅にすると行頭が3つになり `:nth-child(2n+1)` は破綻する。
> **正しいのは位置で決める方法**: 罫をタイルの左端に置き（`margin-left:-1px` で自分の枠の外へ出す）、
> 行の左端に来た1本だけを容器の `overflow:hidden` に落とさせる。容器を左右へ `--sp-3`
> はみ出させてタイルの padding と相殺すると、1列目の中身がカードの左端に揃う。
> **列数・全幅タイル・折り返しのどれにも依存しない。**

#### 沈める（`.surf-sunk`）── **2026/08/23 に廃止（D-7）**

`.card` に `--c-sunk` の塗り・透明の枠・角丸 `--r-md` を与え、その上の `.tile` を白で浮かせる、
という作法だった。データタブが罫へ移って**使い手がゼロ**になったので、CSS ごと削除した。

**復活させないこと。** 面で群を作りたくなったら、まず「整理できていないのでは」を疑う
（本人「整理されていないからカード化する必要に陥っているだけ」）。

#### 作法に共通

- **操作するもの（入力欄・ボタン）の見た目は §6.7 が決める。** 面の作法（罫／沈める）は
  「群のまとめ方」だけを決め、操作するものには触らない。**§6.7 を見ること。**

- **個々の要素が `background` / `border` / `borderRadius` / `padding` を
  インラインで書いてはいけない。** インライン style は CSS より強いので、書いた要素だけ
  作法が効かなくなる。これが**この設計でいちばん壊れやすい点**なので、
  `scripts/pitch-test.mjs` が接頭辞で検査している（`backgroundColor` のような
  キャメルケースの別綴りで一度すり抜けた実績がある）。
  **`gap` は `.tile-row` だけが作法の管轄**（罫の作法では 0 にして罫で分ける）。
  `.card` の中身の並びは各画面の裁量なので、`.card` に `gap` を書くのは可。
- **`index.css` の末尾に同じセレクタで上書きを追記してはいけない。** CSS は後勝ちなので
  作法が丸ごと反転する。これも検査で縛っている。

- **特定1〜2箇所だけ作法の一部（例: 上辺の罫）を消したい場合は、`.card` を含む複合セレクタ
  ではなく、`.card` という文字列を一切含まない別名クラスを使うこと。** `.card.foo` のような
  複合セレクタは検査の `targetsClass("card")` 正規表現に「`.card` に触れている」と
  誤検出される。別名クラス（例: `.no-top-rule`）を、上書きしたい規則と**同じ詳細度**で
  その直後に置けば、CSSの宣言単位カスケード（プロパティごとに後勝ち）で狙った1プロパティ
  だけが上書きされ、他は元の作法のまま生きる。乱用しないこと（本人が明示した箇所だけに使う。
  リード登録カード／リード個別詳細の識別情報カードの上辺の罫、2026-08-04）。

### 6.7 枠線は「状態を持つもの」にだけ使う

**同じ物に「枠線」と「違う地の色」を両方与えない。** どちらか一方で足りるものに両方やると重くなる。

| 型 | クラス | 見た目 | 対象 |
|---|---|---|---|
| **A型** | `.ctl-state` | 枠線 `--c-line-strong` / 地は透明。**ON は枠線の色だけ** `--c-accent` に変える | **状態を持つもの**（on/off・開/閉・選択中/非選択） |
| **B型** | `.ctl-plain` | **枠線なし** / 地は `--c-sunken` | **状態を持たないもの**（入力欄・選択欄・普通のボタン） |

> **これは読み手への約束になる。「枠線があるものは状態を持っている」。**
> メトロノームに枠線を残す理由（on/off が分かるべき）が、そのまま全体の規則になった。

| 対象 | 型 |
|---|---|
| メトロノーム / 拍子 / 1拍の分割 / 詳細トグル | **A** |
| 入力欄（`input` / `select` / `textarea`）全般 | **B** |
| リード登録の 銘柄・番手・使用開始日 | **B** |
| データタブの絞り込み・奏者選択・リード選択ピル | **B** |
| 状態を持たないボタン（追加・キャンセル・クリア等） | **B** |

**ON の合図に地を足さないこと。** 枠線の色だけで返す。地を足すと A型が「枠線＋違う地」になり、
規則そのものを破る。

#### 破壊的な一手だけは塗ってよい

**実際に削除が実行されるボタン**は `--c-danger` の塗り + `--c-on-accent` の文字（**4.83:1**）。
枠を持たないので芯に反しない。地を `--c-sunken` にすると文字が **4.26:1** で AA(4.5:1) を
下回るため、この一手だけ「塗りの強調ボタン」と同じ扱いを許す。
**「削除モードに入る入口」は塗らない**（破壊がまだ起きないので中立の `--c-ink-2`）。

#### 意図した例外（潰さないこと。**全部で6グループ**）

1. `select.pivot-axis-select`（データタブの軸選択・3個）— 表の見出しの中なので枠が表の罫と喧嘩する
2. 計測タブのリード選択ピルの中の `<select>` 2個 — ピル自身が地を持つので中は素のまま
3. **START/STOP** — 状態を持つが、ON は `--c-danger` の枠、OFF は `--c-accent` の塗り。
   A型に流すと**意味色が状態色に化けて §1.5 が崩れる**。実測でどちらの分岐でも
   「見える枠」と「違う地」は同時に立たないので**芯には反していない**。
4. **子タブのセグメンテッドコントロール** — 溝の中で選択中だけが白く浮く。
   枠線を持たないので芯には反しないが、状態を**地**で返すので A型ではない。
5. **塗りの強調ボタン**（録音する / まとめて追加 / 登録 / 完了 / 保存 / 追加 / 変更）—
   枠なし・地だけで芯を満たす。地が `--c-sunken` ではなく `--c-accent`。
6. **通知面**（警告・アップロード告知・`ScrollPicker` の選択帯）— 枠と地を両方持つ。
   **群でも操作でもない第3の種別**で、A型/B型のどちらにも属さない（BACKLOG F-33）。
   タップで消せるものは操作にも見えるが、**線は「操作するものか」ではなく
   「情報を伝える面か」で引く**。検査から明示的に除外してあり、黙って外れているのではない。
   **マイク許可エラーはこの群から抜けた**（2026-08-04）。放置せず必ず閉じるべき操作なので、
   `pendingSession` の保存確認と同じ「下寄せの暗幕モーダル」（§6.1.5 の確認ダイアログ）に
   変更した。現時点で `data-frame-exempt` は **0件**（この群自体は将来の警告UIのために残す）。

> **例外は必ずここに書くこと。** 検査が 0 FAIL を返すだけでは、次の実装役に
> 「例外なのか未対応なのか」を判別する手段がない。

#### 枠を透明にして残す（`border: 1px solid transparent`）

B型で `border: 0` にすると **1px × 2 ぶん要素が縮み、祖先を通って環が 2px 上がる**（実測）。
場所だけ残して線を見せない。§6.1.5 のため。

#### iOS Safari の日付欄（`input[type=date]` / `datetime-local`）は箱を自力で決めない

**症状（本人報告・実機のみ）**: 親より横に広くなり、`SwipePager` の track 上で
**隣のページに重なって見える**。`width: 100%` / `minWidth: 0` / `boxSizing: border-box` を
全部与えても直らなかった。

**効いた対処（実機で確認済み・2026-08-04）**: `-webkit-appearance: none` + `appearance: none`
+ `maxWidth: 100%`。これで本人の端末で**横幅は直った**。タップでネイティブの日付ピッカーが
開く挙動はそのまま残る。

> **ただし「なぜ効いたか」は確定していない。** 有力な説は2つあり、どちらも一次情報で
> 裏が取れていない: (a) WebKit の UA スタイルが temporal input を `-webkit-inline-flex` で
> 描き、**その `display` のときに `width:100%` を無視する**（iOS 18 以降は修正済みという
> 情報もある）、(b) 内部UIの固有幅が `min-width: auto` として効く。**`appearance` は
> `display` を変えないので、(a) が真因なら効いた理由は別にある。**
> 断定して規範化しないこと（1周目に「実測: 親160pxで+16px溢れ」を根拠に規範化して外し、
> ここに至るまで3周かかった。BACKLOG F-39）。

**`appearance` を落としたら、縦も自分で決めること。** 幅と一緒に**縦方向の固有の寸法
（高さ・内部テキストの縦位置）も失われる**。実際、横幅を直した次の周に
「使用開始日だけ縦幅が変わった」と本人報告が来た。セットで必要なもの:

- `height` を**固定**する（`minHeight` は下限でしかなく、ネイティブ描画のままの `select` が
  それを上回ると、`appearance` を落とした欄だけが低くなって並びが揃わない）
- `lineHeight` を明示する（内部テキストが上寄せに落ちるのを防ぐ）
- `overflow: hidden`（内部UIがはみ出した場合の最後の歯止め）

> **Chrome では、この症状は横も縦も最初から再現しない。** Browser pane の実測は必ず
> 「揃っている」と出る。**この欄を触ったときの Chrome の実測は判定に使えない**ので、
> 必ず実機（iOS Safari）で確認すること。
>
> グリッド項目に置く場合は `minWidth: 0` も併せて付ける（グリッドの `min-width: auto` を
> 解くため。ただし**これ単独では iOS の問題は直らない**ことが実機で分かっている）。
> Chrome 実測の参考値: 最小内容幅 150px、親160pxで既定は +16px 溢れ、`minWidth: 0` で 0。
>
> **既知の未適用箇所**: 個別セッション詳細の `datetime-label`（`datetime-local`、幅を
> `190px` 固定で「理想値に設定」と同じ行に置いている）はこの作法をまだ適用していない。
> 本人からの報告が無く実機で確かめられないため意図的に触っていない（BACKLOG F-40）。

---

## 7. 「育てる」の作法

### 大前提（ここを取り違えない）

> **奏者が興味を持っているのは「今の自分の音」。**
> どれくらい練習したかの累計は、**サブ的に見れたら嬉しい**程度のもの。

したがって「育てる」は**主役を張らない**。画面の主役は常に今の音・今回の結果であり、
蓄積はその脇に、しかし確実に見える場所に置く。

DESIGN.md §1 の「蓄積が目に見えて育つこと」は、**蓄積を最大要素にせよという意味ではない**。
現状は蓄積が「見えない」ことが問題であって、「小さい」ことが問題なのではない。

### やること

- **主役は今の音・今回の結果**。累計はその脇に副次的に置く
- ただし累計は**確実に見える**こと（現在は累計指標がアプリ内に1つも存在しない）
- 空状態は「これから育つ」の語り口にする
  - 基準にする既存文言: **「録音するとここに折れ線が育ちます」**（`App.jsx:6556`）
- 蓄積を**形**で見せる（日ごとの縞、線が伸びる、記録が積み上がる）
- リードは消耗品。**開封後日数と使用量**が最も「育つ／老いる」対象

### やらないこと

- **累計を画面の主役にすること**（奏者が見たいのは今の音）
- バッジ・紙吹雪・レベルアップ演出
- **断定的な評価ラベル**（「平均より悪化」など）
- **開いた瞬間に赤で迎えること**
- 数値の自動フルスケール描画（±0.3¢のブレを画面いっぱいの起伏に見せない）

> DESIGN.md §1: 「観葉植物は静かに育つ」「和の間」。
> データタブを「反省する場所」から「見に行きたい場所」に変えるのが目的であって、
> 反省を強化するのが目的ではない。

---

## 8. 移行の順序

`BACKLOG.md` の P2 セクションはこの順で消化する。

| 順 | 内容 | 根拠 |
|---|---|---|
| 1 | 見た目が変わらない変更（P2-1） | 検証コストゼロ。死蔵フォント削除・aria・safe-area定数 |
| 2 | **サイズスケール**（P2-2） | ユニーク15値。色（44値）より桁違いに安全。§2の12px課題に直結 |
| 3 | 判別不能な罫線8箇所（P2-3） | **スクショ差分が出ないこと**が合格条件という明確な基準が立つ |
| 4 | 画面単位のトークン化（P2-5〜） | 計測タブで確立したやり方を各画面に適用 |
| 5 | 角丸・タップ領域（P2-4 / P2-6） | 各画面に入るたびその画面分だけ回収 |

**色は一括置換しない。** 画面ごとに、その画面で使う分だけ。
例外は上記3（8箇所のみ・見た目が変わらないことが検証条件）。

# BACKLOG（現役）

過去の完了記録と北極星移行前の起票は `design/BACKLOG-archive.md` へ分離した(2026/08/16 整理)。
**エージェントが毎周読むのはこのファイルだけでよい。**

---

## 罠の目録（毎周これだけは読む。全て実際に起きた事故の蒸留）

1. **検査名は実際に検証している内容しか名乗らない**(言い過ぎで通算17回差し戻し)。十分条件でないなら「十分条件ではない」と書く。
2. **純関数を守っても配線が無防備なら意味がない。** 描画側が正しい引数で呼ぶことまで固定する。錨は**呼び出しに隣接する綴り**(「コンポーネント内のどこかにあれば通る」は張りぼて)。
3. **期待値は正典(design/*.html)か実装の外から読む。** 定数の定義を同じ式・同じ定数で書き直した検査は恒真。
4. **箇所数の釘付け禁止**(正しい修正を落とす)。縛るなら集合で。件数固定が許されるのは、その件数が不変条件そのものであるときだけ。
5. **明文で禁じるだけの規則は破られても誰も気付かない。** 検査を足すか、構造的に不可能にする(例: .taptext の overflow: visible !important)。
6. **画面に出る文字は画面から読んで確かめる**(幅や桁を測っても文言の誤りは出ない。「スペクトル重心」事故)。
7. **測っていない数値を書かない。** 他人の測定値は測り方ごと引用。**別オリジン比較は面積・形・相対距離だけ**(座標は保存状態で16pxずれた)。
8. **§6.3 ジェスチャー: 終わり方の数え落としで4回固着。** 中断の終端は対象判定より前。touchcancel は中断(指を離した意思表示ではない)。下端シートは上向きを受け付けない。
9. **codeOf() は accept="audio/*" の /* をコメントと誤認して数百行消す**(JSX に使用禁止)。codeOf 済み文字列への indexOf("//…") は常に -1。pitch-test は JSX を評価しない(描画が壊れても緑になり得る)。
10. **Browser pane**: rAF 不発火・トランジション停止 → 判定は inline style。elementFromPoint は整数1px刻み(x.5 で44→43に誤測)。マイク拒否の覆いが奪う → 計測中だけ隠して必ず戻す。**HMR 失敗で古いコードを測る事故** → served コードの綴りを確認してから測る。
11. **変異試験は単独逐次**(F-94: 並列は速度検査が偽 KILL を生み、campaign 全滅が2回)。複製は一意名で `src scripts design index.html package.json` を含め、終わったら必ず削除。
12. **IndexedDB はキー単位復元・clear()/deleteDatabase 禁止。** プロファイルごとに基準が違う(着手時スナップショットを取る)。エージェントは git commit/push 禁止。
13. **Python open(p,"w") は CRLF 化して git 差分に出ず検査12件が落ちた。** LF を維持する。
14. **UTC の暦日は JST 00〜09時で前日にずれる**(開封日で箱が割れた)。暦日は localDayKey。
15. **ネイティブ部品の実寸・<label>活性化・セーフエリア・実指の感触は Chrome で判定不能。** 「実機待ち」と明記し、合格根拠にしない。

---

## 現役の起票

### [ ] F-69 環の粒(feTurbulence)が画面に出ていない
- 場所: `src/App.jsx` の `RING_GLOW_GRAIN_LO` / `RING_GLOW_RAMP_POW` と、外周の光のマスク入れ子
- 本人は「均一なグラデーションではいけない、**粒の表現**が要るかも」と要望し、粒入りの試作から案③を選定した。しかし**実測では粒が画面に出ていない**: 粒を1に固定した場合との最大画素差は本体 **0.972/255**、本人選定の試作も **0.983/255**(差 0.011)。8bit の量子化より下。
- **移植の欠陥ではない**（試作そのものが最初から粒を出していない）。今見えている質感は減衰カーブが作っている。
- **定数を動かしても点かない**（実測）: `grainLo` 0.30→0.00 で寄与 0.972→**0.972** で不変。`rampPow` 1.8→0.6 で 0.958、0.2 で 2.068（なお不可視、かつ光そのものが明るくなる）。効かせるには `falloff × ramp` の掛け算の**構造**を変える必要がある（例: 粒を外側の傾斜だけでなく減衰マスク側へ掛ける）。
- **着手の条件**: 本人が実機で見て「もっとざらつかせたい」と言ったときだけ。言われなければ現状の見た目で問題ない。
- 影響度: 小（本人の要望次第） / コスト: 中（構造変更）

### [ ] F-70 `sourceKind` が書き込まれるだけで誰も読んでいない
- 場所: `src/App.jsx:3024` で書き込み / `:7646` で生成 / **読み出し 0箇所**
- F-67 で理想値プロファイルに由来を記録した際、`sourceSessionIds`（「理想値設定中」の判定に使用中）と一緒に `sourceKind`（"session" / "performer"）も入れたが、こちらは誰も読んでいない。
- 永続データに未使用フィールドが増えているだけで害は無いが、**由来の表示に使う**（例: 「この奏者の平均から作成」と出す）か、**落とす**かを決める。
- 影響度: 小 / コスト: 小

### [ ] F-71 環の光の検査に、作為的だが塞げる穴が1つ残っている
- 場所: `scripts/pitch-test.mjs` の「光を出す SVG は overflow:visible かつ当たり判定を持たない」
- 現在は `PitchRing` 全体に対して `/overflow: "visible", pointerEvents: "none"/` を当てている。現実の退行（片方を消す・両方を消す）はすべて捕まる（審査役が N1/N2/N3 で確認済み）。
- 残る穴（**N5**）: svg から両方消し、**同じ綴りを同じ順序で PitchRing 内の別要素へ移す**と通る（実測 PASS 5212 / FAIL 0）。作為的な変異なので優先度は低い。
- 塞ぎ方: 正規表現の当て先を `PitchRing` 全体ではなく **`<svg` の開きタグ**に絞る。
- 影響度: 小 / コスト: 小

### [ ] F-40 `input[type=datetime-local]`（個別セッション詳細の日付）も iOS Safari で幅が効かない可能性
- 場所: `src/App.jsx` の `SessionDetailView`、日付と「理想値に設定」を同じ行に置いている `input[type="datetime-local"]`
- 現状: F-39 で `input[type=date]`（リード登録の使用開始日）が **`-webkit-appearance` がネイティブのままだと iOS Safari で CSS の `width` を無視する**ことが判明した。`datetime-local` も同じネイティブ日付コントロール系なので同じ問題を持つ可能性が高い。この欄は `width:190` を明示指定して「理想値に設定」ボタンと同じ行に収めている（Chrome 実測では収まる）が、iOS で幅が効かなければ**行が折り返して2段になる**（`flexWrap:"wrap"` を入れてあるので画面外へのはみ出しにはならない）。
- **本人からの報告は無い**（気づいていないだけかもしれないし、実機では問題ないのかもしれない）。憶測で触ると新しい見た目の変化を実機に持ち込むことになるため、F-39 では意図的に触っていない。
- 完了条件: 実機（iOS Safari）で個別セッション詳細を開き、日付と「理想値に設定」が同じ行に収まっているか確認する。折り返していれば `WebkitAppearance:"none"` + `appearance:"none"` を同様に当てる。
- 影響度: 小 / コスト: 小

### [ ] N-5b PIVOT の日付フィルタが常に1日前を表示する（既存バグ。N-5 の調査で発見）
- 場所: `src/App.jsx` の PIVOT の日付範囲 `input[type=date]`（`rangeMin` / `rangeMax` の2箇所）
  ```js
  value={flt.rangeMin ? new Date(flt.rangeMin).toISOString().slice(0, 10) : ""}
  onChange={(e) => updateFilter({ rangeMin: ... .setHours(0, 0, 0, 0) })}
  ```
- **保存は `setHours(0,0,0,0)` でローカル深夜、表示は `toISOString()` で UTC 暦日**。
  JST ではローカル深夜 = 前日15:00 UTC なので、**時間帯に関係なく常に1日前**が表示される。
  統括の実測（`TZ=Asia/Tokyo`）: 8/14 を選ぶ → 保存 `Fri Aug 14 2026 00:00:00 GMT+0900` → 再表示 **`2026-08-13`**
- **絞り込みの結果そのものはずれない**（保存値は正しいローカル深夜）。ずれるのは**入力欄の再表示だけ**だが、
  開き直すたびに1日前になるので、触るたびに範囲が過去へずれていく可能性がある（未確認）
- 直し方: 表示側を `localDayKey`（`src/App.jsx:1301`。N-5 で新設。`getFullYear/getMonth/getDate` 版）に替える
- **`src/App.jsx:1292` が「`new Date().toISOString().slice(0, 10)` を使ってはいけない」と名指しで
  禁じている**のに残っている最後の2箇所。N-5 では担当外（PIVOT は N-6 の範囲）として触っていない
- 影響度: 中 / コスト: 小

### [ ] F-4 物理ミュートのマイクで復旧が永久に回り続ける
- 場所: `src/App.jsx` の `recoverMic` / 無音ウォッチドッグ
- 現状（**F-2の周の審査で指摘**）: ウォッチドッグは「全ゼロが1.5秒続いたら壊れている」と判定するが、**外部マイクの物理ミュートスイッチ等でハード的に無音の場合と区別できない**（Webの API に判別手段が無い）。結果、物理ミュート中は **5秒ごとに `getUserMedia` を呼び直し続ける**。端末のマイク使用インジケータが5秒ごとに点滅し、`errorMsg` も出続ける。
- 方向性: **段階的な後退（バックオフ）**。N回連続で復旧に失敗したらウォッチドッグを止めてタップ待ちに落とす。壊れたまま放置しても静かになる。
- 完了条件: 復旧の連続失敗が一定回数を超えたら自動復旧を停止し、タップでのみ再試行する。
- 影響度: 中 / コスト: 小
- 補足: 実機で長時間使うと最初に気付く類の挙動。バッテリー消費にも効く。

### [ ] F-5 マイクが壊れている状態で録音ボタンを押すと1回空振りする
- 場所: `src/App.jsx` のジェスチャー復旧経路
- 現状（**F-2の周の審査で「新規の退行ではない」と認定**）: 壊れている状態で録音ボタンを押すと、そのタップが先に復旧を起こして**録音開始が空振り**する（`pointerdown` → `recoverMic` → `stopListening` → `setIsRecording(false)`）。体感は「1回目のタップで直り、2回目で録音が始まる」。
- 補足: HEAD も「ctxが running でない」ときは同じ構造だった。**新実装のほうが空振りする条件は広い**（トラックが死んでいる場合も走る）が、**それこそが今回直した不具合そのもの**（HEADでは ctx が running でトラックだけ死んでいる状態に何もしなかった）。空振りは修正の対価。
- 方向性: **録音ボタンだけは押下意図を引き継ぐ**（保留して復旧成功後に録音開始）。
- 完了条件: 壊れている状態で録音ボタンを1回押すと、復旧後にそのまま録音が始まる。
- 影響度: 小 / コスト: 小

### [ ] F-6 `console.warn("mic recovery:")` が本番にも出続ける
- 場所: `src/App.jsx` の `recoverMic`
- 現状: iOS限定の不具合を追うための痕跡としては妥当だが、**恒久的にコンソールへ書き続ける**。
- 完了条件: 開発時のみ出すか、削除する。実機で不具合が再発しないことを確認してから。
- 影響度: 小 / コスト: 小

### [ ] F-2 Safari（非PWA）では計測タブがまだ入りきらない
- 場所: `src/App.jsx` の `MeasureView` 全体の縦の帳尻
- 現状（**F-1の審査で実測**）: F-1 で**「幻のスクロール」は解消した**（`100vh`/`innerHeight` がアドレスバーを含む高さを返すため、中身が収まっていても `min-height` が可視領域より大きく敷かれていた分）。`visualViewport.height = 716` を偽装すると minHeight が **749 → 653** に正しく縮むことを確認済み。
- **しかし別の理由で実オーバーフローが残る**: Safari のブラウザchromeぶん可視領域は約716px。これに対し必要な可視高は **727px**（`padding-top 16` + 中身 664 + `--page-bottom-gap` 47）。下セーフエリア34込みなら **761px**。`dvh` を716相当に強制して実測すると `.app-root` の実高が727になり **11pxはみ出す**。
- 補足: 本人の指摘は「不要なのにスクロールできる」だったので**主旨としては解決**。PWA（375x812）では `812 = 812` で成立する。Safari で開いたときに小さくスクロールする事実だけが残る。
- 判断が必要: 次周で中身を削るか、「**PWA前提**」と割り切るか。
- 影響度: 小 / コスト: 中

### [ ] F-3 セーフエリア込みの縦の余裕が2pxしかない
- 場所: `src/App.jsx` の `MeasureView`（メトロノームを開いた状態）
- 現状（**F-1の審査で実測**）: 375x812＋セーフエリア上44/下34 で、予算 **671px** に対し中身 **669px**。**余裕2px**。
- 問題: **次に何かを1行足したら即座に溢れる。** F-1 では環の下の余白4px（操作UI側の `marginTop:8` と二重取りだった分）を落として捻出しており、もう明らかな無駄は残っていない。
- 補足: マーカー間距離も要件6 CSS px に対し実測 **6.60**（+0.6）で薄い。演出を少しでも大きくする要求が来たら軌道半径から引き直しになる。依存関係は §6.1 の表とコードの両方に記載済み。
- 完了条件: この画面に要素を足す周では、**着手前に縦の帳尻を全要素合算で確認する**こと。
- 影響度: 中 / コスト: —（運用上の注意）
- **【2026-08-01 F-7で再実測】** 予算671pxの内訳は 上端固定87 / 環330 / 可変の中間111 / 下端固定143。
  中間の中身は 素=6+94=100（余り**11px**）、メトロノーム開=8+101=109（余り**2px**）。
  **余りは「可変の中間」に集約されたので、足せる高さ＝メトロノーム開の2px**という関係は変わらない。
  なお環より上（上端固定87px）に1行足すと、その分そのまま環が下がる。

### [ ] F-8 上端固定ブロックに条件付き要素が同居していて、出ると環が動く
- 場所: `src/App.jsx` の `MeasureView` 上端固定ブロック
- 経緯: **F-7 で「メトロノーム／詳細の軸」のぶれは完全に解消した**（4状態×3要素の `top` が完全一致）。しかし本人の指摘「**とにかくどのページでも**追加表示で元の表示がぶれないように」は**録音・アップロードの軸では未達**。
- **審査役が実装役の測っていない状態を試して発見（実測）**:

  | 注入 | 環 top | 録音 top | トグル top | overflow |
  |---|---|---|---|---|
  | 通常 | 147.00 | 600.00 | 687.00 | 0 |
  | **上端に40px出現** | **187.00**(+40) | **629.00**(+29) | **716.00**(+29) | **29** |
  | リード登録あり/なし | **131 / 147**(16px差) | 600（不変） | 687（不変） | 0 |

- 該当する要素: **録音中インジケータ / アップロード進捗・完了 / 「この録音を保存しますか？」 / リード未登録の告知** の4つ。いずれも条件付きなのに**「常にあるもの」を置く上端固定ブロック**に同居している（§6.1.5 の構造表の定義違反）。
- **中間へ移す案は実測で否定済み**: メトロノーム開の中間の余りは **2px** しかなく、15px入れるとアクションが+13px動きスクロールが13px出る（審査役が実際に注入して確認）。**どこに置いても何かが動く**ため、置き場所の設計判断が要る。
- 方向性: **「録音後の登録しますか？」をポップアップにする**（本人の既出の指示）と、この要素はレイアウトの流れから外れる。**残り3つも同じ扱いにできるか**を検討する。F-3（余り2px）と表裏なのでセットで考えること。
- 完了条件: 録音中・アップロード解析中・保存確認・リード未登録のいずれの状態でも、環・録音ボタン・詳細トグルの `top` が変わらない。
- 影響度: 中 / コスト: 中
- **【2026-08-01 対応（過渡的な告知3件のみ）】** 「この録音を保存しますか？」をモーダル（暗幕＋カード。
  背景タップでは閉じず、登録／取り直しのどちらかで閉じる）に、録音中インジケータとアップロードの
  進捗・タップ要求・完了を画面上端に浮かせた告知（`position:fixed`）にして、いずれもレイアウトの
  流れから外した。**流れの中に置く限りどこでも何かが動く**（中間の余りは2px＝F-3）ため、
  「流れから外す」以外の解は無い、という判断。
  実測（375x812・`--nav-h:81px` / ルート `padding-top:60px`）: 素 / メトロノーム開 / 詳細開 /
  メトロ＋詳細開 / 録音中 / 保存確認モーダル / アップロード解析中 / アップロード完了 の**8状態すべてで
  環 147.00 / 録音ボタン 600.00 / 詳細トグル 687.00**。overflow は詳細開の447を除き全状態0。
  - **残: リード未登録の告知**（「「リード」タブでリードを登録できます」）。これは過渡的な告知ではなく
    恒常的な状態表示なので今回は触っていない。**登録あり/なしで環が16px動く問題は未解決**。

### [ ] F-12 グラフの座標係数が3箇所に重複している
- 場所: `NoteAxisLineChart` / `PivotLineChart` / `ReedScoreHistoryChart`
- 現状（**F-11の周の審査で指摘。3箇所目**）: `legendMax = W * 0.42` / `Math.round(FS * 0.35)` / `Math.round(FS * 0.8)` / `dotR = max(1.5, min(3, colStep*0.3))` / `plotH = 94` がグラフを足すたびにコピーされている。`0.42` は **P0-4 の周で既に「2箇所に重複」と指摘済み**で、今回3箇所目。
- 問題: `SERIES_STYLES` や `SVG_SP1/2` は共有できているのに、**この係数群だけ共有されていない**。値は一致しているので今は害が出ていないが、**片方だけ直す事故が起きる形が3セット並んでいる**。
- 補足: `REED_SCORE_PLOT_H = 94` には「リード比較の折れ線と同じ」とコメントが添えられており、**どこから来た値かは追跡できる**状態にはなっている。
- 完了条件: `CHART_*` として1箇所に括り出され、3つのグラフが同じ定数を参照している。
- 影響度: 小 / コスト: 小
- **次にグラフを触る周で必ず回収すること。**

### [ ] F-13 ダイヤルの窓が3行で、1と2はスクロールでしか選べない
- 場所: `RatingDial`（`ReedScoreEditor` 内）
- 現状（**F-11の周の審査で指摘**）: 窓は3行（132px）。値が4のとき見えるのは 5/4/3 のみ。**行タップの逃げ道は見えている3行にしか無い**ため、1と2を選ぶにはスクロールが必要。
- 問題: **スクロールは Browserペインで `scroll` イベントが発火しないため誰も検証できていない経路**。しかもこの周で「位置合わせ由来のスクロールが履歴を増やす」バグが実際に出ている。スクロールに依存する値が残るのは弱い。
- 方向性: **窓を5行にすれば全値が常にタップで届く**（44×5 = 220px、モーダル内なら収まる）。ダイヤルの見た目は保ったまま、スクロールを不要にできる。
- 補足: 3行の窓自体は既存 `ScrollPicker` と同じ性質で、コーディネータが許容と判断済み。これは**改善の候補**であって不合格事由ではない。
- 影響度: 中 / コスト: 小

### [ ] F-14 評価グラフの横軸が等間隔で「変化の速さ」を歪める
- 場所: `ReedScoreHistoryChart`
- 現状（**F-11の周でコーディネータが承認した判断。記録として残す**）: 横軸は「記録1件＝1目盛」の等間隔。同じ日に複数回評価すると点が重なって読めなくなるため。
- 観察（審査役の実測）: 7/10→7/20→7/28→8/1（間隔 10日/8日/4日）が**等間隔に描かれる**。日付ラベルが出るので読めば分かるが、**折れ線の傾きは時間あたりの変化率として読まれやすい**。
- 補足: **記録が疎になるほど誤読が増える**性質がある。実機で違和感が出たら真の時間軸へ変更する。
- 影響度: 小 / コスト: 中

### [ ] F-10 告知に覆われた設定行が「無反応」になる
- 場所: `src/App.jsx` の `MeasureView` 上端の告知スタック（`position:fixed` / `zIndex:40`）
- 現状（**F-8の周の審査で実測**）: 告知が上端に浮くため、表示中は設定行に重なる。

  | 状態 | メトロノーム | アルト | 442Hz | 告知の幅 |
  |---|---|---|---|---|
  | 通常 | 到達可 | 到達可 | 到達可 | — |
  | **録音中** | **到達不可** | 到達可 | 到達可 | 内容幅 |
  | **解析中** | **到達不可** | **到達不可** | **到達不可** | 全幅347 |

- **実装役の申告「録音中は `disabled` なので実害なし」は事実誤認**。実測では3つとも `disabled: false`。録音中もメトロノームボタン1個が塞がれる。
- **触れないこと自体は正しい**（解析中に基準Hzや楽器種別を変えると解析結果と設定が不整合になる）。問題は**押しても何も起きない**こと。解析はファイルの長さぶん時間がかかるので、その間ずっと無反応は「固まった」と解釈される。
- 方向性: 覆われる側を **`disabled` にして見た目でも分かるようにする**（§6.1.5 に規則として追記済み）。または告知を設定行の下に置く。
- あわせて: 暗幕タップで閉じないモーダルは、初見では「反応しないモーダル」に見える。**受け付けたが閉じない**というフィードバック（カードを一瞬揺らす等）があると迷いが消える。
- 完了条件: 告知に覆われた操作が `disabled` として見た目に出る。無反応な領域が無い。
- 影響度: 中 / コスト: 小

### [ ] F-11 `閉じる` ボタンの UA既定フォントサイズが残っている
- 場所: `src/App.jsx` アップロード完了告知の `×` ボタン
- 現状: `<button>` 自体の computed `fontSize` が **13.3333px**（UA既定）。描画される文字は内側 span の13pxなので見た目に影響はないが、`TAP_BUTTON_RESET`（2周前に導入）は同じ問題に `fontSize: var(--fs-xs)` を明示していた。**ここだけ揃っていない**。
- 完了条件: UA既定のフォントサイズが残る `<button>` が0件。
- 影響度: 小 / コスト: 小

### [ ] F-9 §6.1.5 の「動かない」を自動で守る仕組みが無い
- 現状（**F-7の審査で指摘**）: §6.1.5 は「実測せよ」と書いているが、**その実測は毎回審査役が手作業でやり直している**。`top` の不変性は DOM を要するのでハーネス（`extractFunction` 方式）には載らない。
- 問題: §6.1.5 は**今後すべての画面に適用される規則**なので、画面が増えるほど手作業の検証コストが積み上がる。
- 方向性: Playwright 等の軽い DOM テストを1本入れれば、この規則だけは自動で守れる。
- 影響度: 小 / コスト: 中

### [ ] F-34 §6.7 の検査が「操作を包む親」と「自作コンポーネントの定義側」を見ていない
- 場所: `scripts/pitch-test.mjs` の 17.10（芯1・芯2）
- 現状: 走査は操作するものから**下（部分木）**へ潜るが、**上（包む親）**と**自作コンポーネントの定義**を見ていない。統括が実測（いずれも PASS 4749 / FAIL 0 で素通り）:
  - `<div style={{border, background}}><button>x</button></div>` → **SURVIVE**
  - `<label style={{border, background}}><input/></label>` → **SURVIVE**
  - `<button><FakePill/></button>` + `function FakePill(){ return <span style={{border, background}}/> }` → **SURVIVE**（実装役が申告）
- 問題: **コントロールを枠付きの箱で包むのは日常的な書き方**。リード選択ピルがまさにこの形（いまは B型で地だけなので違反していないが、誰かが枠を足しても検査は落ちない）。
- **現時点で違反は 0 件**（実装役が実測で確認。操作の部分木で使う大文字タグ13種のうち枠と地を同時に持つのは `RatingDial` の選択窓マーカー1件だけで、それは `pointerEvents:"none"` の非操作要素）。
- 方向性: 操作の**祖先鎖**も走査に入れる。ただし自作コンポーネントの定義まで伸ばすと `RatingDial` が即座に引っかかり除外の印がもう1つ要るので、**独立した1周にすること**（実装役の判断。妥当）。
- 影響度: 中 / コスト: 中

### [ ] F-35 `SwipePager` のタブが実際のポインタ操作（クリック/タップ）で切り替わらないことがある
- 場所: `src/App.jsx` の `SwipePager`（リード/比較の切り替え・データの My Data/分析、および最下部の計測/リード/データのボトムナビ）
- 現状（**16項目フィードバック対応の実測検証中に統括が発見・HEADでも再現を確認済み＝今回の回帰ではない**）: Browser paneの `computer.left_click`（実タップに近いポインタイベント列を発生させる）でタブボタンを押しても、視覚的なトランスフォームが動かないだけでなく**タブの中身自体が切り替わらない**ことがある（ボトムナビで確認）。一方、`element.click()` を直接呼ぶ（合成クリックイベント1発）と正しく切り替わる。`git stash` で作業ツリーを一時的にHEADへ戻し、別ポートのdev serverで同じ操作をしても同一現象が再現した。
- 問題: `SwipePager`/`SwipeBackArea` はポインタイベント（`pointerdown`/`pointermove`/`pointerup`）ベースの指追従実装（F-16/F-25 で指摘の `overflow:hidden` 依存の実装）だが、**タップ（ドラッグ距離ほぼ0のポインタ操作）を正しくクリックとして扱えていない可能性がある**。実機のタップも合成クリックではなくポインタイベント列なので、本番でも再現し得る実害の疑いがある（未確認）。
- 完了条件: 実機（またはポインタイベントを忠実に再現できる環境）で、ドラッグを伴わない単純なタップでタブが確実に切り替わることを確認する。切り替わらない場合は `SwipePager`/`SwipeBackArea` のポインタハンドラでタップ相当の操作を正しくクリックとして通す。
- 影響度: 大（未確認だが本番のタップ操作そのものに関わる可能性） / コスト: 不明（原因未特定）

### [ ] F-33 通知面（エラー・警告・告知）が体系のどこにも属していない
- 場所: `src/App.jsx` のエラー表示（`#FEF2F2` + `#DC2626`）、警告（`#FDF0E1` + `#F0D9B8`）、アップロード告知、`ScrollPicker` の選択帯
- 現状（**枠線と地の統一の周に実装役が申告**）: いずれも**枠と地を両方持つ**。ただし「操作するもの」ではないので §6.7 の芯（A型/B型）の対象外とした。
- 問題: **§6.6（面の作法）にも §6.7（枠線と地）にも属していない空白地帯**。次に触る人が「違反だ」と読んで潰すか、逆に何でもありの場所として使うか、どちらかになる。
- 方向性: 通知面は「一時的に現れて消えるもの」で、群でも操作でもない第3の種別。**§6 に種別として足す**のが素直。色は機能色（`--c-danger` 等）を持つので、A型/B型とは別の規則になる。
- 影響度: 中 / コスト: 小（文書だけで済む可能性が高い）

### [ ] F-32 削除の文字色が AA(4.5:1) をわずかに下回る
- 場所: `src/App.jsx` の削除実行ボタン（B型 = 地 `--c-sunken` + 文字 `--c-danger`）
- 現状（**枠線と地の統一の周に実測**）: `--c-danger #DC2626` on `--c-sunken #EEF1F4` = **4.26:1**。12px・weight 600 なので WCAG の「大きい文字」に該当せず、**AA の 4.5:1 に 0.24 届かない**。
- 旧実装は白文字 on `#DC2626` = 4.83:1 だったので、**わずかに下がっている**。ただし旧実装は押せないときが白文字 on `#E9ECF0` = **1.19:1**（文字が消えている水準）で、そちらは 2.67:1 に改善した。
- **体系内で上げる段が無い。** `--c-danger` より濃い赤のトークンが存在しない。
- 方向性: (a) 4.26 を許容する、(b) 濃い危険色を1段足す（**値の発明になるので先に DESIGN-SYSTEM を更新すること**）、(c) 削除の文字だけ地を白（`--c-bg`）にして 4.83:1 に戻す＝B型の例外にする。
- 影響度: 小 / コスト: 小

### [ ] F-31 `@import` でスタイルの入口を増やせる
- 場所: `src/index.css`、`scripts/pitch-test.mjs` の「面の作法」
- 現状（**白背景の周に審査役が発見**）: `index.css` の末尾に `@import url("./other.css")` を足すと**検査は素通り**（PASS 4610）。「スタイルの入口は `index.css` と `App.jsx` の `<style>` の2つ」という前提が崩せる。
- 問題: 作法の規則を別ファイルから上書きできてしまう。ただし**わざわざ新しい CSS ファイルを作る必要がある**ので、うっかり踏む経路ではない。
- 完了条件: `@import` が無いことを検査するか、名乗りを「入口は2つ＋`@import` が無いこと」に狭める。
- 影響度: 小 / コスト: 小

### [ ] F-29 同じ「箱」なのに角丸が `--r-lg`(16) と `14` で食い違う
- 場所: `src/App.jsx:6868`（`ReedCompareTab` の箱）と `ReedRegisterView` の箱
- 現状（**白背景の周に審査役が発見**）: 同じ役割の箱なのに一方が `var(--r-lg)`、もう一方が生の `14`。§2 では 14 は `--r-lg` に吸収される値。
- 完了条件: どちらも `--r-lg`。
- 影響度: 小 / コスト: 小

### [ ] F-28 メトロノームの開閉で環が 16px・録音ボタンが 9px 動く
- 場所: `src/App.jsx` の `MeasureView`
- 現状（**白背景の周に審査役が発見**。HEAD でも同じなので**この周の回帰ではない**）: メトロノームパネルを開くと録音ボタンが **+9px** 下がる。
- **【訂正】環も動く。** 当初ここに「環（330×330）は5状態すべてで不変」と書いたが、後の周の実測で**メトロノームを開くと環の top が 147 → 131（−16px）** だった。不変なのは**大きさ**（330×330）であって位置ではない。5状態で不変というのは「その周の変更で動かなかった」という意味で、状態そのものによる移動は元からある。**数字を引き写す前に自分で測ること。**
- 問題: §6.1.5 の「何かを開いても、既にあった要素は1pxも動かない」に反する。**押す物が動く**のは、読み取りより実害が大きい（狙って押せない）。
- 完了条件: メトロノームの開閉で録音・アップロード・詳細トグルの `top` が変わらない。
- 影響度: 中 / コスト: 小

### [ ] F-27 旗の宣言位置を上げると sloppy-mode のグローバルになり、検査間で状態が漏れる
- 場所: `scripts/pitch-test.mjs` の配線切り出し（目印が `let settleTimer = 0;`）
- 現状（**検査補強の周に審査役が発見**）: `dragHorizontal` の宣言を `let settleTimer = 0;` より上へ動かすと、**切り出しは成功したまま旗が宣言されないコードとして評価され、検査間で状態が漏れます**。実測は FAIL 1 で沈黙こそしないが、「配線を実物ごと取り出して組める」は通ってしまう。
- 完了条件: 切り出した断片が宣言を含んでいることを確かめる（`let dragHorizontal` が断片内にあること）。
- 影響度: 小 / コスト: 小

### [ ] F-26 「綴りに依らず数える」が分割代入を数え落とす
- 場所: `scripts/pitch-test.mjs` の「旗への書き込みは計4箇所だけ(綴りに依らず数える)」
- 現状（**検査補強の周に審査役が発見**）: `({ dragHorizontal } = { dragHorizontal: true })` は `=` を使うのに書き込み数4のまま通ります。**実行検査3件が拾うので実害はありません。**
- 問題: 名乗りが「綴りに依らず」なのに、実際は単純代入・複合代入・増減演算の3種しか数えていない。
- 完了条件: 名前を実態（「単純代入・複合代入・増減演算を数える」）に寄せるか、分割代入も数える。
- 影響度: 小 / コスト: 小

### [ ] F-25 `SwipePager` の立入禁止検査に穴（`overflow:hidden` と `data-noswipe` が無防備）
- 場所: `src/App.jsx` の `SwipePager`、`scripts/pitch-test.mjs`
- 現状（**検査補強の周に審査役が発見**）: `SwipePager` の viewport から `overflow:"hidden"` を外す／`isSwipeTarget` の除外から `[data-noswipe]` を外す、**どちらも PASS 4335 / FAIL 0 で通ります**。既存の5件（threshold / 抵抗0.35 / 軸判定6px / `passive:false` / touch イベント）が名乗る範囲外なので名乗り違反ではなく、カバレッジの穴。
- **F-16 と同根**（`SwipePager` 側だけ `SwipeBackArea` より守りが薄い）。実害は現時点で無い（本体3140バイトが HEAD と一致）。
- 完了条件: F-16 とまとめて、`SwipeBackArea` 側と同じ強さの検査を `SwipePager` にも掛ける。
- 影響度: 小 / コスト: 小

### [ ] F-24 `ReedScoreField` の JSX は無検査（純関数の返り値までしか守っていない）
- 場所: `src/App.jsx` の `ReedScoreField`、`scripts/pitch-test.mjs`
- 現状（**スワイプ改善の周に審査役が発見**）: 行の「中身の組み立て」は `reedScoreRowItems` として純関数に出たが、**それをどう描くか**は正規表現だけ。以下が **PASS 4328 / FAIL 0** で通る:
  - `{it.label}` を消す → **項目名が全部消える**
  - `it.rated ? accent : ink-3` を反転 → **未評価が濃く、評価済みが薄くなる**
  - `{it.sep && …}` を `{false && …}` に → **区切りが消える**
- 問題: 検査名「行は項目名をそのまま出す」「区切り『・』は先頭以外にだけ付く」が守っているのは**純関数の返り値だけ**。名乗りの範囲としては正直だが、画面に出るものは守られていない。
- 方向性: F-19 と同根。JSX を実行できないハーネスの構造的限界なので、軽い DOM テスト（F-9 と同じ）を1本入れれば両方まとめて片付く。
- 影響度: 中 / コスト: 中

### [ ] F-23 軸の掃引検査は「掃いた範囲の外」を守っていない
- 場所: `scripts/pitch-test.mjs` の A-3 掃引（0〜90°・距離60px・move 4回）
- 現状（**スワイプ改善の周に審査役が発見**）: 掃引の外側でだけ挙動を変える書き換えが全部通る（すべて PASS 4328 / FAIL 0）:
  - 上向き（`dy < 0`）の斜めドラッグだけ横と断定 → **上スクロールが死ぬ**
  - 250px を超えたら未確定帯を横へ倒す（100px 版は死ぬが、掃引の外へ出せば通る）
  - `e.pointerId >= 5` のときだけ横と断定
  - `document` に常時 `preventDefault` の `touchmove` を1本足す（検査は `window.` しか除外していない）
- **検査名は範囲を正直に名乗っているので名乗り違反ではなく、カバレッジの穴です。**
- 方向性: 掃引の次元を増やす（符号・距離・move 回数・`pointerId`）。ただし理論上の抜け道はゼロにできないので、費用対効果で線を引くこと。
- 影響度: 小 / コスト: 小

### [ ] F-22 ハーネスが「実物を `new Function` で評価する」検査は、リファクタで集計行ごと消える
- 場所: `scripts/pitch-test.mjs` の `onTouchMove` / `isSwipeTarget` の実物評価、`commitReedScores` 周り
- 現状（**スワイプ改善の周に審査役が発見**）: ソースから切り出した実物を `new Function` で評価する検査は、**外側のヘルパを参照する形にリファクタすると `ReferenceError` でハーネスごと落ち、PASS/FAIL の集計行すら出ません**（審査役が N01・N02・N02b・X04 で再現）。
- 問題: 「何件落ちたか」が読めないので、**変異試験の結果が判定に使えなくなります**。実装役は同じ穴を `commitReedScores` で1度直しましたが、今周新設した箇所に同じ形が残っています。
- 方向性: 実物の評価を `try/catch` で包み、失敗を1件の FAIL として計上して集計を続ける。
- 影響度: 中 / コスト: 小

### [ ] F-21 メモ欄を伸ばすと縦250px以上がスワイプの不感帯になる
- 場所: `src/App.jsx` の `SwipeBackArea` の `isSwipeTarget`（`input, select, textarea, [data-noswipe]` を除外）
- 現状（**スワイプ改善の周に審査役が発見**）: 除外は設計どおりだが、メモ欄は `resize: vertical` で伸ばせるため、伸ばすと**画面のかなりの面積がスワイプに反応しなくなります**。審査役の実測では、メモ欄を320pxに伸ばした状態で 20ケース中8件がこの理由で不発。
- 問題: 「反応しない」の体感源になり得る。入力欄の上でスワイプを殺す必要があるのは**横方向のキャレット移動と競合するから**だが、`textarea` の縦の広がりまで殺す必要はない。
- 方向性: 除外を「フォーカスされている入力欄だけ」に絞る、あるいは `textarea` は左端の一定幅だけ除外する。
- 影響度: 中 / コスト: 小

### [ ] F-20 45〜56.31° の直線ドラッグが「未確定のまま」終わる
- 場所: `src/App.jsx` の `swipeAxisIsHorizontal` / `SWIPE_VERTICAL_BIAS`
- 現状（**スワイプ改善の周に審査役が1°刻みで実測**）: 横確定は `|dx| > |dy|`（45°未満）、縦確定は `|dy| >= |dx| * 1.5`（56.31°以上）。**その間の11.3°の帯は永久に未確定**で、横にも動かず縦にも確定しない。
- **修正前も同じ角度で発火しない**ので退行ではありません。「勝つまで決めない」という設計の帰結です。
- 問題: 実ブラウザではこの帯に入るとブラウザが縦スクロールを引き取るので実害は小さいが、**帯の中で指を止めると何も起きない**。
- 方向性: 未確定のまま一定距離を超えたら横に倒す、など。ただし未確定のうちに `preventDefault` すると iOS はそのジェスチャー全体のスクロールを捨てるので、単純な解は無い。
- 影響度: 小 / コスト: 中

### [ ] F-19 リスナーの配線（どのイベントを・どのハンドラに・どの要素へ）が無検査
- 場所: `scripts/pitch-test.mjs:2018-2028`、`src/App.jsx` の `SwipeBackArea` の `useEffect`
- 現状（**スワイプ改修の周の最終審査で発見**）: 検査は「4つのイベント名の文字列があるか」「`g.down`〜`g.cancel` の4つの識別子がソースに出てくるか」しか見ていない。次の3つが **PASS 4187 / FAIL 0 のまま通る**（審査役が実測）:
  - `pointerup → g.cancel` / `pointercancel → g.up` に入れ替える → **スワイプで遷移しなくなる**
  - `pointermove` を張らない → **指に一切追従しなくなる**
  - `pointerdown` を `vp` ではなく `window` に張る → 下部ナビ上のドラッグでも詳細が動く（**いちばん気づきにくい**）
- 問題: 検査名「イベントに繋ぐのはファクトリが返した4つだけ」は、①イベントに繋がっていること ②それだけであること の**どちらも見ていない**。判定と後始末は実行で守れるようになったのに、**それを外界に繋ぐ最後の1段だけが素通し**。
- **その周の欠陥ではない**（3周目以前から居座っていた名乗り。審査役自身も見落としていた）。
- 方向性: `addEventListener` の呼び出しを `(type, 変数名, 対象)` の3つ組で数える程度のソース検査で塞げる。
- 影響度: 中 / コスト: 小

### [ ] F-18 箱の一括削除だけ `window.confirm()` が残っている
- 場所: `src/App.jsx:5786`
- 現状（**スワイプ改修の周に審査役が発見**）: F-8 で「過渡的な告知はモーダル化して流れから外す」を通したのに、箱の一括削除だけ OS ネイティブの確認ダイアログのまま。
- 問題: PWA では見た目も操作感も浮く。しかも**いちばん破壊的な操作**（箱ごと消す）がアプリの作法の外にある。
- 完了条件: F-8 と同じモーダルの作法（`--c-danger`・44pt・背景タップで閉じる）で確認を出す。
- 影響度: 小 / コスト: 小

### [ ] F-16 `SwipePager` の `overflow:hidden` が無検査（テスト全緑のまま外せる）
- 場所: `src/App.jsx` の `SwipePager`（viewport 側）、`scripts/pitch-test.mjs`
- 現状（**スワイプ改修の周に審査役が変異試験で発見**）: 審査役が21件の変異を回したうち、**唯一生存したのが「`SwipePager` の viewport から `overflow:hidden` を外す」**（PASS 4162 / FAIL 0 のまま通過）。
- **その周の回帰ではない**（`SwipePager` はHEADとバイト同一）。`SwipeBackArea` 側の同じ性質は検査されているのに、元祖である `SwipePager` 側は無防備という非対称。
- 問題: これが外れると、ドラッグ量ぶんページが横に伸びてページ全体が横スクロールできる（`SwipeBackArea` で実測 `scrollWidth 375→422`）。誰かが「効いていないように見える」と消しても誰も気づかない。
- 完了条件: `SwipePager` の viewport が `overflow:hidden` を持つことを、外すと落ちる形で検査が固定している。
- 影響度: 小（現時点では正しい） / コスト: 小

### [ ] F-15 一覧のスクロール位置が復元されない（詳細から戻ったとき）
- 場所: `src/App.jsx` のリード一覧／記録一覧と、それぞれの詳細への出入り
- 現状（**スワイプ改修の周に審査役が実測して発見**）: 一覧を `y=129` までスクロール → 詳細を開く → 戻る、で `scrollY` が **0**。スワイプで戻っても「一覧に戻る」ボタンで戻っても同じ。
- **その周の回帰ではない**（ボタン経路でも同じなので、スワイプ導入より前から壊れている）。当該周では明示的に対象外とした。
- 問題: 一覧が長いほど痛い。奏者は「さっき見ていたリードの隣」を見たくて戻るので、毎回先頭に飛ばされると探し直しになる。
- 完了条件: 詳細から戻ったとき、開く直前の `scrollY` に戻る。復元は経路（スワイプ／ボタン）に依らない。
- 影響度: 中 / コスト: 小

### [ ] C-1 `DESIGN.md` §4.2 の系列色の記述を更新
- 現状の記述: 「リード比較の系列色が `#7C3AED` … と別の視覚言語になっている。統一対象」
- 実際: `REED_COMPARE_COLORS`(5178) は**紺基調に修正済み**。Tailwind系8色が残るのは `PIVOT_LINE_COLORS`(5794) **のみ**
- 対応: 「リード比較」→「データタブのピボット折れ線」に修正する

### [ ] C-2 `DESIGN.md` §4.2 の実数を更新
- 現状の記述: 「ユニーク約45色 / hex直書き約470箇所」
- 実際: **ユニーク44色 / 516箇所**（+ rgb/rgba 15箇所は未計上）

---

### [ ] N-3c `sanitizePitchOutliers` の連鎖 null（既存バグ。N-3 とは独立）
- `src/App.jsx` の `sanitizePitchOutliers` は **元配列**から3点中央値を取る
  （`cents` を1回作って `out` に書き戻さない）。そのため単発のオクターブ誤検出が
  200ms 離れていると、**間に挟まった正常フレームまで外れ値と判定して null にする**:
  - i=20: (正常, +1200, 正常) → 中央値=正常 → 誤検出を除去（正しい）
  - i=21: (+1200, 正常, +1200) → **中央値=+1200** → **正常フレームを除去**（誤り）
- 現行の集計（時間の重み）では1〜2フレーム減るだけなので実害は小さく、**本人からの報告も無い**。
  N-3 を再開する場合は先に直すこと（音が割れる原因になる）
- 直し方の候補: 中央値を**逐次更新された値**から取る。ただし「何を捨てるか」が変わるので
  既存の検査（18.x 群）への影響を測ってから
- 影響度: 小（現行では） / コスト: 小

### [ ] N-3b PIVOT と音階ごとの平均で「平均差分」の値が食い違う
- 分析タブ（PIVOT）とセッション詳細の「音階ごとの平均」で、同じ音について違う数字が出る
  （実測 −3.176¢ vs −2.000¢）。N-2 で「平均差分」に語彙を統一したのに、その語彙が指す量が2つある
- **N-3 見送りにより、これは N-3 とは無関係な既存の食い違いとして残る**。直すなら
  どちらに揃えるかを本人に確認すること
- 影響度: 中 / コスト: 小〜中

### [ ] N-4b タップ領域44px未満が9件（既存。§5 は機能側の規定として有効）
- 実測（375×812）: 奏者 select 125×28 / Alto 28.7×22 / 442Hz 42.7×22 / **リード枠 label 174×30** /
  リード箱 select 104×26 / リード個体 select 48×26 / 下部ナビ 計測・リード・データ 各 111.7×32
- **すべて N-4 の変更前から同じ寸法**（リード枠 label は N-4 で `div`→`label` にしたが高さは元と同値）。
  新規に小さくしたものは無い
- 下部ナビは帯46pxに対しボタン32px・左右20pxの余白があり、`height:46 / padding 0` にすれば
  **無反応 6,905px² と 44px 未達が同時に解消する**
- 影響度: 中 / コスト: 小

### [ ] N-5c N-5 が残した小さな負債（審査役が合格時に指摘）
- `src/App.jsx` の「…」の表示条件 `reeds.length > 0 || listMode !== null` の**第2項の説明がソース側に無い**
  （説明がテスト側にしかない）。しかも `confirmBoxDelete` / `confirmMemberDelete` が削除と同じハンドラ内で
  `exitMode()` を呼ぶため **「0枚 かつ モード継続」には到達しない**。防御としては無害だが、
  検査の主張「戻れなくならない」は**到達不能な状態を守っている**
- `usageDays` の `+1`（「開封 n日」）に検査が無い（審査役の変異16件中、唯一の生存）。
  `localDayKey` のコメントが「usageDays もずれる」と言及している以上、1本あるべき
- 影響度: 小 / コスト: 小

---

### [ ] N-6c ヒーローの単位の文字列に検査が無い
`heroMetric.unit` を `¢` → `cent` にする変異が生存。正典 `.hero .u` の中身を読んで突き合わせる1行で塞げる。
- 影響度: 小 / コスト: 小

### [ ] N-6d `DATA_SUBTAB_GAP_PX` に検査が無い
18→12 の変異が生存。正典 `.subtabs` の gap は CSS にあるので `declOf(mockCss, ".subtabs", "gap")` で突き合わせる。
- 影響度: 小 / コスト: 小

### [ ] N-6e 別名のラベル定義がラベル検査の列挙から漏れる
列挙検査は `/const ([A-Z_]*METRICS)\s*=\s*\[/` しか見ないので、`const RowDefs = {…}` のような
別名・別形の定義を `MetricRow` で使う変異が生存。**検査名は範囲を正直に名乗っている**ので実害は小さい。
- 影響度: 小 / コスト: 中

---

### [ ] F-105 個別ページの #番号は「触れて blur しただけ」で自動採番が boxNumber に固定化される（既存・F-102 の周で発見）
- 場所: `src/App.jsx` の `ReedEvaluationDetail` の `commitPosition`
- `positionDraft` は**表示上の自動採番**(reedPosition)で初期化されるが、blur 時の比較相手は
  **保存値**(`String(reed.boxNumber ?? "")`)。boxNumber 未設定のリードで入力欄に触れて何も変えずに
  blur すると、`"4" !== ""` で自動採番の 4 が boxNumber に書き込まれ、以後の削除で自動で詰め直らなくなる。
- F-102 の番号編集シートでは同じ形を避けた(保存値と**表示値の両方**と比較してから書く)。
  個別ページ側も同じ二段判定にすれば直る。
- 影響度: 小(気づきにくいが不可逆) / コスト: 小

### [ ] F-93 読み込み前の「まだリードが登録されていません」が一瞬見える（**F-101 では解消しない**）

**【2026/08/17 F-101 の周で確認】** F97-SPEC は「F-101 のモジュールキャッシュで F-93 も同時に解消する」
としていたが、**構造的に解消しない**ので消し込まない: F-93 の窓は**起動直後**(コールドスタート)のもので、
モジュールスコープの Map はページ読み込みごとに空から始まる(このエントリ自体が
「タブを行き来しても再発しない。起動直後だけの窓」と特定済み)。F-101 が消すのは**再マウント時**の
フリッカ(F-101 の症状そのもの)だけ。凍結仕様と過去の実測が矛盾していたため、仕様側の誤りとして
統括へ報告済み。直すなら従来の候補(「未読み込み」状態の区別)のまま。

審査役が**一度だけ**「リードタブが『まだリードが登録されていません』と表示される」のを観測した件。
**再現条件と原因を特定した。HEAD からある欠陥で、F-84〜F-92 の差分とは無関係。**

- `usePersistedState`（`src/App.jsx:1798`）は **`useState(initialValue)` で始め、
  IndexedDB からの復元を `useEffect` の中で待つ**。`reeds` の初期値は `[]`（同 2655行）。
- リード一覧は `reeds.length === 0 ?「まだリードが登録されていません」: …`（同 8823行）。
- したがって**初回描画から `idbGet("reeds")` が解決するまでの間、空の文言が出る**。

**窓の長さの実測**（375×812 / 静かな状態）: `indexedDB.open` 2.5ms + `get` 1.1ms = **合計 3.6ms**。
通常は1フレーム未満なので目に入らないが、**負荷が高いと伸びる**
（審査役は変異試験を並列で回している最中に観測している）。
`reeds` はトップレベルにあり `navNonce` の再マウント対象外なので、
**タブを行き来しても再発しない。起動直後にリードタブを開いたときだけ**の窓。

**Chrome では狙って再現できない**: Browser pane は `document.hidden = true` で
`requestAnimationFrame` が発火しないため、この短い窓を標本化できなかった
（rAF ループを仕掛けると 30 秒でタイムアウトする）。原因は**コード経路として確定**、
実際の見え方は**実機待ち**。

**直し方の候補**（担当外なので実装していない）:
- 復元前は「空」と「未読み込み」を区別する（`usePersistedState` が読み込み済みかを返す）。
  ただし**全画面の空状態に効く変更**なので、単独の周で扱うこと。
- 影響範囲: `reeds` だけでなく `idealProfiles` `performers` など
  `usePersistedState` を使う全キーが同じ形。

- 影響度: 小（3.6ms）/ コスト: 中（共通フックの契約が変わる）

### [ ] F-94 速度検査が負荷で偽陽性を出す（**最も危険な向きの誤り**。旧起票 F-17 と同件）
`速度(60fps耐性)` / `音色測定の速度(60fps耐性)` は `for (i<100) …; per = 経過 / 100` の**平均**を見ている。
スケジューラの奪い合いは経過時間を**増やす方向にしか効かない**ので、平均は必ず汚染される。
実測: 静穏時 `detectPitchMPM` は **2.93 / 3.22 / 3.09 ms**（しきい値 8ms に対し約2.5倍の余裕）だが、
3並列で回すと 12〜21ms に伸びる。**変異試験の campaign が2周連続で全滅した直接の原因。**

**この偽陽性は SURVIVED を KILLED に見せる向きにしか働かない**（＝「変異が検出された」と誤って安心させる）。
本案件の主要な統制が変異試験である以上、最も危険な向き。

**直し方（審査役の推奨）**: 分離ではなく、**同じ計測を数バッチ繰り返して最小値を採る**。
主張（1回の呼び出しが1フレームに収まる）はそのまま、しきい値 8ms も据え置きで、負荷由来の偽陽性だけが消える。
ゲートも1本に保てる。**しきい値を緩めるのは不可**（主張そのものを弱める）。
分離を選ぶなら `design/LOOP.md` のゲート一覧に速度ゲートを明記し、報告様式に1行足すことをセットにする
（分離すると必ず回し忘れが起きる）。
- 影響度: 大（統制の信頼性） / コスト: 小

---

### [ ] F-96 拍子の当たり判定が §5 の 44pt に届かない（**HEAD からの積み残し・担当外**）

拍子（4/4）の当たり判定は 45×45 の箱だが、左下が − の当たり判定に食われて
**中に入る最大の正方形が 41×41**（拡大前は 38×38）。§5 は 44pt を要求している。
- 原因: 拍子の箱（`height: var(--tap-min)`）とテンポ行（`METRO_PM_H_CSS = 57.6`）が縦に 2.8px 重なり、
  そこで横に 14px 重なる。前面（zIndex 1）のテンポ行が勝つ。
- 直し方の候補: 拍子の当たり判定を上へ寄せる（`translateY` の中心をずらす）／
  テンポ行の上端を下げる。**どちらも見た目の位置を動かさずにできるが、F-74 の背面レイヤの範囲と
  §6.1.5 の「行を伸ばさない」に触るので単独の周で扱うこと。**
- 影響度: 中（本人が名指しした入口の1つ） / コスト: 小

---

### [ ] N-8a 行ヘッダの差分が浮動小数の誤差で「+0.0」と出得る（既存。N-7 から）

- 場所: `src/App.jsx` の `metricRowHeader` の `diffText: diff === null ? null : \`${diff > 0 ? "+" : ""}${m.fmt(diff)}\``
- 実測（N-8 の周・合成データ）: 今日と期間の HNR が実質同値のとき、weightedMean の誤差で
  diff ≈ 2e-15 > 0 になり **「+0.0」**（重心では「+0」）が出た。N-6a の本人指示
  「ちょうど 0 のときは符号を付けない」の精神に反する表示が、書式ではなく**符号の前置判定**の側に残っている。
- 直し方の候補: 符号は `m.fmt(diff)` の結果が `"0.0"` / `"0"` でないときだけ前置する
  （formatSignedCents と同じ「丸めた結果で判定」の考え方）。
- 影響度: 小（実データではほぼ同値のときだけ） / コスト: 小

---

### [ ] N-9a PIVOT の値チップ(46×20)と「＋条件を追加」(102×24)が §5 の 44px 未達
HEAD からの既存未達(N-9 の diff は該当 style 行に無変更 = 審査役が裏取り済み)。
値チップは PIVOT の主要操作経路なので個別に直す価値がある。見た目は変えず当たり判定だけ
広げる(§5 の作法。.taptext と同型)。
- 影響度: 中 / コスト: 小

---

## 直近の完了記録（それ以前は BACKLOG-archive.md）

## 完了: F-97〜F-104 実機フィードバック第3陣（2026/08/17）

本人指示(凍結仕様 = `design/F97-SPEC.md`)。build 成功 / **PASS 6364・FAIL 0**(着手前 6331)。
検証ゲート **4.9 秒**。**変異試験 19 件を一意名の複製ツリー(f97mut-6fe181-a)で単独逐次・
19 KILLED / 生存 0**(複製は終了後に削除)。

| | 内容 |
|---|---|
| F-97 | **現行ビルドで再現せず**(実測: 全3行が同一構造・先頭行の全可視域 16,309 点の elementFromPoint 1px 走査で全点が行に命中・dateLeft 全行 46)。全行が同一 map の1形で先頭行だけ変わる分岐は N-6 以降のどのビルドにも無い。本人スクショの目安凡例から**撮影端末は N-8 より前のビルド = キャッシュ由来の可能性**。対処として挿入→**常設+display 切替**へ構造変更(モード切替時のノード挿入を WebKit が取りこぼす機序を構造的に不可能化)。モード外の外形は不変(dateLeft 16 / rowH 47) |
| F-98 | セッション詳細: 入力欄(日付・メモ)を白地+下線(--c-line。リード個体詳細のメモと同じ作法・枠は透明で外形維持)へ / 奏者・リード・楽器を「ラベル 12px --c-ink-2 + minWidth 3em」の1行1情報へ(コロン廃止・F-55 の 2em を置換) / 解説文2つ(除外率の1行・「— ピッチ一致度で色分け(…基準)」)を削除。**集計の過渡除外そのものは不変**。検出ノート数・平均アタックのデータ行は残置 |
| F-99 | 表題「PIVOT」(15px/--c-accent/700 = 旧実装と同値)と1行の説明「条件・縦軸・横軸・指標を選ぶと、蓄積データをマトリクスで集計します」を復活(N-9 の長文は戻していない) |
| F-100 | 頭拍限定の原因 = `metroDotR` の `(isHead ? e : 0)`(ringBeatEmphasis 自体は毎拍 e>0 を返していた)。e をそのまま使い**毎拍膨らむ**へ。強弱は係数(頭1/他0.55)が担い、膨らみ上限 1.8 据え置き。`METRO_SCALE` 1.2→**1.1**(サンプル B 列の --k も 1.1 へ上書き注記) |
| F-101 | `usePersistedState` にモジュールスコープ Map キャッシュ。null が正当な保存値(selectedReedId)なので `??` でなく `has()` 判定。**F-93 は解消しない**(上の F-93 の項を見ること。仕様の誤りとして報告) |
| F-102 | 「…」に「リード番号を変更」(3件目)。モード中はタイルタップで1枚ぶんの番号シート(作法は ReedMoreMenu と同値)。開いて閉じただけでは書かない(保存値と表示値の両方と比較)。個別ページの破線の下線は削除(編集機能は不変)。出口の文言は「完了」(編集は確定済みなのでキャンセルと出さない) |
| F-103 | `SAX_CONCERT_RANGE` の highMidi を全機種 +4(37音)。運指表・音名軸・チューナー判定はすべてここから導かれるので変更は1箇所。下限不変 |
| F-104 | **本人報告の「音の変わり目で重くなる」は Chrome では再現せず**(下記)。確実に測れた恒常負荷 = tick が毎フレーム確保していた 2×Float32Array(8192) = 64KB/フレームを使い回しへ(計測値は 1bit も不変)。真因の確定は実機待ち |

### 実測（375×812 Chrome。IndexedDB へ `f97test-1..4` を注入して実操作）

- **F-97**: 削除モードで全3行にチェックボックス(20×20・display 切替)+字下げ(dateLeft 46)。
  先頭行 347×47 の全可視域 16,309 点走査で **hit 16,309 / miss 0**。モード外は display:none で
  dateLeft 16 / rowH 47(従来と同一)
- **F-98**: セッション詳細の --c-sunken 地の入力欄 **0**。日付・メモとも bg 透明+下線 1px #E9ECF0・
  radius 0。ラベル5行の left 全て 14 / 値の左端 54 に揃う(初稿の 50 は誤記。審査役の実測で訂正)。解説文2つとも**画面から消えている**。
  scrollWidth 375 = 375
- **F-99**: 表題 15px rgb(23,69,133) 700 / 説明 12px rgb(141,149,161)  375px では2行に折り返す(初稿「1行」は誤り。仕様の1〜2行の範囲内)
- **F-100**: MetroPendulum の実ループを駆動して strobe 実測(20ms 間引き150標本): 点の最大 r =
  **頭拍 8.81 / 2拍目 7.19 / 3拍目 7.10 / 4拍目 7.08**(基準 r 5)。実装値 5×(1+0.8×0.55)=**7.2** /
  頭 5×(1+0.8×1)=9.0 と一致(頭 8.81 は間引きが山を踏み外した分)。svg 実寸 165×28.6 = 150×1.1 /
  拍子 13.2px = 12×1.1
- **F-101**: myDataRange=3ヶ月でタブ往復し、MutationObserver で再マウント後の DOM 変異を全部拾って
  ラベル出現列が **[3ヶ月]のみ**(既定の1ヶ月は一度も出ない)
- **F-103**: チューナー実測(偽マイク) 1113.8Hz(=アルト新上限 D♭6) → **C♯6 と判定**(旧上限では棄却域)。
  音名軸グラフに semitone 36 の点が **x=281.0** = ラベル実測から導いた x(36)(E♭3=77.94・
  1半音=5.9722・軸原点 66.0)に一致 = 軸終端が+4半音
- **F-104 プロファイル**(実 AudioContext + オシレータ偽マイク・MessageChannel 駆動 rAF・実 React):
  定常音 vs 150ms ごとの音替わり(B♭3⇄E♭4 を実検出)で **tick 平均 0.90ms vs 0.87ms /
  他タスク(描画等)の隙間 4.12ms vs 2.25ms = 音替わりで増えない**(むしろ定常の方が音色計測ぶん重い。
  音替わり後 140ms の FFT 抑制が実際に効いている)。ヒープ増加は修正前実測 24.8KB/tick(GCノイズ込み)、
  修正で 64KB/フレームの確保が構造的に 0 に
- 検証データは**キー単位で個別削除**して原状復帰(sessions f97test-1..4 削除→0件 / 検証中に増えた
  kv キー myDataRange・showMetroPanel・metro系5キーを削除 / reeds は shinsa-reed-1 をスナップショット値へ
  キー単位で書き戻し / 誤って作った空 DB `ficus-db` も削除。復帰後 kv 10 キー・全値が着手時
  スナップショットと一致を確認。`clear()` 不使用)。マイク覆いは非表示化せず(要素越しに JS で操作)

### 検査の書き換え（すべて本人指示 F-97〜F-104 を理由とする。黙って消していない）

- 検証16: 最高音の期待値を +4 半音の音名で書き下ろし(A♭6/D♭6/A♭5/D♭5)
- 拍の膨らみ: 「小節頭でだけ膨らむ」→「毎拍膨らむ+頭がより大きい+OFFは全拍同じ」(F-100)
- 17.8: 例外に「下線作法の欄」を追加し件数(2箇所・datetime-local を含む)で固定(F-98)
- 18.9: 「除外率の1行がある」→「解説文が無い」へ反転(F-98。過渡除外の実行検査 18.5〜18.7 は不変)
- F-80 の「…」2件 → F-102 の3件へ / canReorder に !noDrag / #番号の破線は「無い」へ反転
- F-55 の 2em/コロン検査 → F-98 の 3em/1行1情報の検査へ置換
- 29.1: 「PIVOT が無い」→「表題+1行説明がある(長文は無いまま)」へ反転(F-99)
- 21.1: 33音 → 37音(F-103)
- 新設 検証30(F-97 常設checkbox / F-101 キャッシュ / F-103 一貫性 / F-104 バッファ使い回し)

### 実機で確かめてほしいこと（Chrome では判定不能）

- **F-97 が実機で直っているか**(現行ビルドで再現しないため、修正は機序仮説への構造的対処。
  まず PWA を開き直して最新ビルドか確かめてから、削除モードで先頭行を見る)
- **F-104 の「音の変わり目でメトロノームが重くなる」が再発するか**(Chrome では音替わりの追加負荷は
  実測ゼロ。GC 由来なら今回の割当て削減が効くはず。再発するなら iOS 固有の機序なので採取器を検討)
- F-98 の datetime-local の下線が iOS でも出るか(§6.7 の appearance 系の既知の癖の隣。F-40 とセット)
- F-100 の全拍強調の体感(1.1 倍への縮小との組み合わせ)
- F-102 の番号シートのキーボード挙動(実機のフォーカスとシートの重なり)

### 踏んだ罠（次の担当へ）

1. **Browser pane の隠しタブでは setInterval/型どおりの再現が座礁しがち**: 音声を鳴らしている間は
   タイマーが動く。rAF は MessageChannel ポンプで代替できるが、**ポンプを回しっぱなしにすると
   computer ツールのクリックがタイムアウトする**(メインスレッド飽和)。計測窓の外では必ず止める。
2. **音名の innerText は「B\n♭\n3」と行が割れる**。`/^[A-G][♯♭]?\d/m` では拾えず「表示されていない」と
   誤読した(実際は表示されていた)。改行を許す正規表現で読むこと。
3. 8192 点(≒170ms)の解析窓は 150ms 間隔の音替わりでは常に2音の混合になる。**速い受け渡しでの
   検出挙動は窓長で決まる**(F-104 の実機採取をするならこの点を思い出すこと)。
4. usePersistedState は「保存値が無いキーは書かれるまで IndexedDB に現れない」。検証操作で
   showMetroPanel 等のキーが**新規に生えてしまう**ので、着手時スナップショットは「キーの集合」まで
   取り、終了時に差分キーを個別削除すること(今回それで原状復帰した)。

## 完了: N-8 ヒーローの折れ線化・ピッチカード廃止・直近日フォールバック（2026/08/16）

本人指示（原文は `design/N8-SPEC.md`）: 濃い青のカードの折れ線を音ごとの平均の折れ線に変更／
単独の音ごとの平均グラフはグラフごと削除／目安は表示不要（期間平均と今日の2本だけ）／
当日データが無ければ直近の記録のある日の平均を表示。

| | 内容 |
|---|---|
| ヒーロー | スパークライン（時系列）廃止。同じ場所に音名軸の折れ線2本（`NoteAxisLineChart` の新引数 `hero`）。凡例は正典 `.hero .legend` / `.sw` の色棒（`SeriesSwatch` は白地を敷くため紺上では使えない） |
| 配色 | ヒーローの既存色のみ: 期間 `#B9C9E4`（=`--c-accent-line` と同値・3px）/ 今日 `#FFFFFF`（=`--c-on-accent` と同値・2px）/ 0の破線 `#3D5F94`（§1.8 の "4 3"）/ 軸ラベル `#9DB3D6`。定数 `HERO_CHART_*` の3つ。**軸要素は最小限の判断**: 水平グリッド無し・0の破線のみ・目盛3値・音名ラベルは N-6 の noteFocus（E♭3/4/5）のまま・中央E♭のガイド線と強調は hero では描かない（破線ガイドの `--c-accent-line` が期間線と同色で紛れるため） |
| ピッチカード | N-7 の `.pitchcard` をグラフごと削除。§6.0「面は1画面に1枚」の本人委任例外（2枚）も解消 |
| 目安 | `selectedIdeal` の破線と Δ を My Data の全グラフから撤去（`MetricRow` から selectedIdeal/idealKey/idealDiffText、`MY_DATA_METRICS` から idealKey、`idealAvgForFrames` は読み手ゼロで削除）。**他画面（リード詳細・セッション詳細）の目安はそのまま**（検査 28.4 が両方向に固定 — **審査で訂正**: 初版の 28.4 はセッション詳細側が「コンポーネント内のどこかに綴りがあれば」通る形で、指標カードから selectedIdeal を外す変異が生存した。統括が呼び出しに隣接する錨へ直し撃墜を確認）。「目安未設定」の告知は機能の状態表示として残した |
| 直近日 | 純関数 `myDataTodayOrLatestFrames(sessions, now)` → `{ frames, label }`。自分+選択楽器のフレームを持つ最新 recordedAt の**ローカル暦日**（localDayKey）の全フレーム。ラベルは今日なら「今日」、違えば `M/D`（例 `8/15`）、記録ゼロは `[]`+「今日」。ヒーローの数字・濃い線・3行ヘッダ・凡例の**すべて**が戻り値を使う（「今日」の綴りは描画側に無い＝検査 28.2） |
| 正典 | `design/data-tab-final.html` を N-8 版へ改訂（理由コメント入り。検査 27/28 が期待値をここから読む） |

build 成功 / **PASS 6274・FAIL 0**（着手前 6242）。検証ゲート **4.2〜4.4 秒**。
**変異試験 35 件を一意名の複製ツリーで単独逐次・全 35 件 KILLED / 生存 0**
（前周4件生存した MetricRow→グラフの引数固定・フォールバック純関数の迂回・ラベルの直書き・
目安を戻す変異・正典側だけ変える変異を名指しで撃った）。

### 実測（375×812 Chrome。IndexedDB へ `n8test-*` 3件を注入し3状態）

- **今日あり**: ヒーロー左上「今日」/ 数字 +2.0¢（注入 +1,+3 の平均と一致）/ 面の中に svg 1つ・
  polyline 薄 rgb(185,201,228) 3px + 白 rgb(255,255,255) 2px / 0線 rgb(61,95,148) 破線 4,3 /
  軸文字 rgb(157,179,214) / 凡例「1ヶ月・今日」右寄せ / `#F6F8FB`（旧ピッチカード）の要素 **0** /
  scrollWidth 375 = clientWidth 375
- **今日なし直近あり**: 左上ラベル **8/11** / 数字 -2.0¢（8/11 の2セッション36フレームの平均と一致）/
  凡例「1ヶ月・8/11」/ 3行ヘッダすべて「8/11 …」/ 横あふれ 0
- **記録ゼロ**: ヒーロー「平均差分 —¢」・**svg 0**（折れ線ごと出ない）/ 行は「今日 — · 1ヶ月 —」/
  空文言（この期間の…/まだ記録がありません）は従来どおり
- **目安**: selectedIdeal 設定済みの状態で My Data の破線（--c-ink-3 rgb(141,149,161)）**0本**、
  セッション詳細の HNR グラフには破線 5 本 + 凡例「実測/目安」= 他画面は生きている

### 検査の書き換え（すべて本人指示 N-8 を理由とする。黙って消していない）

- 6.8: スパークラインの5検査 → 廃止。「sparkVals が残っていない」「MyDataSection は svg を
  直接描かない」へ。dayFrames/dayVal への綴り替え
- 21.5 / 21.8: 綴り替えのみ（todayVal/todayMetrics/todayErr → dayVal/dayMetrics/dayErr。主張は同一）
- 26.2: 「紺の面の外」→ **中に居る**（<div> 深さ ≥1）へ反転。「Δ は行ではなくグラフへ」→ 廃止
  （部品側の仕組みは他画面用として固定継続）
- 27.0/27.2: `.pitchcard`/`.chartlbl` の検査 → 「正典に無い」「実装に #F6F8FB が無い」へ。
  凡例は `.legend`/`.hero .legend`/`.sw` との突き合わせへ
- 27.3〜27.5: myDataChartSeries の第4引数 dayLabel・第5引数 styles、metricRowHeader の
  戻りキー dayNum/dayUnit へ追随。「今日」の綴りが関数に無いことを追加
- 新設 検証28（**35件**。検証28を丸ごと外すと 6239 に落ちることで審査役が実測 — 初稿の「36件 = 6238 → 6274」は測っていない数字で誤り）: 直近日フォールバックの実行検査（暦日境界・同日合算・
  M/D 書式・localDayKey 強制）/ 描画側の配線 / hero 変形と配色の正典突き合わせ / 目安の撤去

### 踏んだ罠（次の担当へ）

1. **変異の find 文字列が別の場所に先に命中する。** `String.replace` は最初の1件しか置換しない。
   `{ key: "hnrDb", label: "HNR", … }` は REED_COMPARE_METRICS と MY_DATA_METRICS に**同一綴り**で
   存在し、初版の M29 は前者に当たって「生存」に見えた（検査の穴ではなく変異の空振り）。
   一意な綴り（配列の開き括弧を含める等）で当て直して KILLED を確認。**変異は当たり先の一意性を
   先に確かめること。**
2. **このプロファイルの基準は「kv 0」ではなかった。** 着手時に kv 16 キー・`n7shinsa-*` の
   リード3枚と理想値プロファイル1件・selectedIdealId 設定済みが残っていた（N-7 の審査データの
   残置と思われる）。他人のデータは消さず、**着手時スナップショットを取り、自分が入れた
   `n8test-*` のセッション id だけを削除**して原状（sessions 0 / kv 16 キー・値も一致を確認）へ
   復帰した。selectedIdealId が設定済みだったのは目安の実測にはむしろ好都合だった。
3. **Browser pane では click() 直後の同期読みで React の再描画が見えないことがある。**
   セッション詳細のグラフ検査で最初 0 本と出たのは測るのが早すぎただけ。**次の呼び出し
   （別 tick）で読み直す**こと。
4. NoteAxisLineChart は連続しない semitoneIndex を**点（1点ポリライン+円）**で描く。注入データが
   飛び飛びの音だと「折れ線」ではなく点列に見えるが、これは N-7 からの既存仕様（欠けはギャップ）。

### 発見した既存の問題（N-8 では触っていない・起票）

- **N-8a: 行ヘッダの差分が浮動小数の誤差で「+0.0」と出得る**（下記）

### 実機で確かめてほしいこと（Chrome では判定不能）

- 紺地での2本線の見え方（特に薄い #B9C9E4 と 0 破線 #3D5F94 の判別、直射日光下）
- ヒーローが縦に伸びた（実測 h 295px）ことによる1画面の収まりの体感

---

## 完了: N-9 セッション詳細 + 分析(PIVOT)の北極星化（2026/08/16）

本人指示（原文は `design/N9-SPEC.md`）: 「個別セッションないのページとPIVOTがまだ旧式デザインなので
いい感じにほかのページと統一して」「なるべく要素を減らすという点も忘れずに」。

| | 内容 |
|---|---|
| 箱の廃止 | セッション詳細の `.card` 3枚 + PhraseTimeline の `.card` 3枚 + PIVOT の `.card` 1枚・フィルターの枠箱・軸セレクタの `.tile` 3枚・表見出しの地色（--c-sunk）をすべて廃止。白地+罫（--c-rule 上辺1本）で群を分ける（計測/リード/My Data と同じ文法）。残る `.card` は計測タブの詳細カード1枚だけ |
| 罫の配置 | セッション詳細 = セッション平均 / 音階ごとの平均の2本（セッション情報は直前に「一覧に戻る」の区切りがあるので罫なし = no-top-rule と同じ判断）。PhraseTimeline = タイムライン / ドリルダウンの2本。分析 = 軸セレクタ群 / グラフ群の2本 |
| select 類 | 新部品 `PlainSelect`（素のテキスト + ▾。F-72 / PerformerSelector bare と同じ「値は span・透明なネイティブ select を重ねる」構造。**中身のネイティブ select はそのまま**）。呼び出しは5箇所: タイムラインの表示/基準/別セッション・リード紐付け・PIVOT の次元。奏者は PerformerSelector の bare 枝を流用（selectId="session-performer-select"）。軸セレクタ3枚は既存の `pivot-axis-select`（枠なし・ネイビー太字）のまま |
| 要素を減らす | 表題「PIVOT」・冒頭の説明段落（「…マトリクスで俯瞰します」）・グラフ下の軸の説明文を削除（本人:「長いテキストは趣旨がずれてる」）。フィルター削除 × の食い出し相殺（marginTop/Bottom: -8）は、隣が 44px の PlainSelect になり不要になったため撤去（残すと × の中心だけ 8px 上にずれる。実測 中心差 0px） |
| セッション平均 | `.tile` → リード個体詳細と同じ bare(numrow)。value は fmt の結果だけ（単位は部品が描く）。**目安（selectedIdeal / idealKey）は残した**（N-8 で消したのは My Data 側だけ。検査 28.4） |
| ドリルダウン | MetricCard の `.tile` 2枚 → BARE_ROW_STYLES.numrow の数字の列（寸法は写さず参照）。一致度の機能色は数値の色が担う |
| 表見出し | th の地色を落とし罫だけに。**fontWeight 400 を明示**（UA 既定の太字を外した = ラベルの文法へ。意図した変更） |

build 成功 / **PASS 6331・FAIL 0**（着手前 6274）。検証ゲート **4.2〜4.4 秒**。
**変異試験 22 件を一意名の複製ツリー（n9mut-6fe181-a）で単独逐次・22 KILLED / 生存 0**
（初回 M17「PivotLineChart の改名」が生存 → 29.4 の `/<PivotLineChart/` が**接頭辞一致**で
すり抜けていた。`\b` を足して再実行し全滅。SetAsIdealButton / PerformerSelector の錨も同型で補強）。

### 実測（375×812 Chrome。`n9test-a/b` 2セッション + リード1枚を注入して実操作）

- **箱**: 両画面とも「4辺そろった可視枠の非フォーム要素」0 /「白でない地の非フォーム要素」はセッション詳細 0・分析側はヒーロー(紺)・Great ピル・凡例色棒・下部ナビの半透明地の4つ（すべて My Data / ナビ側の既存要素 = 対象外）。B型入力欄（--c-sunken）と A型ピル（値チップ等）は操作の型の正当な例外
- **横あふれ 0**（scrollWidth 375 = clientWidth 375。両画面）
- **44px**（elementFromPoint 整数1px刻み）: フィルター削除 × 44×44 / 次元 PlainSelect 80×44 / 子タブ「分析」45×44 / リード・表示・基準の PlainSelect 縦44。奏者(bare) 縦28 = 計測タブの F-72 実装と同値（流用）。＋条件を追加 102×24・値チップ 47×20 は**HEAD からの既存値**（N-9 で触っていない）
- **機能残存（実操作）**: 日時編集→recordedAt 反映 / 目安に設定→命名→保存→「★ 目安設定中」+ 表の目安との差 0.0¢ + 指標グラフに目安破線3本 / 奏者変更+追加フロー（N9奏者を追加→自分へ戻す） / リード紐付け（PlainSelect の表示値も追従） / メモ（focusout で保存） / 指標切替で折れ線の points が変化・音量時は基準から絶対値が消える / 基準=別セッション→候補選択→位置合わせの1行 / スクラブ→ドリルダウン（t=3.00s・一致度2列・boxes 0） / bare タップ→音名軸グラフ / PIVOT: 条件追加→2行・次元を音名へ→音域帯「低音域」でまとめ選択・値チップ toggle・条件削除・軸3枚切替で行ラベルと軸題が変化・測度7種・指標(色分け)で凡例・**子タブまたぎ / 下部ナビまたぎの両方で軸とフィルタが保持**
- 検証データは**キー単位で個別削除**して原状復帰（着手時: kv 0 / sessions 0 / reeds 0。削除: sessions n9test-a/b・kv idealProfiles/performers/reeds/selectedIdealId → 復帰後 kv 0 / sessions 0 を確認。`clear()` 不使用）。マイクエラーの覆いは計測中だけ display:none にして復元

### 検査の書き換え（すべて本人指示 N-9 を理由とする。黙って消していない）

- 6節: `.card`/`.tile`/`.tile-row` の下限 8/3/4 → 1/2/1（下げたぶんは「3関数に面のクラスが無い」を名指しで固定）
- F-72: 「bare は計測タブの1箇所だけ / セッション詳細は HEAD のまま」→ 2箇所とも bare + 固有 selectId。▾ の綴り 6→7・地なし select 3→4（増分はどちらも PlainSelect の1箇所）
- F-57: 「marginTop/Bottom: -8 で相殺」→ 「相殺を持たない + 隣は PlainSelect」
- 25.6: BARE_OWNERS {ReedEvaluationDetail} → {ReedEvaluationDetail, SessionDetailView}（錨は呼び出しに隣接）
- 新設 検証29（**51件**。検証29 の節を丸ごと外した一時複製で実測 PASS 6280 → 本体 6331 の差）: 削除の固定 / 罫の本数と隣接 / PlainSelect の構造と5箇所の配線 / 機能残存の集合20件

### 実機で確かめてほしいこと（Chrome では判定不能）

- PlainSelect（透明 select 重ね）の iOS での実寸とタップ挙動（F-72 と同じ構造だが画面が増えた）
- 罫だけの群分けの読みやすさ（特に分析子タブのフィルター〜軸〜グラフの3群）
- 音階ごとの平均の表見出し（太字→400）の可読性

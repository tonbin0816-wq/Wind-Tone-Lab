# 統一の棚卸し（2026/09/08）

> **着手状況（2026/09/08 本人裁定を反映）**
> - 0-1 トークン … **色・文字サイズとも完了**。色は引用符つき hex 0 件。サイズは 31 箇所を7段へ。
>   グラフは実測で問題なし（音名は12pxでも 22.3、ラベル間隔は 45.3）。 は元から 12 だった。
>   **触っていないもの**: 開発用の診断パネル 17 箇所（ のときだけ出る）、テンポの 76px、
>   および**正典（モック）が実寸を指定している 12 箇所**（12.5px の .selpill/.ckrow ×7 / 26px の累計 /
>   10px の PlainSelect ラベル / 10.5px のメモのラベル / 11px の条件チップ / 9px の ▾）。
>   **2026/09/08 裁定「A」= 正典が勝つ。** §4.1 に例外の表を書き足した。> - 0-2 9/6の指示 … **中黒は完了**（7箇所）。**戻るは完了**（記号も形も。月送りは対象外）。見出しは波及先が無かった（App 側にタブ名と重複する見出しは1つも無い）
> - 0-3 部品の写し … **カード完了**（JS の写しを消して index.css の .card / .card-list / .rowcard へ）。**シート完了**（器5枚→2枚。残る2枚は理由つき）。ScrollPicker を portal にして回避策を1つ消した
> - A/B/C/D の個別項目は、上の裁定で片付いたものを含めまだ更新していない。次に触るときはここから読み直すこと。
>
> **2026/09/09 本人裁定と、その施工**
> - **A3** 値が無いところは横棒「—」に統一 … **完了**。絞り込みの条件名・説明の文・操作の名前は
>   値ではないので対象外(設計書の表記の節に明文化)
> - **A8** 指標の名前は「音程」に統一 … **完了**。N-2 で決めた「平均差分」を上書きした。
>   正典 `design/north-star-measure.html` の `.l` も同時に直してある(pitch-test が正典から
>   語彙を作るので、片方だけ直すと落ちる)
> - **A12** 数は「計測◯件」 … **完了**。累計カードだけは3つの綴りの並びを崩さないよう「計測件数」
> - **A25** 期間は「1週間 / 1ヶ月 / 1年 / すべて」 … **語は完了**。
>   **⚠ 数え方は揃っていない。** My Data は「直近N日」のローリング、コミュニティは暦(週は週初から /
>   月は1日から / 年は1/1から)。**同じ語で数え方が違う状態**なので、どちらへ寄せるかの裁定が要る
> - **A5 / A6 / A7 / A17 / A18 / A20 / A26** … **完了**(括弧は半角、数と単位の間は詰める、
>   aria は表示の語に合わせる、注意書きは句点で閉じる、「目安基準」→「目安」)
> - **A11 日付書式** … **直す先が無かった。** 「4通り」はいずれもそれぞれの正典が持っている。
>   月見出しの `yyyy/m` は凍結仕様 `design/D10-SPEC.md §2.2` が名指しで持つ例外(0埋めすると
>   正典 `design/canvas/S1.dc.html` の「2026/8」と食い違う)。現場にコメントを残した
> - **A21 助数詞** … **指示のとおりには直せなかった。** 「両方とも枚に」だったが、
>   `App.jsx:12511` が数えているのは `pivotCol` の値であって**リードではない**
>   (`PIVOT_DIMENSIONS` は音名・音域帯・奏者・メモ等12種で、リードはそのうち3つだけ)。
>   「枚」にすると分析軸に奏者を選んでいるとき「残り3枚」と出る。同じ文の中に既にある
>   「見分けのつく系列は6本まで」に合わせて**「本」**にした(新しい語は作っていない)。
>   `App.jsx:11377` のほうは対象が本当にリード個体なので**「枚」のまま**
> - **B10 / C14-16 / D6 / D7** … 文章では判断できないという指摘を受け、**実測どおりのモックを
>   キャンバスの面「統一の比較」に置いた**(`design/canvas/unify.mjs` が生成)。裁定待ち
> - **D8 保存後の遷移** … 絵にならないので文で: コミュニティで目安を設定したときだけ画面が移る
>   (マイページへ)。他の4つ(セッションの編集・リードの評価・目安の設定・人物紹介の目安設定)は
>   その場に留まる。1 対 4。裁定待ち
>
> **2026/09/09 第2陣の裁定**
> - **B10** ページ左右余白 … **30px に寄せる**。コミュニティの +16 は撤去済み（子タブの包みも同時に）。
>   **リードの +10 は保留** ── 正典 `.rlist { padding: 0 24px }` が指定していて、検査が4箇所で
>   突き合わせている。寄せるには正典を書き換えることになり、裁定「A」（正典が勝つ）と衝突する
> - **C14 / C15 / C16 / D6** シート … **①（下寄せ+つまみ）に統一。完了。**
>   4枚（リードの評価 / 目安に設定 / 人をタップ / バックアップ）を `BottomSheet` へ畳んだ。
>   閉じ方は6通り→**4つが常に揃う**形になった。体系 §4.5 に書いた
> - **D7** 主要動作の置き場 … **すべて右下に浮かせる**。着手中
> - **D8** 保存後の遷移 … **そのままでよい**（コミュニティだけ画面を移す。1対4のまま）
> - **A25** 期間の数え方 … 語は揃ったが数え方は未決。暦（コミュニティ）が `isStatsFresh` の
>   足場になっているのでローリングへ寄せられない。本人に4案を提示して回答待ち
>
> **2026/09/09 第3陣の裁定（これで A/B/C/D の主要項目は片付いた）**
> - **B10** リードも 30 へ … **完了（案ア）。正典 `.rlist` / `.subtabs` を 24px → 14px に
>   書き換えた。** これは通常と逆向き（普段は実装を正典に合わせる）。差分を足す定数3つと
>   包みの div 2枚が消え、リードタブは左右余白の値を1つも持たなくなった。
>   唯一の答えは `index.css` の `--page-side-pad`。実測で子タブの文字が My Data と同じ 14px
> - **D7** 人物紹介 … **完了。面の右下に貼り付け**（`position: sticky; bottom: 0`）。
>   シート（重なり順60）の中では画面右下のフロート（45）が暗幕に隠れるため。体系 §4.5 に書いた
> - **D7** 「編集」 … **ヘッダのまま**（裁定）。主要動作は1画面に1つ
> - **A25** … **C案。語を分け直した**（コミュニティは今週/今月/今年へ差し戻し）。
>   数え方が違うまま語だけ揃えると嘘になる。体系 §6.0 の表記に表で書いた
> - **A12 の残り** … 画面に出る「セッション」7箇所を「計測」へ。同じ物を2つの語で呼ばない
>
> **検査で1つ穴が見つかった。** JSX が壊れて `vite build` が落ちる状態でも
> `pitch-test` は 7292 件すべて緑だった（テキストで綴りを見る検査で、構文解析はしていない）。
> **3つのゲートは全部回すこと** ── `vitest` は 323 → 309 に減って気づけた。
>
> **2026/09/09 第4陣 — 掃除と、測り直しで見つかったやり残し**
> - **A1 中黒の廃止はやり残しがあった。** 1周目で直したのは DetailHeader の1行メタ7箇所だけで、
>   画面に出る区切りの `·` が3箇所残っていた(計測タブの「Alto · 442Hz」/ カレンダーの
>   「◯時間 · ◯日」/ My Data の集計範囲の行)。同じ裁定なので余白(9px)へ置き換えて完了。
>   **文中の並列の中黒**(「音量・音色」「総評・厚さ・バランス」)は日本語の句読点であって
>   区切り記号ではないので対象外 ── ここを混同すると日本語が壊れる
> - **死んだコード `tallyGear` を撤去**(本人裁定「不要と思うものは削除でよし」)。
>   2段の内訳へ作り替えたとき `tallyGearByBrand` / `tallyGearModels` が取って代わり、
>   呼んでいたのは自分の試験だけになっていた。試験9件も一緒に落とした(323 → 314)。
>   隣の試験にあった「1段版を壊していない」という対比も、相手が消えたので落とした
> - **`videos/*/.thumbnails/` を git から外した**(104K / 22ファイル)。作り直せる生成物。
>   .gitignore に追加
>
> **測り直しで分かったこと**: 状況欄が「完了」と書いていても、**綴りで数え直すと残っていることがある**。
> A1 がそれ。報告の前に必ず測ること(このときは行末コメントまで数えてしまい、
> 1度目の測定は 193 件という無意味な数字が出た。コメントを剥がして 14 件、
> うち区切りは4件、という順で絞った)。
>
> **検査を8箇所、緩めずに向け直した。** シートの器の性質（暗幕・影・fixed・portal・暗幕タップ）は
> 各シートの持ち物ではなく `BottomSheet` の持ち物になったので、見る先を器へ移した。
> 変異試験4件（器の暗幕の濃さ / 器の角丸を四方囲みへ / 評価が自前の暗幕を持ち直す /
> CommunityTab に surf-card を増やす）で**すべて落ちること**を確認済み。7287 → 7290 件。
>
> **緑は「文言が正しい」証拠ではない。** 今回直した文言のほとんどは検査に錨が無い
> (「診断(計器)」「倍音構成(実測 / 目安)」「削除するセッションを選ぶ」など)。
> 変異試験で錨があると確かめられたのは2件だけ。
> **実例**: 2026/09/08 の中黒廃止で `DetailHeader` に入れたコメント2行が `//` で書かれていて、
> **JSX の子として画面に文字で出ていた**（リード個体詳細とセッション詳細のタイトルの下）。
> 検査 7287 件は全部緑のまま、本番にも出ていた。2026/09/09 に修正。JSX の中では `//` は
> コメントにならない。同じ形の事故が他に無いことは走査して確かめた（0件）。


アプリ全体（計測・リード・My Data・分析・詳細画面・シート・コミュニティ）を、
**テキスト / 色・寸法 / UI部品 / 画面遷移** の4観点で読み、「同じ役目なのに作りが違う」箇所を
根拠つきで列挙したもの。**提案ではなく事実の一覧。**どちらに揃えるかは本人の裁定で決める。

- 時点: コミット `6669839`（2026/09/08）
- 方法: 観点ごとに1本ずつ、grep で当たりをつけて該当行だけ読む。行番号は実在のものだけ。
  抜き取り検査 11 件（各表から）はすべて実在した。
- 件数の数え方: hex はコメント行を除いた実コード行。fontSize / borderRadius / gap は `grep -oE` の出現数。

---

## 0. まず決めること（根本が3つ）

個別の項目の前に、下の3つを決めると多くの項目が自動的に片付く。

| # | 根本 | 実態 | 決めること |
|---|---|---|---|
| 0-1 | **2つの方言** | `src/App.jsx` は値を直書き（fontSize 数値 147 件 / トークン 35 件、hex 86 件 18 種）、`src/community/*` はほぼ全部トークン参照（hex は `#fff` 2 件のみ）。同じ見た目を2つの書き方で持っている | App.jsx をトークン参照に寄せるか（設計書 §4.1 が吸収先を明記済み）、現状維持か |
| 0-2 | **9/6 の本人指示がコミュニティにだけ効いている** | 「中黒を使わない・区切りは余白」「見出しを置かない」「戻るは `< 行き先`」は 2026/09/06 の指示で、`src/community` にはあるが App.jsx 側（`·` の区切り、`‹ 一覧`、eyebrow 見出し）には及んでいない | 9/6 の指示を全画面に広げるか、コミュニティ限定にするか |
| 0-3 | **部品の写し** | BottomSheet の枠を4箇所が書き写し（`App.jsx:13520` 本体 / `10176` / `10237` / `10470` / `CommunityTab.jsx:243`）、カード寸法を CSS と JS の両方で定義（`index.css:489` と `screens.jsx:26`）、A型チップを2ファイルで同値実装（`screens.jsx:81` と `CommunityTab.jsx:516`） | 写しを1つに畳むか。畳むなら順序（シート → カード → チップ） |

---

## A. テキスト・語彙（利用者が読む文字）

| # | 項目 | 場所A | 場所B | 備考 |
|---|---|---|---|---|
| A1 | 区切り記号が `·` / `・` / 余白 / 全角空白 の4系統 | `App.jsx:13756` DetailHeader の `·`（`--c-line-strong`）、`App.jsx:15256` `.join(" · ")`、`backup/BackupPanel.jsx:135` | `CommunityTab.jsx:1009` `listOrDash` が `"・"`、`App.jsx:11571` `" ・ "`、`App.jsx:8697` `" ・ 平均アタック"` | `screens.jsx:224` / `970`「中黒を使わない。区切りは余白」（本人指示）。`screens.jsx:1158` は全角空白 |
| A2 | 「戻る」の記号が `‹` / `<` / `←` | `App.jsx:12176` `‹ 一覧`、`15425` `‹ 一覧`、`15142` `‹ My Data` | `screens.jsx:1060` `< 一覧` / `< 音のデータ`、`592` `< メーカー`、`App.jsx:7052` `←①〜⑥` | `screens.jsx:1051`「`< 一覧`（2026/09/06 本人指定）」。月送りは `App.jsx:13909` `‹` |
| A3 | 「値が無い」の語が9通り | `App.jsx:12320`/`13653`/`15271` 未紐付け、`10430` 未測定、`12333` 未評価 | `screens.jsx:215`/`954`、`CommunityTab.jsx:688` 未選択、`App.jsx:10799` 開封日 未設定、`14323` 目安未設定、`8782` 未登録、`8151` (未選択)、`12369` （メモなし）、`CommunityTab.jsx:1009`/`screens.jsx:978` — | |
| A4 | 空状態の「まだ」の有無 | `App.jsx:10779` まだリードが登録されていません、`12027`、`11820`、`12215`、`15019` | `screens.jsx:1103` 楽器の組は登録されていません（まだ無し）と `1118` この楽器の登録はまだありません が**同じ PersonSheet 内** | `App.jsx:14295`、`15210` も「まだ」無し |
| A5 | 括弧が全角 `（）` と半角 `()` | `App.jsx:10030` `…箱（…枚）を削除しますか？(元に戻せません)`（**1文に両方**） | 全角: `App.jsx:2700, 7033, 7043, 8123, 11529, 12369, 12740`、`CommunityTab.jsx:43, 54`。半角: `App.jsx:10036, 12394, 14582`、`screens.jsx:1158` | |
| A6 | 単位の括弧 | `App.jsx:11815` `（¢）` 全角 | `App.jsx:12394` `平均差分(¢)`、`screens.jsx:1158` `(Hz)` 半角 | `App.jsx:11425` / `12770` / `screens.jsx:695` は unit を別フィールドで持つ |
| A7 | 数値と単位のスペース | `App.jsx:14217` `{n} 件`、`10430` `{n} セッション`、`13905` `時間 · 日`、`8784` `Hz` | `App.jsx:13962` `{n}件`、`9410` `{n}セッション`、`10429` `{n}日`、`8785` `Hz`、`8697` `ms` | 同じ「セッション件数」で有/無が併存 |
| A8 | 同じ指標 `pitchCentsSigned` のラベル | `App.jsx:11425` / `12770` 平均差分 | `screens.jsx:697` **音程** | `DESIGN-SYSTEM.md:1049`「ピッチ誤差 → 平均差分」 |
| A9 | ¢ の符号と 0 の綴りが3式 | `App.jsx:11394` `formatSignedCents`（+ 前置、`-0.0` → `0.0`） | `screens.jsx:758` 目盛は `v.toFixed(digits)`（符号なし）、`App.jsx:12394` `(v > 0 ? "+" : "") + v.toFixed(1)`（`-0.0` 対策なし） | 本人の好み: ピッチ0は「0.0¢」（符号なし） |
| A10 | 母数の書き方が4通り | `screens.jsx:609` `n = 〇人` | `857` 目安を公開している〇人、`178` 上位〇人、`668` 〇人 | 「n =」は1箇所だけ |
| A11 | 日付書式が4通り | `App.jsx:1994` yyyy/mm/dd（0埋め） | `13262` yyyy/m（0埋めなし）、`2006` m月d日、`9037`/`12823` m/d | `DESIGN-SYSTEM.md:1051`「yyyy/mm/dd」 |
| A12 | セッション件数の呼び名 | `App.jsx:14161` 計測回数（回）、`14217` すべてのセッション 〇件 | `screens.jsx:1158` 録音〇回、`BackupPanel.jsx:66` 〇回の計測、`App.jsx:9410`/`10430` 〇セッション | 計測 / 録音 / セッション が同一量 |
| A13 | 見出し「編集」で実行ボタンが「作成」 | `CommunityTab.jsx:890` `プロフィールを編集` | `CommunityTab.jsx:985` `作成`（`initial` の有無で分岐していない） | |
| A14 | 取り消しが「やめる」と「キャンセル」 | `CommunityTab.jsx:994` やめる | `App.jsx:10109`、`15149` キャンセル | |
| A15 | 見出し「編集」で実行が「変更」 | `App.jsx:10451` `箱を編集` | `App.jsx:10447` `この箱を変更`、`10174` `リード番号を変更` | |
| A16 | 「追加」系が5通り | `App.jsx:10909` `＋ リードを追加`、`10451` 追加 | `10443` `1枚を追加` / `〇枚の箱を追加`、`9486` 追加、`10456` `＋ 新しい銘柄を入力...`、`9505` `＋ 名前を入力...` | `＋` 全角、`...` 半角3点（他は `…`） |
| A17 | aria-label と表示の語彙が別 | `App.jsx:7043` aria `計測をやり直す（…）` | `7045` 表示 `測り直し`。`7055` aria `診断を閉じる` / 表示 `閉じる` | |
| A18 | aria-label が動作より強い | `App.jsx:15184` aria `セッションを選んで削除`（押すと選択モードに入るだけ） | 実削除は `15153` の別ボタン | |
| A19 | 確認文言の語調 | `App.jsx:10030`/`10036`/`14582` `〜しますか？(元に戻せません)`（句点なし） | `CommunityTab.jsx:1043` `〜完全に削除します。この端末の記録は残ります。`（断定・句点） | |
| A20 | 注意書きの句点 | `CommunityTab.jsx:484`/`491` 文末句点あり | `screens.jsx:877`、`App.jsx:12740` 文末句点なし | |
| A21 | 同義の注意書きが2通り | `App.jsx:11529` `〇枚のうち先頭6枚を表示しています（…）。残り〇枚は…` | `App.jsx:12740` `残り〇件は表示していません（…）。フィルターで…` | 助数詞も枚/件 |
| A22 | eyebrow の綴り・寸法 | `App.jsx:14157` 10px `.08em`、`15458` 10.5px、`12023` 10.5px | `screens.jsx:40` 共通定数 10px、`App.jsx:13676` `メモ` 10.5px（weight・字間なし） | App 側は3箇所直書き |
| A23 | 見出しを置く／置かない | `CommunityTab.jsx:1057`、`screens.jsx:883`「見出しは置かない（9/6 本人指示）」 | `CommunityTab.jsx:481` `コミュニティ`、`890` `プロフィールを編集`、`screens.jsx:614`/`855` eyebrow | 同じ CommunityTab 内で併存 |
| A24 | スラッシュ全角/半角 | `App.jsx:8784` `Hz ／ 記音` | `12557`/`12558`、`aggregate.js:255` `" / "` | 全角は1箇所 |
| A25 | 期間の語 | `App.jsx:12787` 全期間、1週間/1ヶ月/1年 | `stats.js:117` すべて、今週/今月/今年 | 意味も違う（直近N日 vs 暦の区切り）。揃える前に意味を決める |
| A26 | 「目安基準」の残存 | `App.jsx:8785` `音色一致度(目安基準)` | — | `DESIGN-SYSTEM.md:1050` の置換対象「基準」 |
| A27 | 設計書どうしの不一致 | `DESIGN-SYSTEM.md:1053` S.Sax 等 | `north-star-coverage.md:25`「英語表記（Alto / Tenor）」 | 実装は S.Sax で統一済み |

---

## B. 色・フォント・寸法

| # | 項目 | 実態 | 設計書 | 備考 |
|---|---|---|---|---|
| B1 | fontSize の数値直書き | App.jsx: 数値 **147** / `var(--fs-*)` **35**。community は逆（全44件がトークン） | §4.1 | 方言 0-1 |
| B2 | 型スケール外のフォントサイズ | App.jsx 約71件: 11×23（`7032`,`7045`）、12.5×9（`1868`,`8384`）、10×9、10.5×7、14×6、11.5×4（`6974`,`15433`）、17×3、9.5×2、20×2、26×2、13.5×1、9×1 | §4.1 は「9/9.5/10/11 → 12、14 → 15、17 → 18、26 → 28」と吸収先を明記 | |
| B3 | hex 直書き | App.jsx **86件・18種**（`#174585`21 / `#8D95A1`20 / `#435266`14 / `#121F32`7 / `#C3CAD3`4 …）。すべてトークンが在る値 | §1 | community はコードに `#fff` 2件のみ |
| B4 | 体系に無い色 | `App.jsx:4342` `#ECEEF1`（下部ナビの罫）、`4578` `#DDE2E8`（中央線）、`8178` `#FDF0E1` / `#F0D9B8` / `#8A5A00`（注意の面） | §1.3 / §1.5 | 4値がトークン表に無い |
| B5 | `.sans` が no-op | 定義は `App.jsx:4075` の `font-family: var(--font-jp)` のみ（index.css に無い）。body と同一宣言なので App.jsx の 159 箇所が効いていない。`.jp-label`（`index.css:782` +0.02em）は community 13件、App 0件 | §4.4「`.sans` に字間を入れる」 | 設計書自身が「141箇所 no-op」と指摘済み（今は159） |
| B6 | 影の直書き | `0 8px 24px rgba(15,23,42,0.18)` **16件**（シート・モーダル）。トークン参照は6件。つまみ `CommunityTab.jsx:590` `rgba(0,0,0,.2)`、`index.css:754` `rgba(20,30,50,.22)` | §1.10（トークンは card/row の2つ） | シート用の影トークンが無い |
| B7 | 暗幕・zIndex | `rgba(15,23,42,0.28)` **14件**、全部 `zIndex:60`。他に 50（`7020`）/45（`10368`）/40（`14644` 告知、`screens.jsx:1049` PersonSheet）/30（`4340`） | 規定なし | 40 が告知と全画面シートで共用 |
| B8 | 角丸の直書き | `999`×7（`--r-pill` 参照は9件と混在）、`28px 28px 0 0`×6（段に無い）、`2`×14、`12`、`8`、`6`、`5`、`3px 3px 0 0` | §2（5段+50%） | |
| B9 | `--c-sunk` vs `--c-sunken` | `--c-sunk` 2件（`12671`,`13151`）、`--c-sunken` 12件。`index.css:70` のコメントは sunk を「入力欄の地」と書くが実装（`index.css:695`）は sunken | §1.2 sunk=群の境界 / sunken=触れるもの | コメントと実装の食い違い |
| B10 | ページ左右余白の三重化 | 14（`--page-side-pad`）/ 24（リード `App.jsx:9575` +10）/ 30（community `pageStyle` 16 が `.surf-card` の中） | §3 `--page-pad` 14 | カード内文字の左端: App 30px / community **46px** |
| B11 | 44px 未満の押せる要素 | `App.jsx:7707`,`7709`（実高≈24）、`8215`（≈20）、`9486`（5px 8px）、`9487`（×）、押せる div `8213`（≈30） | §5（例外は式の行20 / 条件チップ30 の2つ） | 他の `<button>` 97件のうち98件が `--tap-min` |
| B12 | `--c-ink-3` を本文に | `App.jsx:10888`,`10897`（11px の説明文）、`11820`,`14828`（hex `#8D95A1` の空状態文） | §1.1「読ませたい文章に使わない」 | `screens.jsx:37` は `bodyNoteStyle`(ink-2) を分けて回避済み |
| B13 | `--c-ink-4` を読ませる文字に | `App.jsx:9134` ScrollPicker の非選択の選択肢（`--fs-lg`） | §1.1 装飾専用 | |
| B14 | 機能色の二重管理 | `App.jsx:12261` `pitchCellColor` と `1524` `scoreToColor` が hex/数値で同じ3色を持つ（index.css と合わせて3箇所） | §1.5 | |
| B15 | エラー色のトークンが2つ | `screens.jsx:1188` `--c-bad`、`CommunityTab.jsx:446` `--c-danger`（同値・意味別） | §1.5 / §1.6 | 同じタブ内 |
| B16 | 字間の直書き | `.08em`×4（`12023`,`14157`,`15458`,`screens.jsx:40`）、`-.02em`×3（`.num-tight` と同値だが使用1件） | §4.4 | |
| B17 | serif がリード番号に無い | `--font-serif` は `App.jsx:6280` 音名の1件。リード番号は `--font-num` | §4.3「音名とリード番号のみ」 | |
| B18 | var() のフォールバック値の不一致 | `screens.jsx:1050` `var(--sp-6, 40px)`（実 24）、`1127` `var(--fs-lg, 22px)`（実 18） | §3 / §4.1 | 実行はされないが体系外の値 |
| B19 | gap / padding の段外 | gap 段外 16件（7×7 など）、`padding: "4px 11px"`×8（ピル） | §3 | |
| B20 | 白の直書き | `App.jsx:4410` `#FFFFFF`、`8448`、`CommunityTab.jsx:589` `#fff`、`icons.jsx:63` | §1.6 `--c-on-accent` / `--c-surface` | `index.css:501` が禁じている |
| B21 | 順位色・系列色 | §1.5a・§1.7 とも一致（違反なし） | — | 記録 |

---

## C. UI 部品（同じ役目で作りが違う）

| # | 部品 | 種類 | 場所と違い | 設計書 |
|---|---|---|---|---|
| C1 | 指標の下線タブ | 2 | `App.jsx:13421` MetricUnderlineTabs（margin -8 / padding 0 8、`aria-pressed`）／`screens.jsx:48` UnderlineTabs（-10 / 0 10、`role=tablist`+`aria-selected`、`flexWrap`） | §6.6 |
| C2 | 下線タブの下罫 | 2 | `bordered` を渡すのは `App.jsx:11540` だけ。`13498`,`14230`、community は無し | D30 §7.2 |
| C3 | 「▾」で開く選択 | 3 | `App.jsx:8583` PlainSelect（透明 select、文字 ▾ 10px）／`14375` My Data 集計範囲（10px 文字 → DataOptionSheet）／`screens.jsx:121` FilterPill（透明 select、34px 地、7×7 SVG） | §6.7 B型 |
| C4 | A型チップの重複 | 2 | `screens.jsx:81` Chip と `CommunityTab.jsx:516` PillGroup が同値 | §6.7 |
| C5 | A型なのに ON を地で返すピル | 2 | `App.jsx:1866` ReedStrengthPills、`11505` compareRow: 12.5px / `4px 11px` / `999` / ON=`--c-accent` 塗り | §6.7「ON の合図に地を足さない」 |
| C6 | ctl-pill の中身寸法 | 5 | `7045` 6/10 11px、`7077` 6/8、`10109` 7/14 12px、`13710` 7/12 12px、`8526`/`9425` padding 無し | `index.css:657` は地と角丸のみ |
| C7 | 期間の選択 | 2 | `screens.jsx:363` A型チップ横並び／`App.jsx:14383` 10px 文字 ▾ → 下端シート | §5 |
| C8 | 主要ボタン | 4 | `CommunityTab.jsx:450`（100% / pill / 15 / 700）、`App.jsx:9432` 保存、`9195` 完了、`screens.jsx:1169` 目安に設定（**r-md** / 13） | §6.7 例外5 |
| C9 | 副次ボタン | 3 | `CommunityTab.jsx:456`（sunken / pill / 15 / 600）、`App.jsx:9425`（ctl-pill / 15）、`10109`（ctl-pill / 12 / 7px 14px） | §6.7 B型 |
| C10 | 破壊的ボタン | 2 | `CommunityTab.jsx:462`（danger 塗り / pill / 13）、`App.jsx:13698` DeleteActionButton（ctl-danger + data-armed / 12 / アイコン） | §6.7 |
| C11 | 「編集」 | 2 | `App.jsx:15429` 文字だけ 11.5px、`CommunityTab.jsx:1097` 幅100%の sunken ピル 15px | §4.1 に 11.5 無し |
| C12 | 「戻る」 | 3 | `App.jsx:13726` DetailHeader（地なし・13・accent）／`15133` すべてのセッション（自前・左食い出し）／`screens.jsx:1054` PersonSheet（sunken 塗り・r-md・13・600） | §6.7 |
| C13 | カード寸法の二重定義 | 2 | `index.css:489` `.card` と `screens.jsx:26` cardStyle（同3値）。card-list / rowcard も同様 | §6.6 |
| C14 | 下端シートの器 | 2+3 | `App.jsx:13520` BottomSheet（portal / maxHeight / overflowY）／`CommunityTab.jsx:243` BackupSheet（同値を写し、portal 無・maxHeight 無）／リード系3つ（`10176`,`10237`,`10470`）も写し | §6.7 例外6 |
| C15 | モーダルの位置 | 3 | 下寄せ+つまみ（`13529`）／下寄せ・つまみ無し（`9379` SetAsIdeal）／**中央**（`9168` ReedScoreEditor） | §6.1.5 |
| C16 | 全画面の詳細 | 1（別作法） | `screens.jsx:1048` PersonSheet: fixed / zIndex **40** / 暗幕なし / 角丸なし / 閉じ方は左上ボタン1つ | §6.7 例外6 |
| C17 | 入力欄 | 4 | 既定（sunken 地）／MemoField（地なし）／datetime（地なし・幅190）／controlStyle（既定）／`REED_FORM_CONTROL_STYLE` `0 8px` | §6.7 B型 |
| C18 | トグル | 3+1 | SwitchRow（44×26）／CheckRow（native 18）／`.slist-check`（20）／ViewToggle（アイコン色だけ、高さ35） | §6.7 |
| C19 | 一覧の行 | 3 | RankRow 小（11px 2px / 47 / avatar 34）と目安一覧（同値・別実装）／`.slist-row`（44 / gap 10 / `.slist-sub` **11px**） | §6.6 |
| C20 | 項目行のラベル幅 | 2 | `screens.jsx:943` 7em ／ `CommunityTab.jsx:1002` **6.5em** | §6.6 |
| C21 | アイコン | 3系統 | lucide（size 14/17、stroke 1.8/1.9/2/2.2）／MeasureIcon（自前 24 grid）／山形3種（文字 ▾ 10px、SVG 7×7、SVG 8×8） | §1.8 |
| C22 | 凡例 | 2 | `screens.jsx:784` Legend（14×3 の帯、**破線を表現しない**）／`App.jsx:11358` SeriesSwatch（SVG line、破線あり、白地+2px） | §1.7 |
| C23 | 音名×指標の折れ線 | 2 | `screens.jsx:729` LineChart（viewBox 320×160 拡縮、9px、目盛2本、`C#`）／`App.jsx:11623` NoteAxisLineChart（実寸 1:1、12px、目盛3本、E♭ ガイド、`C♯`） | §1.9 / §4.1 |
| C24 | 空/読み込み/エラー | 4+2 | Empty（12/ink-3/16px）／Centered（13/ink-3/24px）／`App.jsx:11524`（12/20px）／hex `#8D95A1` 4件。エラー色 `--c-bad` / `--c-danger` | §1.1 |
| C25 | 削除の確認 | 2 | `window.confirm` 4件／`13698` の armed 塗り（押下先が confirm） | §6.1.5 |

---

## D. 画面遷移・導線

| # | 導線 | 実態 | 揃っていない点 | 設計書 |
|---|---|---|---|---|
| D1 | 右スワイプで戻る | SwipeBackArea は3画面（`App.jsx:10055`,`14549`,`14605`）。PersonSheet（`screens.jsx:1047`）は無し | 全画面の詳細のうちコミュニティだけ戻れない | §6.3「作法は1つだけ」 |
| D2 | 左スワイプの意味 | onForward はリード個体詳細だけ（比較へ）。他2画面は何も起きない | 同じ詳細で意味が違う | §6.3 |
| D3 | 子タブ切替時の解除 | リードは SubTabs と SwipePager の両方が `exitMode()`、データは SubTabs だけ、コミュニティは person だけ閉じて backup は閉じない | 3通り | — |
| D4 | 戻るボタンの形 | C12 に同じ。すべてのセッションは `15132` で左へ食い出したまま（DetailHeader は右余白に変えた経緯 `13729`） | 当たり判定の取り方が逆 | §5 |
| D5 | スクロール復元・先頭送り | リード: 復元あり・先頭送りなし（`9981`）／My Data: 両方（`14508`）／セッション詳細: 先頭送り（`15409`）／リード詳細: 無し／PersonSheet: 自前容器 | 3〜4通り | F-15 |
| D6 | シートの閉じ方 | BottomSheet: Esc/暗幕/つまみ/下スワイプ/portal。ReedScoreEditor: 下スワイプ・つまみ無し。SetAsIdeal: 暗幕判定が逆（`9382`）・下スワイプ無し。ScrollPicker: portal 無し（呼び手が回避 `10487`）。BackupSheet: portal 無し。PersonSheet: ボタン1つ | 6通り | §6.3 |
| D7 | 主要動作の置き場 | リード詳細=右下フロート（`12234`）／セッション詳細=ヘッダ右（`15426`）／PersonSheet=本文中（`1169`） | 3箇所 | §6.0 |
| D8 | 保存後の遷移 | コミュニティだけ画面を移す（`CommunityTab.jsx:395`）。セッション編集・リード評価・目安設定・人物紹介の目安設定は留まる | 1 vs 4 | — |
| D9 | 破壊的操作の確認 | 文言2型（A19）。公開スイッチ OFF で `unpublishAllIdeals`（`CommunityTab.jsx:176`）は確認なし | サーバの目安を消す操作だけ確認が無い | — |
| D10 | タブ間の状態記憶 | リード: 残る（ただしナビタップで初期化 `2934`）／データ: 消える／コミュニティ: 消える。`navNonce` 再マウントはリードとデータだけ（`4113`,`4205`）、コミュニティ `4223` は無し | 3通り | — |
| D11 | 読み込み失敗 | 本体: 再試行あり（`348`、CONFIG_ERROR は無し）／子タブ: 文言のみ（`162`）／目安: 黙って空（`123`） | 3通り。戻る導線なし | — |
| D12 | フロートの出し分け | 一覧は `pageActive` で括る（`10907`,`15335`）、リード詳細（`12234`）は括らない | — | — |
| D13 | 端末の「戻る」 | `popstate` 登録なし。`history` は `7277` の replaceState 1件 | 全画面が非対応 | — |
